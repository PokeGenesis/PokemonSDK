---
phase: 09-sample-project
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - samples/StarterGame/StarterGame.csproj
  - samples/StarterGame/nuget.config
  - samples/StarterGame/Content/Content.mgcb
  - samples/StarterGame/Content/Fonts/DefaultFont.spritefont
  - samples/StarterGame/Content/Fonts/DejaVuSansMono.ttf
  - samples/StarterGame/Game1.cs
  - samples/StarterGame/Program.cs
autonomous: true
---

<objective>
## Goal
Créer `samples/StarterGame/` — projet MonoGame standalone qui consomme `PokeForge.SDK` via NuGet (jamais ProjectReference), avec MGCB configuré et `dotnet run` fonctionnel.

## Purpose
C'est le point de départ du "template scaffold" (D-20 : CLI Phase 10 embarque ce sample). Chaque développeur qui découvre le SDK doit pouvoir cloner, `dotnet build samples/StarterGame/`, et avoir un jeu qui tourne. Zéro référence interne au repo — D-19 strict.

## Output
- `samples/StarterGame/StarterGame.csproj` — csproj NuGet-only (PokeForge.SDK + MonoGame)
- `samples/StarterGame/nuget.config` — feed local + NuGet.org
- `samples/StarterGame/Content/Content.mgcb` — config MGCB (font uniquement — sprites Wave 09-03)
- `samples/StarterGame/Content/Fonts/DefaultFont.spritefont` + `DejaVuSansMono.ttf`
- `samples/StarterGame/Game1.cs` — fenêtre MonoGame minimale + headless support
- `samples/StarterGame/Program.cs` — entry point avec flag `--headless`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/09-sample-project/09-01-SUMMARY.md

## Source Files à modéliser
@src/SDK.MonoGame/Content/Content.mgcb
@src/SDK.MonoGame/Content/Fonts/DefaultFont.spritefont
@src/SDK.MonoGame/StarterGame.csproj (ne pas copier — lire pour la structure MonoGame uniquement)

## Décisions structurantes
- D-19 : Sample consomme via NuGet — ZÉRO `<ProjectReference>` dans StarterGame.csproj
- D-20 : CLI scaffold = sample stabilisé — ce projet sera le template embarqué Phase 10
- D-02 : MonoGame.DesktopGL — jamais WindowsDX
- Pack workflow : `dotnet pack PokemonSDK.slnx -p:Version=0.1.0 -o ./nupkg` avant build du sample
</context>

<acceptance_criteria>

## AC-1: Build standalone zéro ProjectReference
```gherkin
Given dotnet pack PokemonSDK.slnx -p:Version=0.1.0 -o ./nupkg produit 8 packages
When dotnet build samples/StarterGame/ --configuration Release
Then build réussit 0 erreur — packages résolus depuis ./nupkg/ (PokeForge.SDK) + NuGet.org (MonoGame)
```

## AC-2: Headless run exit 0
```gherkin
Given samples/StarterGame/ est buildé
When dotnet run --project samples/StarterGame/ -- --headless
Then le programme démarre, affiche "StarterGame: headless mode — exiting cleanly", exit code 0
```

## AC-3: D-19 respecté — zéro ProjectReference
```gherkin
Given samples/StarterGame/StarterGame.csproj
When grep -r "ProjectReference" samples/StarterGame/
Then aucun résultat — seuls des PackageReference présents
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold samples/StarterGame/ — csproj + nuget.config + MGCB + font</name>
  <files>
    samples/StarterGame/StarterGame.csproj,
    samples/StarterGame/nuget.config,
    samples/StarterGame/Content/Content.mgcb,
    samples/StarterGame/Content/Fonts/DefaultFont.spritefont,
    samples/StarterGame/Content/Fonts/DejaVuSansMono.ttf
  </files>
  <action>
    **Créer les répertoires :**
    ```
    samples/StarterGame/
    samples/StarterGame/Content/
    samples/StarterGame/Content/Fonts/
    ```

    **`samples/StarterGame/StarterGame.csproj` :**
    ```xml
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <OutputType>WinExe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <RootNamespace>StarterGame</RootNamespace>
        <AssemblyName>StarterGame</AssemblyName>
      </PropertyGroup>
      <ItemGroup>
        <!-- SDK via NuGet — jamais ProjectReference (D-19) -->
        <PackageReference Include="PokeForge.SDK" Version="0.1.0" />
        <PackageReference Include="MonoGame.Framework.DesktopGL" Version="3.8.4.1" />
        <PackageReference Include="MonoGame.Content.Builder.Task" Version="3.8.4.1" />
      </ItemGroup>
    </Project>
    ```

    **`samples/StarterGame/nuget.config` :**
    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <configuration>
      <packageSources>
        <!-- Feed local : dotnet pack PokemonSDK.slnx -p:Version=0.1.0 -o ./nupkg (depuis repo root) -->
        <add key="local-pokeforge" value="../../nupkg" />
        <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
      </packageSources>
    </configuration>
    ```

    **`samples/StarterGame/Content/Content.mgcb` :**
    Copier/adapter depuis `src/SDK.MonoGame/Content/Content.mgcb` — conserver uniquement la section font, supprimer les shaders commentés.
    ```
    #----------------------------- Global Properties ----------------------------#

    /outputDir:bin/$(Platform)/$(Configuration)/Content
    /intermediateDir:obj/$(Platform)/$(Configuration)/Content
    /platform:DesktopGL
    /config:
    /profile:Reach
    /compress:False

    #-------------------------------- References --------------------------------#

    #---------------------------------- Content ---------------------------------#

    #begin Fonts/DefaultFont.spritefont
    /importer:FontDescriptionImporter
    /processor:FontDescriptionProcessor
    /processorParam:PremultiplyAlpha=True
    /processorParam:TextureFormat=Compressed
    /build:Fonts/DefaultFont.spritefont
    ```

    **`samples/StarterGame/Content/Fonts/DefaultFont.spritefont` :**
    Copier depuis `src/SDK.MonoGame/Content/Fonts/DefaultFont.spritefont` (identique).

    **`samples/StarterGame/Content/Fonts/DejaVuSansMono.ttf` :**
    Copier depuis `src/SDK.MonoGame/Content/Fonts/DejaVuSansMono.ttf` (binaire — utiliser cp).
    ```bash
    cp src/SDK.MonoGame/Content/Fonts/DejaVuSansMono.ttf samples/StarterGame/Content/Fonts/DejaVuSansMono.ttf
    ```

    **Points critiques :**
    - AUCUN `<ProjectReference>` dans le csproj — D-19
    - `MonoGame.Content.Builder.Task` requis pour que MGCB s'exécute au build
    - nuget.config doit pointer vers `../../nupkg` (relatif à samples/StarterGame/, pointe vers repo root/nupkg/)
    - Feed `local-pokeforge` en premier (priorité au feed local sur NuGet.org)
  </action>
  <verify>
    ```bash
    dotnet pack PokemonSDK.slnx -p:Version=0.1.0 -o ./nupkg --configuration Release 2>&1 | tail -5
    dotnet restore samples/StarterGame/ 2>&1 | tail -5
    grep -r "ProjectReference" samples/StarterGame/StarterGame.csproj || echo "OK: aucun ProjectReference"
    ```
  </verify>
  <done>AC-1 partiellement satisfait : packages résolus (PokeForge.SDK depuis local feed, MonoGame depuis NuGet.org). AC-3 satisfait.</done>
</task>

<task type="auto">
  <name>Task 2: Game1.cs + Program.cs minimal MonoGame + headless + verify build + run</name>
  <files>
    samples/StarterGame/Game1.cs,
    samples/StarterGame/Program.cs
  </files>
  <action>
    **`samples/StarterGame/Program.cs` :**
    ```csharp
    using StarterGame;

    var headless = args.Contains("--headless");
    using var game = new Game1(headless);
    game.Run();
    ```

    **`samples/StarterGame/Game1.cs` :**
    ```csharp
    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;
    using Microsoft.Xna.Framework.Input;

    namespace StarterGame;

    public class Game1 : Game
    {
        private readonly GraphicsDeviceManager _graphics;
        private readonly bool _headless;
        private SpriteBatch _spriteBatch = null!;
        private SpriteFont _defaultFont = null!;

        public Game1(bool headless = false)
        {
            _headless = headless;
            _graphics = new GraphicsDeviceManager(this)
            {
                PreferredBackBufferWidth  = 1920,
                PreferredBackBufferHeight = 1080
            };
            Content.RootDirectory = "Content";
            IsMouseVisible = true;
        }

        protected override void Initialize()
        {
            base.Initialize();
            if (_headless)
            {
                Console.WriteLine("StarterGame: headless mode — exiting cleanly");
                Exit();
            }
        }

        protected override void LoadContent()
        {
            _spriteBatch = new SpriteBatch(GraphicsDevice);
            _defaultFont = Content.Load<SpriteFont>("Fonts/DefaultFont");
        }

        protected override void Update(GameTime gameTime)
        {
            if (Keyboard.GetState().IsKeyDown(Keys.Escape))
                Exit();
            base.Update(gameTime);
        }

        protected override void Draw(GameTime gameTime)
        {
            GraphicsDevice.Clear(Color.CornflowerBlue);
            _spriteBatch.Begin();
            _spriteBatch.DrawString(
                _defaultFont,
                "PokeForge StarterGame — Appuyer sur Echap pour quitter",
                new Vector2(20, 20),
                Color.White);
            _spriteBatch.End();
            base.Draw(gameTime);
        }
    }
    ```

    **Points critiques :**
    - `_headless` path : Initialize() → Exit() immédiat → AC-2
    - `LoadContent()` charge le font compilé par MGCB au build
    - `OutputType>WinExe` dans csproj évite une fenêtre console sur Windows
    - `GraphicsDeviceManager` alloué dans le constructeur (obligatoire MonoGame)
    - `_spriteBatch` et `_defaultFont` annotés `null!` — initialisés dans LoadContent, jamais null en pratique
  </action>
  <verify>
    ```bash
    dotnet tool restore
    dotnet build samples/StarterGame/ --configuration Release 2>&1 | tail -10
    dotnet run --project samples/StarterGame/ --configuration Release -- --headless
    echo "Exit code: $?"
    ```
    Attendu : build 0 erreur, run exit code 0, message "StarterGame: headless mode — exiting cleanly".
  </verify>
  <done>AC-1 et AC-2 satisfaits : build 0 erreur, headless exit 0.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/` — aucune modification des projets SDK
- `PokemonSDK.slnx` — `samples/StarterGame/` n'est PAS ajouté (D-19)
- `tests/` — inchangé
- `.github/workflows/` — CI mis à jour en Plan 09-04, pas ici

## SCOPE LIMITS
- Ce plan crée uniquement le scaffold MonoGame fonctionnel
- Pas d'assets CC0 Kenney (sprites/tiles) — déférés Plan 09-03 (overworld)
- Pas de BGM FreeMusicArchive — déféré Plan 09-03
- Pas de contenu de demo (combat, dialogue, badge) — Plans 09-03 + 09-04
- Pas de README getting-started — Plan 09-04
- Pas de CI update — Plan 09-04

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `grep -r "ProjectReference" samples/StarterGame/StarterGame.csproj` → aucun résultat
- [ ] `dotnet restore samples/StarterGame/` → 0 erreur (PokeForge.SDK résolu depuis local feed)
- [ ] `dotnet build samples/StarterGame/ --configuration Release` → 0 erreur, 0 warning
- [ ] `dotnet run --project samples/StarterGame/ --configuration Release -- --headless` → exit 0
- [ ] `samples/StarterGame/` absent de `PokemonSDK.slnx`
- [ ] `ls samples/StarterGame/Content/Fonts/` → `DefaultFont.spritefont` + `DejaVuSansMono.ttf`
</verification>

<success_criteria>
- StarterGame.csproj sans ProjectReference (D-19 strict)
- `dotnet build samples/StarterGame/` → 0 erreur depuis un repo cloné (après `dotnet pack` local)
- `dotnet run -- --headless` → exit 0 (headless CI-safe)
- MGCB compile le font au build (Content/bin/ généré)
- Prêt pour Wave 2 (09-03) : overworld + assets CC0
</success_criteria>

<output>
Après completion, créer `.paul/phases/09-sample-project/09-02-SUMMARY.md`
</output>

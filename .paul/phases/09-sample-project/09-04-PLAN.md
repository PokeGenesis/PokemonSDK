---
phase: 09-sample-project
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - samples/StarterGame/Game1.cs
  - samples/StarterGame/Scenes/OverworldScene.cs
  - samples/StarterGame/Content/Scripts/npc_dialogue.lua
  - samples/StarterGame/Content/Content.mgcb
  - samples/StarterGame/README.md
autonomous: true
---

<objective>
## Goal
Intégrer SDK.Battle + SDK.Scripting + ISaveSystem dans StarterGame (NuGet-only D-19) — combat NPC via BattleEngine + NuzlockePlugin, badge Lua, save/load F5/F9, README getting-started. Phase 9 complète.

## Purpose
Démontre la valeur core du SDK : `dotnet add package PokeForge.SDK` → combat + scripting + persistance opérationnels. Preuve finale DX-04.

## Output
- `Game1.cs` étendu : ISaveSystem + LuaScriptEngine construits, passés à OverworldScene
- `OverworldScene.cs` câblé : BattleEngine + NuzlockePlugin internes, combat NPC, Lua badge, F5/F9
- `Content/Scripts/npc_dialogue.lua` : badge boulder Lua
- `Content/Content.mgcb` : directive `/copy:` pour .lua
- `samples/StarterGame/README.md` : getting-started 15 lignes
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/09-sample-project/09-03-SUMMARY.md

## Source Files
@samples/StarterGame/Game1.cs
@samples/StarterGame/Scenes/OverworldScene.cs
@samples/StarterGame/Content/Content.mgcb
</context>

<acceptance_criteria>

## AC-1 : Combat NPC complet
```gherkin
Given BattleEngine construit avec NeutralTypeChart + Gen1DamageFormula + StoryDifficultyMode + NuzlockePlugin
When joueur presse Espace adjacent au NPC (row 7, col 11)
Then BattleEngine.RunBattle(request) retourne BattleResult(PlayerWon, TurnsElapsed > 0)
     et texte "Victoire!" ou "Défaite…" s'affiche dans la boîte dialogue
```

## AC-2 : Badge Lua
```gherkin
Given npc_dialogue.lua contient `badges:AwardBadge('boulder')`
     et BadgeApi enregistré sur _scriptEngine avant LoadFile()
When joueur gagne le combat (BattleResult.PlayerWon == true)
Then _gameState.GetFlag<bool>("badge_boulder") == true
     et "Badge: boulder ✓" affiché à l'écran
```

## AC-3 : Save/load cycle
```gherkin
Given badge_boulder = true dans _gameState courant
When F5 pressé → save1.json créé, puis F9 pressé → reload
Then _gameState.GetFlag<bool>("badge_boulder") == true après reload
     (texte badge toujours affiché)
```

## AC-4 : Build 0 erreur + headless exit 0 + D-19
```gherkin
Given StarterGame.csproj inchangé — zéro ProjectReference ajouté (D-19)
When dotnet build --configuration Release && dotnet run -- --headless
Then exit code 0, "StarterGame: headless mode — exiting cleanly" affiché
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : Game1.cs — SDK composition (ISaveSystem + LuaScriptEngine)</name>
  <files>samples/StarterGame/Game1.cs</files>
  <action>
    Étendre Game1 pour construire les deux dépendances SDK légères et les passer à OverworldScene.
    Toutes les dépendances sont disponibles via PokeForge.SDK 0.1.0 (déjà référencé — D-19 OK).

    Namespaces à ajouter :
    - `using SDK.Core.Interfaces;`   (ISaveSystem)
    - `using SDK.Core.Services;`     (SaveSystem)
    - `using SDK.Scripting.Engine;`  (LuaScriptEngine)

    Dans les champs de Game1 :
    ```
    private readonly ISaveSystem _saveSystem = new SaveSystem();
    private readonly LuaScriptEngine _scriptEngine = new LuaScriptEngine();
    ```

    Dans LoadContent(), remplacer :
    ```
    _scene = new OverworldScene(GraphicsDevice);
    ```
    par :
    ```
    _scene = new OverworldScene(GraphicsDevice, _saveSystem, _scriptEngine);
    ```

    NE PAS MODIFIER :
    - Headless guard (Initialize)
    - BGM try/catch guard
    - Constantes TilemapData pour window size
    - StarterGame.csproj (zéro ProjectReference D-19)
  </action>
  <verify>
    `dotnet build samples/StarterGame --configuration Release` → 0 Error(s)
  </verify>
  <done>Prérequis pour AC-1/2/3 satisfaits — ISaveSystem + LuaScriptEngine accessibles dans OverworldScene</done>
</task>

<task type="auto">
  <name>Task 2 : OverworldScene.cs + npc_dialogue.lua + Content.mgcb</name>
  <files>
    samples/StarterGame/Scenes/OverworldScene.cs,
    samples/StarterGame/Content/Scripts/npc_dialogue.lua,
    samples/StarterGame/Content/Content.mgcb
  </files>
  <action>
    ────────────────────────────────────────
    A) OverworldScene.cs — constructeur étendu
    ────────────────────────────────────────
    Namespaces à ajouter :
    - `using SDK.Core.Interfaces;`          (ISaveSystem, ITypeChart)
    - `using SDK.Core.Services;`            (SaveSystem)
    - `using SDK.Core.ValueObjects;`        (BattleRequest, BattlePokemon, BattleMove, BattleConfig, BattleResult, GameState)
    - `using SDK.Core.Enums;`               (MoveCategory)
    - `using SDK.Battle;`                   (BattleEngine)
    - `using SDK.Battle.Formulas;`          (Gen1DamageFormula)
    - `using SDK.Battle.Difficulty;`        (StoryDifficultyMode)
    - `using SDK.Battle.Plugins;`           (PluginRegistry)
    - `using SDK.Plugins.Nuzlocke;`         (NuzlockePlugin)
    - `using SDK.Scripting.Engine;`         (LuaScriptEngine)
    - `using SDK.Scripting.Bindings;`       (BadgeApi)

    Nouveaux champs :
    ```
    private GameState _gameState = new();
    private readonly ISaveSystem _saveSystem;
    private readonly LuaScriptEngine _scriptEngine;
    private readonly BattleEngine _battleEngine;
    private string _lastBattleResult = string.Empty;
    private string _scriptPath = string.Empty;
    private KeyboardState _prevKb;
    ```

    Modifier le constructeur en :
    `public OverworldScene(GraphicsDevice gfx, ISaveSystem saveSystem, LuaScriptEngine scriptEngine)`

    Dans le corps du constructeur (après _gfx = gfx, etc.) :
    - Assigner _saveSystem, _scriptEngine
    - Créer PluginRegistry, enregistrer NuzlockePlugin avec callback lambda :
      `(flagKey, val) => _gameState = _gameState.WithFlag(flagKey, val)`
    - Créer BattleEngine :
      `new BattleEngine(new Gen1DamageFormula(), new StoryDifficultyMode(), new StoryDifficultyMode(), new NeutralTypeChart(), plugins)`
    - Ajouter classe privée NeutralTypeChart : ITypeChart → `GetFactor(a,b,g) => 1.0m`

    Dans LoadContent(), après _font = ... :
    - Définir `_scriptPath = Path.Combine(AppContext.BaseDirectory, "Content", "Scripts", "npc_dialogue.lua")`

    ────────────────────────────────────────
    B) OverworldScene.cs — Update() étendu
    ────────────────────────────────────────
    Remplacer le bloc interaction NPC (Espace + tuile adjacente) :

    Quand NPC adjacent détecté (IsNpc = true) :
    1. Construire player BattlePokemon :
       - SpeciesId=1, Nickname="Bulbasaur", Level=5, CurrentHp/MaxHp=45
       - Attack=49, Defense=49, SpecialAttack=65, SpecialDefense=65, Speed=45
       - Type1Id=12 (Grass), Type2Id=4 (Poison)
       - Moves = [ new BattleMove(MoveId=33, Identifier="tackle", TypeId=1, Category=MoveCategory.Physical, Power=40, Accuracy=100, CurrentPP=35, MaxPP=35) ]
    2. Construire opponent BattlePokemon :
       - SpeciesId=19, Nickname="Rattata", Level=3, CurrentHp/MaxHp=30
       - Attack=56, Defense=35, SpecialAttack=25, SpecialDefense=35, Speed=72
       - Type1Id=1 (Normal), Type2Id=null
       - Moves = [ new BattleMove(MoveId=33, Identifier="tackle", TypeId=1, Category=MoveCategory.Physical, Power=40, Accuracy=100, CurrentPP=35, MaxPP=35) ]
    3. `var request = new BattleRequest(player, opponent, new BattleConfig());`
    4. `var result = _battleEngine.RunBattle(request);`
    5. Si result.PlayerWon :
       - `_lastBattleResult = $"Victoire en {result.TurnsElapsed} tours!";`
       - Exécuter script Lua :
         ```
         var api = new BadgeApi(_gameState);
         _scriptEngine.RegisterApi("badges", api);
         _scriptEngine.LoadFile(_scriptPath);
         _gameState = api.GetState();
         ```
       - Mettre à jour `_dialogue` avec badge status
    6. Sinon : `_lastBattleResult = "Défaite...";`
    7. `_dialogue = _lastBattleResult;`

    Ajouter gestion F5/F9 (just-pressed via `_prevKb`) :
    - F5 just-pressed :
      ```
      Directory.CreateDirectory("data");
      _saveSystem.Save(_gameState, "data/save1.json");
      _dialogue = "Sauvegardé!";
      ```
    - F9 just-pressed :
      ```
      var loaded = _saveSystem.Load("data/save1.json");
      if (loaded != null) { _gameState = loaded; _dialogue = "Chargé!"; }
      ```

    Fin de Update() : `_prevKb = kb;`

    ────────────────────────────────────────
    C) OverworldScene.cs — Draw() étendu
    ────────────────────────────────────────
    Après le rendu tilemap+joueur, avant `sb.End()`, ajouter :
    - Afficher badge si `_gameState.GetFlag<bool>("badge_boulder")` == true :
      `sb.DrawString(_font, "Badge: boulder ✓", new Vector2(20, TilemapData.Height * TD - 30), Color.Gold);`
    - Garder la boîte dialogue existante (affiche `_dialogue`)

    ────────────────────────────────────────
    D) Content/Scripts/npc_dialogue.lua
    ────────────────────────────────────────
    Créer le fichier avec exactement :
    ```lua
    badges:AwardBadge('boulder')
    ```

    ────────────────────────────────────────
    E) Content/Content.mgcb
    ────────────────────────────────────────
    Ajouter après le bloc bgm.ogg existant :
    ```
    #begin Scripts/npc_dialogue.lua
    /copy:Scripts/npc_dialogue.lua
    ```
    La directive `/copy:` copie le fichier verbatim vers
    `Content/bin/DesktopGL/Content/Scripts/npc_dialogue.lua`

    NE PAS MODIFIER :
    - TilemapData.cs (carte + helpers inchangés)
    - Assets Tileset.png + bgm.ogg
    - SamplerState.PointClamp dans sb.Begin()
    - Warp bidirectionnel existant
    - StarterGame.csproj
  </action>
  <verify>
    1. `dotnet tool restore && dotnet-mgcb build samples/StarterGame/Content/Content.mgcb`
       → `Content/bin/DesktopGL/Content/Scripts/npc_dialogue.lua` présent
    2. `dotnet build samples/StarterGame --configuration Release` → 0 Error(s)
    3. `grep "badge_boulder\|AwardBadge" samples/StarterGame/Scenes/OverworldScene.cs` → ≥1 match (AC-2)
    4. `grep "ISaveSystem\|_saveSystem\|SaveSystem" samples/StarterGame/Scenes/OverworldScene.cs` → match (AC-3)
    5. `grep "BattleEngine\|RunBattle\|NuzlockePlugin" samples/StarterGame/Scenes/OverworldScene.cs` → match (AC-1)
  </verify>
  <done>AC-1 (combat NPC), AC-2 (badge Lua), AC-3 (save/load F5/F9) satisfaits</done>
</task>

<task type="auto">
  <name>Task 3 : README.md getting-started + vérification finale</name>
  <files>samples/StarterGame/README.md</files>
  <action>
    Créer `samples/StarterGame/README.md` :

    ```markdown
    # StarterGame — PokeForge SDK Demo

    Démo jouable du SDK via NuGet uniquement (D-19).

    ## Prérequis
    - .NET 10 SDK
    - `dotnet tool restore` (installe dotnet-mgcb 3.8.4.1)

    ## Lancer
    ```bash
    cd samples/StarterGame
    dotnet run
    ```

    ## Contrôles
    | Touche | Action |
    |--------|--------|
    | Flèches | Déplacer le joueur (jaune) |
    | Espace | Interagir avec le NPC (magenta) |
    | F5 | Sauvegarder |
    | F9 | Charger |
    | Esc | Quitter |

    ## Ce que ça démontre
    - **SDK.Battle** : BattleEngine 1v1 headless + NuzlockePlugin
    - **SDK.Scripting** : `badges:AwardBadge('boulder')` via Lua SoftSandbox
    - **ISaveSystem** : save/load GameState JSON
    - Assets **CC0** : Kenney Tiny Town tileset + Music Jingles NES00

    > PokeForge SDK — MIT License — <https://github.com/PokeGenesis/PokemonSDK>
    ```

    Puis vérifier la régression headless :
    `dotnet run --project samples/StarterGame -- --headless`
    → exit code 0

    Vérifier D-19 :
    `grep "ProjectReference" samples/StarterGame/StarterGame.csproj | wc -l` → 0
  </action>
  <verify>
    1. `ls samples/StarterGame/README.md` → présent
    2. `dotnet run --project samples/StarterGame -- --headless` → exit code 0
    3. `grep "ProjectReference" samples/StarterGame/StarterGame.csproj | wc -l` → 0
  </verify>
  <done>AC-4 satisfait : D-19 confirmé, headless exit 0, README créé — Phase 9 complète</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `samples/StarterGame/StarterGame.csproj` — zéro ProjectReference (D-19 absolu)
- `samples/StarterGame/World/TilemapData.cs` — carte + helpers inchangés
- `samples/StarterGame/Content/Sprites/Tileset.png` — asset CC0 figé
- `samples/StarterGame/Content/Music/bgm.ogg` — asset CC0 figé
- `src/` — zéro modification aux projets SDK

## SCOPE LIMITS
- Pas de DB SQLite dans StarterGame — BattlePokemon avec stats hard-codées uniquement
- NeutralTypeChart (GetFactor → 1.0m) — pas de vrai type chart multi-générations
- README : 15-20 lignes max
- Pas de RandomizerPlugin dans cette wave

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build samples/StarterGame -c Release` → 0 Error(s) 0 Warning(s)
- [ ] `dotnet-mgcb build samples/StarterGame/Content/Content.mgcb` → `Content/bin/DesktopGL/Content/Scripts/npc_dialogue.lua` présent
- [ ] `dotnet run --project samples/StarterGame -- --headless` → exit code 0 (régression 09-02/09-03)
- [ ] `grep "ProjectReference" samples/StarterGame/StarterGame.csproj | wc -l` → 0 (D-19)
- [ ] `grep "AwardBadge\|badge_boulder" samples/StarterGame/Scenes/OverworldScene.cs` → match (AC-2)
- [ ] `grep "_saveSystem\|F5\|F9" samples/StarterGame/Scenes/OverworldScene.cs` → match (AC-3)
- [ ] `grep "BattleEngine\|RunBattle" samples/StarterGame/Scenes/OverworldScene.cs` → match (AC-1)
- [ ] `grep "NuzlockePlugin" samples/StarterGame/Scenes/OverworldScene.cs` → match
</verification>

<success_criteria>
- 3/3 tasks complétées
- AC-1 (combat NPC), AC-2 (badge Lua), AC-3 (save/load), AC-4 (build + headless + D-19) tous PASS
- README.md présent dans samples/StarterGame/
- Phase 9 complète → prêt pour TAG v1.0 + squash merge feature/phase9 → dev → staging → main (E-04)
</success_criteria>

<output>
Après completion : créer `.paul/phases/09-sample-project/09-04-SUMMARY.md`
</output>

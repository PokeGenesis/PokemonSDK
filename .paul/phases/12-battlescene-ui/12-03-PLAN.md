---
phase: 12-battlescene-ui
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/SDK.MonoGame/UI/HpBar.cs
  - src/SDK.MonoGame/Scenes/BattleScene.cs
  - src/SDK.MonoGame/Program.cs
autonomous: true
---

<objective>
## Goal
Créer `HpBar` (widget HP GBA-style) et `BattleScene` (machine à états Init→SelectMove→Running→BattleEnd) — enregistrés dans DI au boot de Game1 comme décidé en review engineering.

## Purpose
`HpBar` est un widget autonome réutilisable dans toutes les scènes de combat. `BattleScene` est le conteneur principal de BTLUI-01 : elle orchestre `IBattleEngine.RunTurn()`, affiche les sprites placeholder, les HP bars, et délègue MoveMenu/StatusIcon (Plan 12-04). L'enregistrement DI au boot garantit que BattleScene est prête avant tout `SwitchToScene()`.

## Output
- Nouveaux fichiers : `src/SDK.MonoGame/UI/HpBar.cs`, `src/SDK.MonoGame/Scenes/BattleScene.cs`
- Modifié : `src/SDK.MonoGame/Program.cs` (enregistrement DI BattleScene + IBattleEngine)
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/phases/12-battlescene-ui/12-01-SUMMARY.md
@.paul/phases/12-battlescene-ui/12-02-SUMMARY.md

## Source Files
@src/SDK.MonoGame/Scenes/IGameScene.cs
@src/SDK.MonoGame/Game1.cs
@src/SDK.MonoGame/Program.cs
@src/SDK.Core/Interfaces/IBattleEngine.cs
@src/SDK.Core/ValueObjects/BattleState.cs
@src/SDK.Core/ValueObjects/BattlePokemon.cs
@src/SDK.Core/Enums/StatusConditionId.cs
</context>

<acceptance_criteria>

## AC-1: HpBar rend une barre colorée selon % HP à la résolution interne 480x270
```gherkin
Given HpBar.Draw(spriteBatch, currentHp, maxHp, position, label, font)
When currentHp/maxHp > 0.5 (vert), 0.2..0.5 (jaune), < 0.2 (rouge)
Then un rectangle coloré est dessiné à position dans le referentiel 480x270
And un label textuel est rendu si font != null
```

## AC-2: BattleScene implémente IGameScene avec machine à états
```gherkin
Given BattleScene.LoadBattle(BattleState initialState) est appelé
When BattleScene.Update(GameTime) est appelé
Then la state machine transite : Init → SelectMove (attend input joueur)
When le joueur sélectionne une move (signal PlayerMoveSelected)
Then la state machine transite : SelectMove → Running → exécute RunTurn() → retour SelectMove ou BattleEnd
```

## AC-3: BattleScene.Draw() affiche HP bars + sprites placeholder
```gherkin
Given BattleScene est en état SelectMove ou Running
When BattleScene.Draw(spriteBatch, gameTime) est appelé
Then HpBar joueur (bas-droite) et HpBar adversaire (haut-gauche) sont rendus
And des rectangles 96x96 pixels représentent les sprites (placeholder)
And le fond est noir (combat commence sans tilemap background dans ce plan)
```

## AC-4: BattleScene enregistrée dans DI au boot (pas de lazy creation)
```gherkin
Given Program.cs construit le ServiceProvider
When l'application démarre
Then BattleScene est déjà instanciée dans le DI container (singleton)
And Game1 peut appeler SwitchToScene(sp.GetRequiredService<BattleScene>()) sans new()
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: HpBar widget</name>
  <files>src/SDK.MonoGame/UI/HpBar.cs</files>
  <action>
    Créer `src/SDK.MonoGame/UI/HpBar.cs` dans namespace `SDK.MonoGame.UI` :

    ```
    public sealed class HpBar
    {
        private readonly Texture2D _pixel;

        public HpBar(GraphicsDevice gd)
        {
            _pixel = new Texture2D(gd, 1, 1);
            _pixel.SetData(new[] { Color.White });
        }

        public void Draw(SpriteBatch sb, int currentHp, int maxHp, Vector2 position,
                         int barWidth, int barHeight, string label, SpriteFont? font)
        {
            float ratio = maxHp > 0 ? (float)currentHp / maxHp : 0f;
            var color = ratio > 0.5f ? Color.Green
                      : ratio > 0.2f ? Color.Yellow
                      : Color.Red;

            // Fond gris
            sb.Draw(_pixel, new Rectangle((int)position.X, (int)position.Y, barWidth, barHeight), Color.DarkGray);
            // Barre colorée
            sb.Draw(_pixel, new Rectangle((int)position.X, (int)position.Y, (int)(barWidth * ratio), barHeight), color);

            if (font != null)
                sb.DrawString(font, $"{label} {currentHp}/{maxHp}",
                    new Vector2(position.X, position.Y - 12), Color.White);
        }

        public void Dispose() => _pixel.Dispose();
    }
    ```

    Usings requis : Microsoft.Xna.Framework, Microsoft.Xna.Framework.Graphics.
    Le Draw est appelé DANS BeginScene/EndScene (référentiel 480x270 — D-14).
    barWidth recommandé : 80px, barHeight : 8px à cette résolution.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs</verify>
  <done>AC-1 satisfait : HpBar compile et rend une barre colorée</done>
</task>

<task type="auto">
  <name>Task 2: BattleScene state machine + DI registration</name>
  <files>src/SDK.MonoGame/Scenes/BattleScene.cs, src/SDK.MonoGame/Program.cs</files>
  <action>
    Créer `src/SDK.MonoGame/Scenes/BattleScene.cs` :

    Namespace : `SDK.MonoGame.Scenes`
    Enum interne : `BattlePhase { Init, SelectMove, Running, BattleEnd }`

    Constructor :
    ```
    public BattleScene(IBattleEngine engine, GraphicsDevice graphicsDevice, SpriteFont? font = null)
    ```
    Champs : `_engine`, `_hpBar = new HpBar(graphicsDevice)`, `_font`, `_state`, `_phase = BattlePhase.Init`
    Champ `_selectedMove : BattleMove?`

    Méthode publique `LoadBattle(BattleState initialState)` :
    - Assigne `_state = initialState`
    - Met `_phase = BattlePhase.SelectMove`

    `Update(GameTime gameTime)` :
    ```
    switch (_phase)
    {
        case BattlePhase.SelectMove:
            // MoveMenu signal : si _selectedMove != null → Running
            if (_selectedMove != null) { ExecuteTurn(); _selectedMove = null; }
            break;
        case BattlePhase.Running:
            // Transition auto vers SelectMove ou BattleEnd selon HP
            _phase = (_state.Player.CurrentHp <= 0 || _state.Opponent.CurrentHp <= 0)
                ? BattlePhase.BattleEnd
                : BattlePhase.SelectMove;
            break;
    }
    ```

    Méthode privée `ExecuteTurn()` :
    ```
    var opponentMove = _engine.SelectOpponentMove(_state);
    _state = _engine.RunTurn(_state, _selectedMove!, opponentMove);
    _phase = BattlePhase.Running;
    ```

    Propriété publique `void SetPlayerMove(BattleMove move) => _selectedMove = move;`
    (MoveMenu appellera cette méthode depuis Plan 12-04)

    `Draw(SpriteBatch sb, GameTime gameTime)` dans référentiel 480x270 :
    - Fond noir : DrawRectangle(sb, fullRect, Color.Black)
    - Sprite adversaire placeholder : rect 96x96 à (200, 20) en Color.Gray
    - Sprite joueur placeholder : rect 96x96 à (80, 80) en Color.DarkGray
    - HpBar adversaire : position (160, 10), label "FOE"
    - HpBar joueur : position (20, 130), label "PLR"
    - Si `_phase == BattlePhase.BattleEnd` : afficher "BATTLE END" centré via DrawString
    - Note : DrawRectangle helper local utilise un Texture2D 1x1 blanc (créer dans constructor)

    Méthode privée helper `DrawRect(SpriteBatch sb, Rectangle rect, Color c)` :
    Réutilise le même pixel blanc que HpBar (ou un champ `_pixel` interne).

    Dans `src/SDK.MonoGame/Program.cs`, ajouter dans les registrations DI :
    - `services.AddSingleton<IBattleEngine, BattleEngine>()` si pas encore enregistré
    - `services.AddSingleton<BattleScene>()` — SINGLETON, créé au boot

    Usings : `SDK.MonoGame.Scenes`, `SDK.Battle`, `SDK.Core.Interfaces`, `SDK.Core.ValueObjects`.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs && dotnet test tests/SDK.MonoGame.Tests/ -- 3/3 verts</verify>
  <done>AC-2, AC-3, AC-4 satisfaits : BattleScene compile, DI enregistré, smoke tests toujours verts</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.MonoGame/Scenes/IGameScene.cs` — interface figée depuis Plan 12-02
- `src/SDK.MonoGame/UI/HpBar.cs` ne dépend pas de MoveMenu/StatusIcon (Plan 12-04)
- `src/SDK.MonoGame/Rendering/RenderPipeline.cs` — inchangé
- D-14 : résolution 480x270 — toutes coordonnées dans ce plan sont en pixels internes

## SCOPE LIMITS
- MoveMenu N'EST PAS dans ce plan (Plan 12-04) — `SetPlayerMove()` est le point d'entrée manuel
- StatusIcon N'EST PAS dans ce plan (Plan 12-04)
- Wipe transition overworld→battle N'EST PAS dans ce plan (Plan 12-05)
- BattleScene ne gère pas le retour à WorldScene dans ce plan (Plan 12-05)
- Les sprites réels (PNG 96x96) sont en Phase 17 — placeholder colored rects seulement

</boundaries>

<verification>
Avant de déclarer ce plan complet :
- [ ] `dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj` — 0 erreurs, 0 warnings
- [ ] `dotnet test tests/SDK.MonoGame.Tests/` — 3/3 smoke tests toujours verts
- [ ] `dotnet build PokemonSDK.slnx` — solution entière compile
- [ ] BattleScene est résolvable via `sp.GetRequiredService<BattleScene>()`
- [ ] HpBar rend du vert quand HP > 50%, jaune 20-50%, rouge < 20%
</verification>

<success_criteria>
- HpBar widget fonctionnel avec 3 couleurs
- BattleScene state machine : Init→SelectMove→Running→BattleEnd
- BattleScene.LoadBattle() + SetPlayerMove() + ExecuteTurn() fonctionnels
- BattleScene singleton dans DI, prêt pour SwitchToScene()
</success_criteria>

<output>
Après complétion, créer `.paul/phases/12-battlescene-ui/12-03-SUMMARY.md`
</output>

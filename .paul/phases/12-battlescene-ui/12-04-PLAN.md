---
phase: 12-battlescene-ui
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/SDK.MonoGame/UI/StatusIcon.cs
  - src/SDK.MonoGame/UI/MoveMenu.cs
  - src/SDK.MonoGame/Scenes/BattleScene.cs
autonomous: true
---

<objective>
## Goal
Créer `StatusIcon` (indicateur visuel de statut PSN/BRN/SLP/PRZ/FRZ) et `MoveMenu` (menu 4 moves avec couleurs de type et curseur) — puis les intégrer dans `BattleScene.Draw()`.

## Purpose
BTLUI-01 requiert un menu de moves jouable et l'affichage des icônes de statut. `MoveMenu` permet au joueur de sélectionner une move via input clavier, ce qui déclenche `BattleScene.SetPlayerMove()` et enclenche un tour. `StatusIcon` lit `BattlePokemon.Status` (ajouté en Plan 12-01) pour afficher les altérations actives.

## Output
- Nouveaux fichiers : `src/SDK.MonoGame/UI/StatusIcon.cs`, `src/SDK.MonoGame/UI/MoveMenu.cs`
- Modifié : `src/SDK.MonoGame/Scenes/BattleScene.cs` (intégration MoveMenu + StatusIcon dans Update/Draw)
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/phases/12-battlescene-ui/12-02-SUMMARY.md
@.paul/phases/12-battlescene-ui/12-03-SUMMARY.md

## Source Files
@src/SDK.MonoGame/Scenes/IGameScene.cs
@src/SDK.MonoGame/Scenes/BattleScene.cs
@src/SDK.MonoGame/Input/KeyboardInputProvider.cs
@src/SDK.Core/ValueObjects/BattlePokemon.cs
@src/SDK.Core/ValueObjects/BattleMove.cs
@src/SDK.Core/Enums/StatusConditionId.cs
</context>

<acceptance_criteria>

## AC-1: StatusIcon affiche la bonne couleur + label pour chaque StatusConditionId
```gherkin
Given StatusIcon.Draw(sb, StatusConditionId.Poison, position, font)
When le sprite batch est actif
Then un carré coloré PSN (violet) + label "PSN" est rendu à position
And StatusConditionId.None n'affiche rien (no-op)
And chaque statut a sa couleur : BRN=orange, SLP=bleu, PRZ=jaune, FRZ=cyan, PSN=violet
```

## AC-2: MoveMenu affiche les 4 moves avec couleur de type et le curseur
```gherkin
Given MoveMenu créé avec IReadOnlyList<BattleMove> moves (1 à 4 moves)
When MoveMenu.Draw(sb, position, font) est appelé
Then chaque move est affiché avec son identifier et sa couleur de type
And le curseur (▶) indique le slot sélectionné
```

## AC-3: MoveMenu répond à l'input clavier (Up/Down/Confirm)
```gherkin
Given MoveMenu est visible et _cursorIndex = 0
When Update() reçoit KeyUp → _cursorIndex décrémente (wraparound)
When Update() reçoit KeyDown → _cursorIndex incrémente (wraparound)
When Update() reçoit KeyZ (confirmer) → SelectedMove est assigné à moves[_cursorIndex]
Then SelectedMove != null signal indique au BattleScene que le joueur a choisi
```

## AC-4: BattleScene intègre MoveMenu + StatusIcon dans Update/Draw
```gherkin
Given BattleScene en état SelectMove
When Draw() est appelé
Then MoveMenu est affiché en bas de l'écran (référentiel 480x270)
And StatusIcon.Draw() est appelé pour player et opponent
When MoveMenu.SelectedMove != null
Then BattleScene.SetPlayerMove(move) est appelé et MoveMenu.SelectedMove est réinitialisé
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: StatusIcon widget</name>
  <files>src/SDK.MonoGame/UI/StatusIcon.cs</files>
  <action>
    Créer `src/SDK.MonoGame/UI/StatusIcon.cs` dans namespace `SDK.MonoGame.UI` :

    ```
    public sealed class StatusIcon
    {
        private static readonly Dictionary<StatusConditionId, (Color color, string label)> StatusDisplay = new()
        {
            [StatusConditionId.Sleep]     = (Color.CornflowerBlue, "SLP"),
            [StatusConditionId.Freeze]    = (Color.Cyan,           "FRZ"),
            [StatusConditionId.Burn]      = (Color.OrangeRed,      "BRN"),
            [StatusConditionId.Poison]    = (Color.MediumPurple,   "PSN"),
            [StatusConditionId.Paralysis] = (Color.Yellow,         "PRZ"),
        };

        private readonly Texture2D _pixel;

        public StatusIcon(GraphicsDevice gd)
        {
            _pixel = new Texture2D(gd, 1, 1);
            _pixel.SetData(new[] { Color.White });
        }

        public void Draw(SpriteBatch sb, StatusConditionId? status, Vector2 position, SpriteFont? font)
        {
            if (status is null or StatusConditionId.None) return;
            if (!StatusDisplay.TryGetValue(status.Value, out var display)) return;

            sb.Draw(_pixel, new Rectangle((int)position.X, (int)position.Y, 24, 10), display.color);
            if (font != null)
                sb.DrawString(font, display.label, new Vector2(position.X + 2, position.Y + 1), Color.White, 0f,
                    Vector2.Zero, 0.5f, SpriteEffects.None, 0f);
        }

        public void Dispose() => _pixel.Dispose();
    }
    ```

    Usings : `Microsoft.Xna.Framework`, `Microsoft.Xna.Framework.Graphics`, `SDK.Core.Enums`.
    Scale 0.5f sur le texte : adapté à la résolution 480x270 (D-14) — caractères réduits.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs</verify>
  <done>AC-1 satisfait : StatusIcon compile, affiche rien pour None, couleur correcte pour chaque statut</done>
</task>

<task type="auto">
  <name>Task 2: MoveMenu + intégration BattleScene</name>
  <files>src/SDK.MonoGame/UI/MoveMenu.cs, src/SDK.MonoGame/Scenes/BattleScene.cs</files>
  <action>
    Créer `src/SDK.MonoGame/UI/MoveMenu.cs` dans namespace `SDK.MonoGame.UI` :

    Champs : `_moves IReadOnlyList<BattleMove>`, `_cursorIndex int = 0`, `_prevKeyState KeyboardState`
    Propriété publique : `BattleMove? SelectedMove { get; private set; }`

    Couleurs de type (mapping TypeId → Color) — static Dictionary dans MoveMenu :
    ```
    private static readonly Dictionary<int, Color> TypeColors = new()
    {
        [1]  = Color.Gray,       // Normal
        [2]  = new Color(255,140,0),  // Fire
        [3]  = Color.DodgerBlue, // Water
        [4]  = Color.LimeGreen,  // Grass
        [5]  = Color.Yellow,     // Electric
        [6]  = Color.Orchid,     // Ice (approx)
        [7]  = Color.Sienna,     // Fighting
        [8]  = Color.MediumPurple, // Poison
        [9]  = Color.SandyBrown, // Ground
        [10] = Color.SkyBlue,    // Flying
        [11] = Color.Violet,     // Psychic (approx)
        [12] = Color.OliveDrab,  // Bug
        [13] = Color.SlateGray,  // Rock
        [14] = Color.DarkViolet, // Ghost
        [15] = Color.DarkGray,   // Dragon
        [16] = Color.DimGray,    // Dark
        [17] = Color.Silver,     // Steel
        [18] = Color.Pink,       // Fairy
    };
    ```

    Constructor : `MoveMenu(IReadOnlyList<BattleMove> moves) => _moves = moves;`

    `Update(KeyboardState ks)` :
    ```
    if (ks.IsKeyDown(Keys.Up)   && !_prevKeyState.IsKeyDown(Keys.Up))
        _cursorIndex = (_cursorIndex - 1 + _moves.Count) % _moves.Count;
    if (ks.IsKeyDown(Keys.Down) && !_prevKeyState.IsKeyDown(Keys.Down))
        _cursorIndex = (_cursorIndex + 1) % _moves.Count;
    if (ks.IsKeyDown(Keys.Z)    && !_prevKeyState.IsKeyDown(Keys.Z))
        SelectedMove = _moves[_cursorIndex];
    _prevKeyState = ks;
    ```

    `ResetSelection() => SelectedMove = null;`

    `Draw(SpriteBatch sb, Vector2 origin, SpriteFont? font)` :
    Pour i in 0.._moves.Count :
    - Rectangle de fond : 110x12 à (origin.X, origin.Y + i*14), couleur TypeColors[move.TypeId] * 0.6f ou Gray si absent
    - DrawString : `(i == _cursorIndex ? "▶ " : "  ") + move.Identifier` à (origin.X + 2, origin.Y + i*14 + 1), Color.White, scale 0.6f

    Dans `BattleScene.cs`, ajouter intégration :
    - Champ : `private MoveMenu? _moveMenu;`
    - Champ : `private StatusIcon _statusIcon;` — instancié dans constructor
    - Dans `LoadBattle(BattleState state)` : `_moveMenu = new MoveMenu(state.Player.Moves);`
    - Dans `Update()` branche `SelectMove` : appeler `_moveMenu?.Update(Keyboard.GetState())`; si `_moveMenu?.SelectedMove != null` → `SetPlayerMove(_moveMenu.SelectedMove); _moveMenu.ResetSelection();`
    - Dans `Draw()` : appeler `_moveMenu?.Draw(sb, new Vector2(5, 200), _font)` si phase = SelectMove
    - Dans `Draw()` : appeler `_statusIcon.Draw(sb, _state.Player.Status, new Vector2(20, 150), _font)` et `_statusIcon.Draw(sb, _state.Opponent.Status, new Vector2(160, 20), _font)`

    Ajouter `using Microsoft.Xna.Framework.Input;` et `using SDK.MonoGame.UI;` dans BattleScene.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs && dotnet test tests/SDK.MonoGame.Tests/ -- 3/3 verts</verify>
  <done>AC-2, AC-3, AC-4 satisfaits : MoveMenu + StatusIcon intégrés dans BattleScene</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.MonoGame/UI/HpBar.cs` — inchangé depuis Plan 12-03
- `src/SDK.MonoGame/Scenes/WorldScene.cs` — inchangé
- `src/SDK.MonoGame/Rendering/RenderPipeline.cs` — inchangé
- D-14 : référentiel 480x270 — toutes positions dans ce plan

## SCOPE LIMITS
- Wipe transition N'EST PAS dans ce plan (Plan 12-05)
- PP moves (épuisement) non géré dans ce plan — hors scope BTLUI-01
- Sons SFX de sélection non inclus (Phase 18 SFX-01)
- Pas de mapping complet TypeId→Color requis — les 18 types suffisent, TypeId inconnu → Color.Gray

</boundaries>

<verification>
Avant de déclarer ce plan complet :
- [ ] `dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj` — 0 erreurs, 0 warnings
- [ ] `dotnet test tests/SDK.MonoGame.Tests/` — 3/3 smoke tests toujours verts
- [ ] `dotnet build PokemonSDK.slnx` — solution entière compile
- [ ] MoveMenu.SelectedMove est null par défaut, set après appui Z
- [ ] StatusIcon.Draw() no-op pour StatusConditionId.None
</verification>

<success_criteria>
- StatusIcon : 5 statuts colorés, None = no-op
- MoveMenu : Up/Down/Z navigation, wraparound, SelectedMove signal
- BattleScene : StatusIcon + MoveMenu intégrés dans Draw/Update
</success_criteria>

<output>
Après complétion, créer `.paul/phases/12-battlescene-ui/12-04-SUMMARY.md`
</output>

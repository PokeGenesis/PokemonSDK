---
phase: 12-battlescene-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SDK.MonoGame/Scenes/IGameScene.cs
  - src/SDK.MonoGame/Scenes/WorldScene.cs
  - src/SDK.MonoGame/Game1.cs
autonomous: true
---

<objective>
## Goal
Introduire `IGameScene` et `WorldScene`, refactorer `Game1` pour piloter une scène courante via `_currentScene` et `SwitchToScene()` — sans rompre les smoke tests headless existants.

## Purpose
BattleScene (Plan 12-03) doit être activable par un appel `SwitchToScene(battleScene)`. Sans cette abstraction, Game1 ne peut pas changer de contexte de rendu. Le refactor isole `WorldSystem`/`PlayerSystem` dans `WorldScene` et libère Game1 pour orchestrer plusieurs scènes.

## Output
- Nouveau répertoire : `src/SDK.MonoGame/Scenes/`
- Nouveaux fichiers : `IGameScene.cs`, `WorldScene.cs`
- Modifié : `Game1.cs` (remplace champs directs par `_currentScene`)
- `HeadlessSmokeTests` toujours verts (HeadlessRunner n'utilise pas Game1)
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/SDK.MonoGame/Game1.cs
@src/SDK.MonoGame/HeadlessRunner.cs
@tests/SDK.MonoGame.Tests/HeadlessSmokeTests.cs
</context>

<acceptance_criteria>

## AC-1: IGameScene interface définit le contrat Update/Draw
```gherkin
Given un nouveau fichier IGameScene.cs dans SDK.MonoGame.Scenes
When une classe implémente IGameScene
Then elle doit fournir Update(GameTime) et Draw(SpriteBatch, GameTime)
And l'interface est dans le namespace SDK.MonoGame.Scenes
```

## AC-2: WorldScene encapsule WorldSystem + PlayerSystem
```gherkin
Given WorldScene reçoit WorldSystem et PlayerSystem via DI constructor injection
When WorldScene.Update(GameTime) est appelé
Then world.Update(delta) et player.Update() sont appelés dans cet ordre
When WorldScene.Draw(SpriteBatch, GameTime) est appelé
Then la scène délègue au RenderPipeline existant (stub no-op acceptable dans ce plan)
```

## AC-3: Game1 utilise IGameScene _currentScene, SwitchToScene() fonctionne
```gherkin
Given Game1 refactoré avec _currentScene et SwitchToScene(IGameScene)
When Initialize() est appelé
Then _currentScene est assigné à une WorldScene instanciée depuis DI
When SwitchToScene(next) est appelé
Then _currentScene devient next au prochain Update()
```

## AC-4: HeadlessSmokeTests passent sans modification
```gherkin
Given HeadlessRunner.Run(sp, frames) appelle directement WorldSystem et PlayerSystem
When dotnet test tests/SDK.MonoGame.Tests
Then les 3 smoke tests passent (aucune dépendance à Game1 ni IGameScene)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: IGameScene interface + WorldScene wrapper</name>
  <files>src/SDK.MonoGame/Scenes/IGameScene.cs, src/SDK.MonoGame/Scenes/WorldScene.cs</files>
  <action>
    Créer `src/SDK.MonoGame/Scenes/IGameScene.cs` :
    ```
    namespace SDK.MonoGame.Scenes;
    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;

    public interface IGameScene
    {
        void Update(GameTime gameTime);
        void Draw(SpriteBatch spriteBatch, GameTime gameTime);
    }
    ```

    Créer `src/SDK.MonoGame/Scenes/WorldScene.cs` :
    - Namespace : `SDK.MonoGame.Scenes`
    - Constructor : `(WorldSystem world, PlayerSystem player)` — injecté depuis DI
    - `Update(GameTime gt)` : appelle `_world.Update(gt.ElapsedGameTime)` puis `_player.Update()`
    - `Draw(SpriteBatch sb, GameTime gt)` : stub no-op pour ce plan (le vrai rendu tilemap reste dans Plan 12-05 scope; le RenderPipeline est géré par Game1.Draw)

    Ajouter usings nécessaires : `SDK.MonoGame.World` pour WorldSystem/PlayerSystem.
    Ne pas injecter RenderPipeline dans WorldScene — Game1 reste owner du pipeline.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs</verify>
  <done>AC-1 et AC-2 satisfaits : IGameScene + WorldScene compilent</done>
</task>

<task type="auto">
  <name>Task 2: Game1 refactor vers _currentScene + SwitchToScene()</name>
  <files>src/SDK.MonoGame/Game1.cs</files>
  <action>
    Refactorer `Game1.cs` :

    Ajouter champ : `private IGameScene _currentScene = null!;`
    Ajouter champ : `private IGameScene? _pendingScene;`
    Supprimer champs directs : `private WorldSystem _world` et `private PlayerSystem _player`
    Ajouter using : `using SDK.MonoGame.Scenes;`

    Ajouter méthode publique :
    ```
    public void SwitchToScene(IGameScene next) => _pendingScene = next;
    ```

    Dans `Initialize()` :
    - Supprimer : `_world = _services.GetRequiredService<WorldSystem>();`
    - Supprimer : `_player = _services.GetRequiredService<PlayerSystem>();`
    - Ajouter : `_currentScene = new WorldScene(_services.GetRequiredService<WorldSystem>(), _services.GetRequiredService<PlayerSystem>());`
    - Garder : `_keyboard`, `_saveSystem`, `_scriptEngineFactory`, hotreloader, TextInput — inchangés

    Dans `Update(GameTime gameTime)` :
    - Remplacer `_world.Update(gameTime.ElapsedGameTime); _player.Update();` par `_currentScene.Update(gameTime);`
    - Ajouter AVANT base.Update() : `if (_pendingScene is not null) { _currentScene = _pendingScene; _pendingScene = null; }`
    - Garder bloc #if DEBUG (LuaConsole) inchangé

    Dans `Draw(GameTime gameTime)` :
    - Appeler `_currentScene.Draw(_spriteBatch, gameTime);` ENTRE BeginScene() et EndScene()
    - Remplacer le commentaire stub `// Plan 03-04+` par l'appel réel
    - Garder LuaErrorOverlay + LuaConsole Draw inchangés

    IMPORTANT : WorldSystem et PlayerSystem restent enregistrés dans le DI container (ils sont injectés dans WorldScene via _services.GetRequiredService) — HeadlessRunner les résout encore directement.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs && dotnet test tests/SDK.MonoGame.Tests/ -- 3/3 verts</verify>
  <done>AC-3 et AC-4 satisfaits : Game1 utilise IGameScene, smoke tests passent</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.MonoGame/Rendering/RenderPipeline.cs` — inchangé
- `src/SDK.MonoGame/HeadlessRunner.cs` — inchangé (ne dépend pas de Game1)
- `tests/SDK.MonoGame.Tests/HeadlessSmokeTests.cs` — inchangé
- `src/SDK.MonoGame/UI/` — DialogueBox, LuaConsole, LuaErrorOverlay inchangés
- D-14 : résolution 480×270 → xBR ×4 → 1920×1080 figée, RenderPipeline inchangée

## SCOPE LIMITS
- BattleScene N'EST PAS créée dans ce plan (c'est Plan 12-03)
- Le rendu effectif de WorldScene (tilemap, sprites) est stub no-op — seul le mécanisme de dispatch existe
- Pas de transition wipe dans ce plan (Plan 12-05)
- `SwitchToScene()` est synchrone via _pendingScene buffer — pas de gestion async

</boundaries>

<verification>
Avant de déclarer ce plan complet :
- [ ] `dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj` — 0 erreurs, 0 warnings
- [ ] `dotnet test tests/SDK.MonoGame.Tests/` — 3/3 smoke tests verts
- [ ] `dotnet build PokemonSDK.slnx` — solution entière compile
- [ ] `Game1._currentScene` non-null après Initialize()
- [ ] Vérifier que `_world` et `_player` fields ont été supprimés de Game1
</verification>

<success_criteria>
- `src/SDK.MonoGame/Scenes/` créé avec IGameScene + WorldScene
- Game1 délègue Update/Draw à `_currentScene`
- `SwitchToScene()` disponible sur Game1
- 3 smoke tests toujours verts
</success_criteria>

<output>
Après complétion, créer `.paul/phases/12-battlescene-ui/12-02-SUMMARY.md`
</output>

---
phase: 12-battlescene-ui
plan: 05
type: execute
wave: 3
depends_on: ["12-03", "12-04"]
files_modified:
  - src/SDK.MonoGame/Scenes/WipeTransition.cs
  - src/SDK.MonoGame/Scenes/WorldScene.cs
  - src/SDK.MonoGame/Scenes/BattleScene.cs
  - src/SDK.MonoGame/UI/BattleEndOverlay.cs
autonomous: false
---

<objective>
## Goal
Implémenter la transition wipe overworld→battle (15 frames noir), l'overlay BattleEnd (victoire/défaite), le retour à WorldScene après combat, et un déclencheur debug (touche B en WorldScene) pour valider le flux complet BTLUI-01.

## Purpose
Sans transition et retour, BattleScene est une île inaccessible. Ce plan ferme la boucle : overworld → wipe → battle (HP bars + moves + statuts) → BattleEnd → retour overworld. C'est le critère de done du requirement BTLUI-01.

## Output
- Nouveaux fichiers : `src/SDK.MonoGame/Scenes/WipeTransition.cs`, `src/SDK.MonoGame/UI/BattleEndOverlay.cs`
- Modifiés : `WorldScene.cs` (trigger debug B + lancement wipe), `BattleScene.cs` (BattleEnd overlay + retour WorldScene)
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/phases/12-battlescene-ui/12-03-SUMMARY.md
@.paul/phases/12-battlescene-ui/12-04-SUMMARY.md

## Source Files
@src/SDK.MonoGame/Scenes/IGameScene.cs
@src/SDK.MonoGame/Scenes/WorldScene.cs
@src/SDK.MonoGame/Scenes/BattleScene.cs
@src/SDK.MonoGame/Game1.cs
@src/SDK.MonoGame/Rendering/RenderPipeline.cs
@tests/SDK.MonoGame.Tests/HeadlessSmokeTests.cs
</context>

<acceptance_criteria>

## AC-1: WipeTransition anime 15 frames vers noir puis appelle OnComplete
```gherkin
Given WipeTransition(duration=15, onComplete=callback)
When WipeTransition.Update() est appelé 15 fois
Then alpha passe de 0f à 1f en 15 frames
And onComplete() est appelé exactement une fois à frame 15
When WipeTransition.Draw(sb) est appelé
Then un rectangle noir semi-transparent couvre tout l'écran (référentiel 480x270)
```

## AC-2: WorldScene déclenche SwitchToBattle sur pression de la touche B (debug)
```gherkin
Given WorldScene.Update() et Game1.SwitchToScene disponible
When la touche B est pressée en WorldScene
Then une WipeTransition est lancée; à son terme, Game1.SwitchToScene(battleScene) est appelé
And BattleScene.LoadBattle(testState) est appelé avec un BattleState de test synthétique
```

## AC-3: BattleScene affiche BattleEndOverlay et retourne à WorldScene
```gherkin
Given BattleScene est en état BattleEnd
When Update() est appelé
Then BattleEndOverlay.Draw() affiche "YOU WIN" ou "YOU LOSE" selon PlayerWon
When le joueur presse A (confirm)
Then Game1.SwitchToScene(worldScene) est appelé pour retourner à l'overworld
```

## AC-4: HeadlessSmokeTests passent sans modification
```gherkin
Given les modifications de WorldScene et BattleScene
When dotnet test tests/SDK.MonoGame.Tests
Then 3/3 smoke tests passent (HeadlessRunner ne passe pas par WorldScene.Update)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: WipeTransition + BattleEndOverlay</name>
  <files>src/SDK.MonoGame/Scenes/WipeTransition.cs, src/SDK.MonoGame/UI/BattleEndOverlay.cs</files>
  <action>
    Créer `src/SDK.MonoGame/Scenes/WipeTransition.cs` :
    ```
    namespace SDK.MonoGame.Scenes;
    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;

    public sealed class WipeTransition
    {
        private readonly int _duration;
        private readonly Action _onComplete;
        private readonly Texture2D _pixel;
        private int _frame;
        private bool _done;

        public bool IsComplete => _done;

        public WipeTransition(GraphicsDevice gd, int duration, Action onComplete)
        {
            _duration = duration;
            _onComplete = onComplete;
            _pixel = new Texture2D(gd, 1, 1);
            _pixel.SetData(new[] { Color.White });
        }

        public void Update()
        {
            if (_done) return;
            _frame++;
            if (_frame >= _duration) { _done = true; _onComplete(); }
        }

        public void Draw(SpriteBatch sb, int screenW = 480, int screenH = 270)
        {
            float alpha = Math.Min(1f, (float)_frame / _duration);
            sb.Draw(_pixel, new Rectangle(0, 0, screenW, screenH), Color.Black * alpha);
        }

        public void Dispose() => _pixel.Dispose();
    }
    ```

    Créer `src/SDK.MonoGame/UI/BattleEndOverlay.cs` :
    ```
    namespace SDK.MonoGame.UI;
    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;

    public sealed class BattleEndOverlay
    {
        public void Draw(SpriteBatch sb, bool playerWon, SpriteFont? font)
        {
            if (font is null) return;
            var msg = playerWon ? "YOU WIN!" : "YOU LOSE...";
            var pos = new Vector2(160, 110);
            sb.DrawString(font, msg, pos + Vector2.One, Color.Black);
            sb.DrawString(font, msg, pos, playerWon ? Color.LimeGreen : Color.Red);

            sb.DrawString(font, "Press A to continue", new Vector2(130, 130), Color.White,
                0f, Vector2.Zero, 0.6f, SpriteEffects.None, 0f);
        }
    }
    ```
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs</verify>
  <done>AC-1 satisfait : WipeTransition anime 15 frames, BattleEndOverlay compile</done>
</task>

<task type="auto">
  <name>Task 2: WorldScene debug trigger B + BattleScene retour WorldScene</name>
  <files>src/SDK.MonoGame/Scenes/WorldScene.cs, src/SDK.MonoGame/Scenes/BattleScene.cs</files>
  <action>
    Dans `WorldScene.cs` :
    - Ajouter constructor params : `(WorldSystem world, PlayerSystem player, BattleScene battleScene, Game1 game1)`
    - Ajouter champs : `_battleScene`, `_game1`, `_prevKs KeyboardState`, `_wipe WipeTransition?`
    - Dans `Update(GameTime gt)` :
      ```
      var ks = Keyboard.GetState();
      if (ks.IsKeyDown(Keys.B) && !_prevKs.IsKeyDown(Keys.B) && _wipe is null)
      {
          var testState = CreateTestBattleState();
          _wipe = new WipeTransition(_game1.GraphicsDevice, 15, () =>
          {
              _battleScene.LoadBattle(testState);
              _game1.SwitchToScene(_battleScene);
              _wipe = null;
          });
      }
      _wipe?.Update();
      _prevKs = ks;
      // WorldSystem + PlayerSystem update (identique Task 2 Plan 12-02)
      _world.Update(gt.ElapsedGameTime);
      _player.Update();
      ```
    - Dans `Draw(SpriteBatch sb, GameTime gt)` : appeler `_wipe?.Draw(sb)` EN DERNIER (overlay)
    - Méthode privée `CreateTestBattleState()` retourne un `BattleState` synthétique avec 2 BattlePokemon de test (HP=100, moves=[move1], level=5 etc.) — juste assez pour que LoadBattle fonctionne.

    Dans `BattleScene.cs` :
    - Ajouter constructor param : `WorldScene worldScene, Game1 game1` (ou passer via SetReturnScene)
    - Alternative plus propre : méthode `SetContext(WorldScene ws, Game1 g1)` appelée depuis Program.cs après construction DI
    - Ajouter champ `_battleEndOverlay = new BattleEndOverlay()`
    - Dans `Update()` branche `BattleEnd` :
      ```
      var ks = Keyboard.GetState();
      if (ks.IsKeyDown(Keys.A) && !_prevKs.IsKeyDown(Keys.A))
          _game1?.SwitchToScene(_worldScene!);
      _prevKs = ks;
      ```
    - Dans `Draw()` : si phase == BattleEnd, appeler `_battleEndOverlay.Draw(sb, _state.Player.CurrentHp > 0, _font)`

    Mise à jour DI dans Program.cs si WorldScene params ont changé : `services.AddSingleton<WorldScene>()` (le container résout les nouveaux params si enregistrés).
    Appel `battleScene.SetContext(sp.GetRequiredService<WorldScene>(), game1)` dans Program.cs après build.

    Note : `Game1.GraphicsDevice` est accessible si exposé via `public GraphicsDevice GraphicsDevice => base.GraphicsDevice;` dans Game1 — ajouter cette propriété si absente.
  </action>
  <verify>dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -- 0 erreurs && dotnet test tests/SDK.MonoGame.Tests/ -- 3/3 verts</verify>
  <done>AC-2 et AC-3 satisfaits : touche B déclenche battle, A retourne à overworld</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Flux complet BTLUI-01 : overworld → wipe 15f → BattleScene (HP bars + sprites 96x96 placeholders + MoveMenu 4 moves + StatusIcons) → BattleEnd overlay → retour overworld.
  </what-built>
  <how-to-verify>
    1. `dotnet run --project src/SDK.MonoGame -- --configuration Debug`
    2. Overworld s'affiche (fond noir + spawn joueur)
    3. Presser B → wipe noir 15 frames → BattleScene apparaît
    4. Vérifier : HP bar adversaire (haut-gauche), HP bar joueur (bas-droite), 2 rectangles gris 96x96
    5. MoveMenu visible en bas : 4 moves listés avec couleur de type
    6. Up/Down déplace curseur ▶, Z confirme → HP bars changent
    7. Si un Pokémon tombe à 0 HP → "YOU WIN!" ou "YOU LOSE..." + "Press A to continue"
    8. Presser A → retour overworld
    9. `dotnet test tests/SDK.MonoGame.Tests/` → 3/3 verts
  </how-to-verify>
  <resume-signal>Taper "approved" pour continuer, ou décrire les problèmes à corriger</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.MonoGame/Rendering/RenderPipeline.cs` — la wipe est dans la couche IGameScene.Draw(), pas dans RenderPipeline
- `src/SDK.MonoGame/UI/HpBar.cs`, `StatusIcon.cs`, `MoveMenu.cs` — inchangés
- `src/SDK.Battle/BattleEngine.cs` — inchangé
- D-14 : toutes les coords WipeTransition et BattleEndOverlay en pixels 480x270

## SCOPE LIMITS
- Trigger B est DEBUG ONLY — pas un vrai système d'encounter (EncounterSystem → BattleScene wiring est Phase 15+)
- Sprites réels (PNG 96x96 nommés D-16) sont Phase 17
- Audio SFX sur transition absent (Phase 18)
- Animations de HP bar (smooth decrease) hors scope — HP change instantané

</boundaries>

<verification>
Avant de déclarer ce plan complet :
- [ ] `dotnet build PokemonSDK.slnx` — 0 erreurs, 0 warnings
- [ ] `dotnet test tests/SDK.MonoGame.Tests/` — 3/3 smoke tests verts
- [ ] `dotnet run --project src/SDK.MonoGame` — démarre sans exception
- [ ] Touche B → wipe → BattleScene (observé visuellement)
- [ ] Move sélectionnée → HP change → BattleEnd overlay apparaît
- [ ] Touche A en BattleEnd → retour overworld
- [ ] checkpoint:human-verify approuvé
</verification>

<success_criteria>
- WipeTransition 15 frames fonctionne visuellement
- BattleEndOverlay "YOU WIN/LOSE" + retour overworld via A
- Debug trigger B déclenche le flux complet
- BTLUI-01 : HP bars + sprites placeholder + MoveMenu + StatusIcon = DONE
- 3 smoke tests verts
</success_criteria>

<output>
Après complétion, créer `.paul/phases/12-battlescene-ui/12-05-SUMMARY.md`
</output>

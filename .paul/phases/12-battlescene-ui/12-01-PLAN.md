---
phase: 12-battlescene-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/SDK.Core/Enums/StatusConditionId.cs
  - src/SDK.Core/ValueObjects/BattlePokemon.cs
  - src/SDK.Core/Interfaces/IBattleEngine.cs
  - src/SDK.Battle/BattleEngine.cs
  - tests/SDK.Battle.Tests/BattleEngineTests.cs
autonomous: true
---

<objective>
## Goal
Ajouter `StatusConditionId` enum, `BattlePokemon.Status`, extraire `RunTurn()` de `BattleEngine` et étendre `IBattleEngine` — fondation que BattleScene utilisera pour exécuter les tours visuellement.

## Purpose
BattleScene (Plan 12-03) a besoin de : (1) contrôler tour par tour via `RunTurn()`, (2) afficher l'état de statut via `Status`, (3) demander à l'IA adverse sa move via `SelectOpponentMove()`. Ces 3 APIs doivent exister dans `IBattleEngine` avant tout travail MonoGame.

## Output
- Nouveau fichier : `src/SDK.Core/Enums/StatusConditionId.cs`
- Modifié : `BattlePokemon` (+1 param default), `IBattleEngine` (+2 méthodes), `BattleEngine` (RunTurn extrait)
- 6 nouveaux tests dans `BattleEngineTests.cs`
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/SDK.Core/ValueObjects/BattlePokemon.cs
@src/SDK.Core/Interfaces/IBattleEngine.cs
@src/SDK.Battle/BattleEngine.cs
@tests/SDK.Battle.Tests/BattleEngineTests.cs
@tests/SDK.Battle.Tests/Helpers/BattleTestHelpers.cs
</context>

<acceptance_criteria>

## AC-1: StatusConditionId enum existe et BattlePokemon compile sans breaking change
```gherkin
Given BattlePokemon.cs avant modification a 13 params positionnels sans Status
When on ajoute StatusConditionId? Status = null comme 14e param (dernier, avec default)
Then tous les appels BattlePokemon existants compilent sans modification
And MakePokemon() dans BattleTestHelpers compile sans modification
```

## AC-2: IBattleEngine expose RunTurn() et SelectOpponentMove()
```gherkin
Given IBattleEngine n'a que RunBattle()
When on ajoute RunTurn(BattleState, BattleMove, BattleMove) et SelectOpponentMove(BattleState)
Then BattleEngine implémente les deux sans erreur de compilation
And RunBattle() délègue à RunTurn() en interne (régression : comportement identique)
```

## AC-3: RunTurn() contient ApplyBeforeMove(x2) + speed sort + ApplyMove(x2) + Turn++
```gherkin
Given un BattleState avec deux Pokémon vivants
When RunTurn(state, playerMove, opponentMove) est appelé
Then ApplyBeforeMove est appelé pour player ET opponent avant tout ApplyMove
And le Pokémon le plus rapide attaque en premier
And state.Turn est incrémenté de 1
And NotifyTurnStart/End sont appelés
```

## AC-4: 6 tests RunTurn() passent (incluant régression RunBattle)
```gherkin
Given les 6 nouveaux tests dans BattleEngineTests
When dotnet test tests/SDK.Battle.Tests
Then tous les tests passent (anciens + nouveaux)
And aucun warning de compilation n'est introduit
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: StatusConditionId enum + BattlePokemon.Status</name>
  <files>src/SDK.Core/Enums/StatusConditionId.cs, src/SDK.Core/ValueObjects/BattlePokemon.cs</files>
  <action>
    Créer `src/SDK.Core/Enums/StatusConditionId.cs` :
    ```
    namespace SDK.Core.Enums;
    public enum StatusConditionId { None = 0, Sleep = 1, Freeze = 2, Burn = 3, Poison = 4, Paralysis = 5 }
    ```

    Dans `src/SDK.Core/ValueObjects/BattlePokemon.cs`, ajouter `StatusConditionId? Status = null` comme DERNIER param positionnel (après `IReadOnlyList<BattleMove> Moves`) avec default :
    ```
    public sealed record BattlePokemon(
        int SpeciesId, string Nickname, int Level,
        int CurrentHp, int MaxHp,
        int Attack, int Defense, int SpecialAttack, int SpecialDefense, int Speed,
        int Type1Id, int? Type2Id,
        IReadOnlyList<BattleMove> Moves,
        StatusConditionId? Status = null);
    ```

    Ajouter `using SDK.Core.Enums;` si absent.
    Ne pas modifier BattleTestHelpers — l'appel existant `new BattlePokemon(1, "TestMon", 50, ...)` doit compiler sans changement grâce au default.
  </action>
  <verify>dotnet build src/SDK.Core/SDK.Core.csproj -- 0 erreurs, 0 warnings</verify>
  <done>AC-1 satisfait : BattlePokemon compile, StatusConditionId enum accessible</done>
</task>

<task type="auto">
  <name>Task 2: IBattleEngine + BattleEngine.RunTurn() + SelectOpponentMove()</name>
  <files>src/SDK.Core/Interfaces/IBattleEngine.cs, src/SDK.Battle/BattleEngine.cs</files>
  <action>
    Dans `IBattleEngine.cs`, ajouter deux méthodes (ajouter `using SDK.Core.ValueObjects;` si nécessaire) :
    ```
    BattleState RunTurn(BattleState state, BattleMove playerMove, BattleMove opponentMove);
    BattleMove SelectOpponentMove(BattleState state);
    ```

    Dans `BattleEngine.cs`, extraire `RunTurn()` depuis `RunBattle()` :

    Nouveau `public BattleState RunTurn(BattleState state, BattleMove playerMove, BattleMove opponentMove)` contient, dans cet ordre EXACT :
    1. `_plugins.NotifyTurnStart(state);`
    2. `state = _plugins.ApplyBeforeMove(state, new BattleAction(playerMove.MoveId, true));`
    3. `state = _plugins.ApplyBeforeMove(state, new BattleAction(opponentMove.MoveId, false));`
    4. Speed sort (playerFirst bool, identique au code actuel lignes 71-72)
    5. ApplyMove conditionnel (identique lignes 74-86 actuelles)
    6. `state = state with { Turn = state.Turn + 1 };`
    7. `_plugins.NotifyTurnEnd(state);`
    8. `return state;`

    Nouveau `public BattleMove SelectOpponentMove(BattleState state)` :
    ```
    public BattleMove SelectOpponentMove(BattleState state) =>
        _opponentStrategy.SelectMove(state.Opponent, state.Player, state.Config);
    ```

    Refactorer `RunBattle()` loop : supprimer le bloc NotifyTurnStart..NotifyTurnEnd actuel, remplacer par :
    ```
    var playerMove = _playerStrategy.SelectMove(state.Player, state.Opponent, state.Config);
    var opponentMove = _opponentStrategy.SelectMove(state.Opponent, state.Player, state.Config);
    state = RunTurn(state, playerMove, opponentMove);
    ```

    Garder NotifyBattleStart, les HP checks, NotifyFainted, NotifyBattleEnd — ils restent dans RunBattle().
  </action>
  <verify>dotnet build src/SDK.Battle/SDK.Battle.csproj -- 0 erreurs</verify>
  <done>AC-2 et AC-3 satisfaits : IBattleEngine étendue, RunTurn extrait avec ApplyBeforeMove inclus</done>
</task>

<task type="auto">
  <name>Task 3: 6 tests RunTurn() + régression RunBattle()</name>
  <files>tests/SDK.Battle.Tests/BattleEngineTests.cs</files>
  <action>
    Ajouter 6 tests APRÈS les tests existants dans `BattleEngineTests` :

    Test 1 — `RunTurn_ReducesOpponentHp_WhenPlayerFirst` :
    Setup player speed > opponent speed, damage=50. Appeler `engine.RunTurn(state, playerMove, opponentMove)`.
    Assert : `result.Opponent.CurrentHp == initialOpponentHp - 50`.

    Test 2 — `RunTurn_ReducesPlayerHp_WhenOpponentFirst` :
    Setup opponent speed > player speed, damage=50. Appeler `engine.RunTurn(state, playerMove, opponentMove)`.
    Assert : `result.Player.CurrentHp == initialPlayerHp - 50`.

    Test 3 — `RunTurn_IncrementsTurnCounter` :
    BattleState avec Turn=5. Appeler `engine.RunTurn(state, playerMove, opponentMove)`.
    Assert : `result.Turn == 6`.

    Test 4 — `RunTurn_CallsNotifyTurnStart_ThenNotifyTurnEnd` :
    Mock PluginRegistry via IBattlePlugin mock, vérifier NotifyTurnStart appelé avant NotifyTurnEnd.
    Alternative : utiliser une `PluginRegistry` réelle avec un `Mock<IBattlePlugin>` enregistré.
    Assert : `plugin.Verify(p => p.OnTurnStart(It.IsAny<BattleState>()), Times.Once)`.
    Assert : `plugin.Verify(p => p.OnTurnEnd(It.IsAny<BattleState>()), Times.Once)`.

    Test 5 — `RunTurn_CallsApplyBeforeMove_ForBothActions` :
    Mock IBattlePlugin, track OnBeforeMove calls.
    Assert : OnBeforeMove appelé 2 fois (player + opponent actions).

    Test 6 — `RunBattle_StillWorks_AfterRunTurnRefactor` (régression) :
    Identique au test `Player_Wins_When_Opponent_HP_Reaches_Zero` existant. Vérifier `result.PlayerWon == true`.

    Pour créer le BattleState dans les tests RunTurn :
    ```
    var state = new BattleState(player, opponent, 5, WeatherType.None,
        BattleTestHelpers.NoCritConfig(), Array.Empty<string>());
    ```

    Pour la PluginRegistry avec mock plugin dans tests 4+5 :
    ```
    var plugin = new Mock<IBattlePlugin>();
    var registry = new PluginRegistry();
    registry.Register(plugin.Object);
    var engine = new BattleEngine(formula.Object, playerStrat.Object, opponentStrat.Object, chart.Object, registry);
    ```
  </action>
  <verify>dotnet test tests/SDK.Battle.Tests/ -- tous les tests passent (anciens 6 + nouveaux 6)</verify>
  <done>AC-4 satisfait : 12+ tests verts, 0 warnings</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Core/SDK.Core.csproj` — zéro NuGet externe (D-01, règle fondamentale)
- `src/SDK.Battle/Formulas/`, `src/SDK.Battle/Difficulty/` — pas de modification dans ce plan
- Tests existants dans `BattleEngineTests.cs` — ajouter seulement, jamais modifier
- `BattleState` record — pas de modification dans ce plan
- `tests/SDK.Battle.Tests/Helpers/BattleTestHelpers.cs` — pas de modification (backward-compat validé)

## SCOPE LIMITS
- Ce plan ne touche pas SDK.MonoGame (UI, Game1, scènes)
- Pas d'affichage visuel dans ce plan
- `SelectOpponentMove` est sur l'interface IBattleEngine mais ne gère pas encore la sélection visuelle joueur
- `StatusConditionId.Status` est ajouté à BattlePokemon mais aucune logique d'application de statut (brûlure/poison/sommeil) n'est implémentée dans ce plan

</boundaries>

<verification>
Avant de déclarer ce plan complet :
- [ ] `dotnet build src/SDK.Core` — 0 erreurs, 0 warnings
- [ ] `dotnet build src/SDK.Battle` — 0 erreurs, 0 warnings
- [ ] `dotnet test tests/SDK.Battle.Tests` — tous les tests passent (12+ expected)
- [ ] `dotnet build PokemonSDK.slnx` — solution entière compile
- [ ] BattlePokemon sans Status param compile (backward compat)
</verification>

<success_criteria>
- StatusConditionId enum dans SDK.Core.Enums
- BattlePokemon.Status = null default, 0 breaking changes
- IBattleEngine expose RunTurn + SelectOpponentMove
- BattleEngine.RunTurn inclut ApplyBeforeMove(x2) avant ApplyMove(x2)
- 6 nouveaux tests verts + 0 régression
</success_criteria>

<output>
Après complétion, créer `.paul/phases/12-battlescene-ui/12-01-SUMMARY.md`
</output>

---
phase: 08-nuget-distribution
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - src/SDK.Tools/SDK.Tools.csproj
  - tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj
autonomous: true
---

<objective>
## Goal

`dotnet list package --vulnerable` retourne 0 vulnérabilités. Décision FluentAssertions v8 documentée et close.

## Purpose

Avant toute publication NuGet, `dotnet list package --vulnerable` montrant HIGH sévérité (GHSA-2cmq-823j-5qj8) est un signal négatif fort — même si SDK.Tools n'est pas un package distribué. Les deux CVEs (high + moderate) dans SixLabors.ImageSharp 2.1.9 doivent être résolus pour crédibiliser le repo GitHub public.

FluentAssertions v8.x est une question de licence ouverte depuis Plan 01-01 (⚠️). Elle doit être tranchée et documentée avant que la Phase 8 progresse vers la publication.

## Output

- `src/SDK.Tools/SDK.Tools.csproj` : SixLabors.ImageSharp passé à une version sans CVE
- Décision FluentAssertions v8 documentée dans STATE.md, ⚠️ fermé
- `dotnet list package --vulnerable` → 0 warnings HIGH/MODERATE
- `dotnet test` toujours au vert (166 tests)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Packages concernés

**SixLabors.ImageSharp (HIGH CVE)**
- Fichier : `src/SDK.Tools/SDK.Tools.csproj`
- Version actuelle : `2.1.9`
- CVE-1 : GHSA-2cmq-823j-5qj8 (High) — https://github.com/advisories/GHSA-2cmq-823j-5qj8
- CVE-2 : GHSA-rxmq-m78w-7wmc (Moderate) — https://github.com/advisories/GHSA-rxmq-m78w-7wmc
- Impact : `SDK.Tools.Tests` reçoit le warning par référence projet (pas une dépendance directe)
- D-25 : SixLabors.ImageSharp dans SDK.Tools (Apache 2.0 2.1.9). D-25 autorise un upgrade de version.

**FluentAssertions (licence ⚠️)**
- Projets : 8 projets tests, 7 à v8.10.0, 1 (SDK.Tools.Tests) à v8.4.0
- Licence v8.x : Xceed Commercial License — GRATUIT pour projets open-source
- PokemonSDK = MIT open-source → FluentAssertions v8 libre pour nous
- FA est test-only — ne transite JAMAIS vers les consommateurs NuGet des 7 packages
- Décision à documenter : v8.x acceptable + standardiser à v8.10.0

## Décisions figées à respecter
- D-17 : SDK.Tools sans MonoGame — rester headless
- D-25 : SixLabors.ImageSharp dans SDK.Tools uniquement — pas d'ImageSharp dans les packages distribués
- D-18 : NuGet SemVer strict — pas de breaking change sans bump majeur (non affecté par ce plan)
</context>

<acceptance_criteria>

## AC-1 : SixLabors.ImageSharp sans CVE

```gherkin
Given SDK.Tools/SDK.Tools.csproj référence SixLabors.ImageSharp 2.1.9 (2 CVEs actifs)
When on met à jour la version vers la version minimale résolvant GHSA-2cmq-823j-5qj8 et GHSA-rxmq-m78w-7wmc
Then `dotnet list package --vulnerable` retourne 0 warnings NU1902/NU1903 pour ImageSharp
  And `dotnet build PokemonSDK.slnx` retourne 0 erreurs
  And `dotnet test tests/SDK.Tools.Tests/` retourne 0 tests échoués
```

## AC-2 : Décision FluentAssertions documentée, ⚠️ fermé

```gherkin
Given FluentAssertions v8.x est présent dans 8 projets tests (⚠️ deferred depuis Plan 01-01)
  And SDK.Tools.Tests utilise v8.4.0 au lieu de v8.10.0 (désynchronisé)
When on audite la licence v8 pour PokemonSDK (open source MIT)
  And on standardise SDK.Tools.Tests de v8.4.0 → v8.10.0
Then la décision est documentée dans STATE.md : "FA v8 libre pour open-source, v8.10.0 standardisé"
  And le ⚠️ deferred issue est fermé dans STATE.md
  And `dotnet test` toujours 0 échecs après bump de version
```

## AC-3 : Bilan vulnérabilités clean

```gherkin
Given les deux corrections appliquées
When on exécute `dotnet list package --vulnerable --include-transitive`
Then output contient "no vulnerable packages" pour tous les projets
  And exit code = 0 (aucun warning de restauration)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T1: ImageSharp — upgrade vers version sans CVE</name>
  <files>
    src/SDK.Tools/SDK.Tools.csproj
  </files>
  <action>
    **Étape 1 : Identifier la version cible**

    ```bash
    # Trouver la version minimale corrigeant les deux CVEs
    # GHSA-2cmq-823j-5qj8 et GHSA-rxmq-m78w-7wmc sont corrigées en 3.x+
    # v3.x = "Six Labors Split License" : gratuit pour open-source (MIT), commercial = payant
    # PokemonSDK = MIT → licence 3.x gratuite pour nous
    # SDK.Tools = Exe non packagé → licence ne transite pas vers les consommateurs NuGet
    # v4.x = même modèle Split License → aussi acceptable, mais préférer 3.1.x (LTS plus stable)

    # Mettre à jour vers la version stable la plus récente de la branche 3.x :
    dotnet add src/SDK.Tools/SDK.Tools.csproj package SixLabors.ImageSharp --version 3.1.7
    # Si 3.1.7 n'est pas disponible, utiliser la dernière 3.x stable :
    # dotnet add src/SDK.Tools/SDK.Tools.csproj package SixLabors.ImageSharp
    # puis épingler la version exacte résolue dans le .csproj
    ```

    **Étape 2 : Vérifier que l'API utilisée est compatible 3.x**

    Les APIs ImageSharp utilisées dans SDK.Tools (AtlasPacker) :
    - `Image<Rgba32>` — stable 2.x→3.x
    - `Image.Load()` — stable 2.x→3.x
    - `image.SaveAsPng()` — stable 2.x→3.x
    - `image.Mutate(ctx => ctx.DrawImage(...))` — stable 2.x→3.x

    Si une API est renommée en 3.x, adapter l'appel correspondant dans `src/SDK.Tools/`.

    **Ne pas modifier :** SDK.Tools.Tests.csproj (ImageSharp est une référence transitive via la référence projet SDK.Tools, pas une dépendance directe du projet test).
  </action>
  <verify>
    ```bash
    dotnet build PokemonSDK.slnx   # 0 erreurs
    dotnet test tests/SDK.Tools.Tests/ --no-build  # tous verts
    dotnet list package --vulnerable   # 0 warnings NU1902/NU1903 pour ImageSharp
    ```
  </verify>
  <done>AC-1 satisfait : SixLabors.ImageSharp sans CVE, build 0 erreur, tests SDK.Tools.Tests au vert.</done>
</task>

<task type="auto">
  <name>T2: FluentAssertions — audit licence + standardisation v8.10.0</name>
  <files>
    tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj,
    .paul/STATE.md
  </files>
  <action>
    **Étape 1 : Confirmer la licence FA v8 pour PokemonSDK**

    - FluentAssertions v8 = Xceed Commercial License
    - La licence Xceed stipule : GRATUIT pour logiciels open-source publiés sous licence OSI approuvée
    - PokemonSDK = MIT (licence OSI approuvée) → **FA v8 gratuit pour nous**
    - FA est test-only : jamais dans les 7 packages NuGet distribués (pas de `<PackageReference>` dans les .csproj packables)
    - Conclusion : Pas besoin de passer à v7.x. Rester en v8.x.

    **Étape 2 : Standardiser SDK.Tools.Tests sur v8.10.0**

    SDK.Tools.Tests est le seul projet à v8.4.0 ; les 7 autres projets tests sont à v8.10.0.
    Mettre à jour pour homogénéiser :

    ```bash
    dotnet add tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj package FluentAssertions --version 8.10.0
    ```

    **Étape 3 : Fermer le deferred issue dans STATE.md**

    Dans STATE.md, section `### Deferred Issues`, mettre à jour la ligne FluentAssertions :

    ```
    | FluentAssertions v8 licence Xceed (commercial) | Plan 01-01 | Avant Phase 8 | ✅ Résolu — FA v8 gratuit pour open-source MIT. v8.10.0 standardisé sur tous les 8 projets tests. FA test-only, ne transite pas vers les consumers NuGet. |
    ```

    Ajouter dans STATE.md section `### Decisions` (Décisions émergentes Phase 8) :

    ```
    Décisions émergentes (Plan 08-02 — licence/CVE) :
    - FluentAssertions v8.10.0 retenu — Xceed License gratuite pour projets open-source MIT. Test-only, 0 impact consommateurs NuGet.
    - SixLabors.ImageSharp 3.x — Six Labors Split License gratuite pour open-source. D-25 confirmé (SDK.Tools uniquement). CVEs GHSA-2cmq-823j-5qj8 + GHSA-rxmq-m78w-7wmc résolus.
    ```
  </action>
  <verify>
    ```bash
    dotnet test --no-build   # 0 échecs, toujours 166 tests verts
    grep "FluentAssertions" tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj  # v8.10.0
    grep "8.4.0" tests/ -r  # 0 résultats (version obsolète éliminée)
    ```
  </verify>
  <done>AC-2 satisfait : FluentAssertions v8.10.0 standardisé. Deferred issue ⚠️ fermé dans STATE.md.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Core/` `src/SDK.Battle/` `src/SDK.Scripting/` `src/SDK.Data/` — projets packables non concernés
- `src/SDK.MonoGame/` — runtime MonoGame hors scope
- `src/plugins/` — plugins non concernés
- Logique métier dans `src/SDK.Tools/` (AtlasPacker, SqliteSyncer, SpriteValidator, CLI) — seul le .csproj est modifié
- `Directory.Build.props` — ne pas ajouter de version ImageSharp/FA ici (maintenir opt-in par projet)

## SCOPE LIMITS
- 08-02 = CVE ImageSharp + audit FA + standardisation version
- `publish-nuget.yml` completion → plan 08-03
- Local NuGet feed smoke test → plan 08-04
- Ne pas passer FA à v7.x — décision documentée v8 OK pour open-source
- Ne pas upgrader d'autres packages (MonoGame, EF Core, MoonSharp) — hors scope phase 8

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet list package --vulnerable` → 0 warnings NU1902/NU1903
- [ ] `dotnet build PokemonSDK.slnx` → 0 erreurs, 0 nouveaux warnings
- [ ] `dotnet test` → 166 tests verts (même count qu'avant)
- [ ] `grep "2.1.9" src/SDK.Tools/SDK.Tools.csproj` → 0 résultats (version obsolète absente)
- [ ] `grep "8.4.0" tests/ -r` → 0 résultats (version obsolète absente)
- [ ] Deferred issue FluentAssertions = ✅ dans STATE.md
- [ ] Décisions ImageSharp 3.x + FA v8.10.0 documentées dans STATE.md
</verification>

<success_criteria>
- AC-1 : ImageSharp sans CVE — `dotnet list package --vulnerable` = 0 HIGH/MODERATE
- AC-2 : FA v8.10.0 standardisé — decision documentée, ⚠️ fermé
- AC-3 : `dotnet list package --vulnerable --include-transitive` → clean
- Build 0 erreur, 166 tests verts après les deux modifications
</success_criteria>

<output>
Après completion, créer `.paul/phases/08-nuget-distribution/08-02-SUMMARY.md`
</output>

---
phase: 05-plugins-characters
plan: 03
type: tdd
wave: 1
depends_on: ["05-02"]
files_modified:
  - src/SDK.Core/Entities/Character.cs
  - src/SDK.Core/Entities/VillainGroup.cs
  - src/SDK.Core/Entities/VillainMember.cs
  - src/SDK.Data/PokemonDbContext.cs
  - src/SDK.Data/Configurations/CharacterConfiguration.cs
  - src/SDK.Data/Configurations/VillainGroupConfiguration.cs
  - src/SDK.Data/Configurations/VillainMemberConfiguration.cs
  - src/SDK.Data/Seeding/CharacterDataSeeder.cs
  - src/SDK.Data/Seeding/DataSeeder.cs
  - tests/SDK.Data.Tests/CharacterDataTests.cs
autonomous: true
---

<objective>
## Goal
Ajouter les entités Character, VillainGroup et VillainMember dans SDK.Core, les configurer via EF Core (migration AddCharacterData), les seeder avec 6 locales obligatoires par D-22, et valider via CharacterTranslationsD22Tests.

## Purpose
Satisfaire CHAR-01→03 (personnages, rivaux, antagonistes). Clôture le dernier deferred issue D-22 Phase 5 dans STATE.md. Complète la Phase 5 Plugins + Characters.

## Output
- 3 entités SDK.Core.Entities : Character, VillainGroup, VillainMember
- 3 fichiers Configuration EF Core (ApplyConfigurationsFromAssembly)
- Migration EF Core `AddCharacterData` (tables characters, villain_groups, villain_members)
- CharacterDataSeeder : 5 Characters × 6 locales + 1 VillainGroup × 6 locales + 2 VillainMembers
- CharacterDataSeeder.SeedAll() branché dans DataSeeder.SeedAll()
- Tests : CharacterTranslationsD22Tests + intégration CRUD (≥ 6 nouveaux tests)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/05-plugins-characters/05-02-SUMMARY.md

## Patterns Établis (à suivre exactement)

### Entités SDK.Core (pattern Badge / Trainer)
- Namespace `SDK.Core.Entities`
- `Id` int, `Identifier` string = `string.Empty`, `Generation` int (D-09)
- PAS de colonnes name_* — translations table (D-07)
- Navigations nullable init à `null!` ou `new List<>()`

### Configurations EF (pattern BadgeConfiguration)
- `IEntityTypeConfiguration<T>` dans `SDK.Data.Configurations`
- `ApplyConfigurationsFromAssembly` les auto-découvre — aucune registration manuelle
- Table snake_case, HasKey, IsRequired, HasMaxLength, HasIndex, HasOne/WithMany/HasForeignKey

### Seeders (pattern DataSeeder / ProgressionDataSeeder)
- Namespace `SDK.Data.Seeding`, class `static`
- Guard `if (ctx.X.Any()) return;` en tête de chaque méthode
- Translations : tuple array `(id, en, es, fr, de, it, ja)` + nested foreach
- EntityType PascalCase : "Character" / "VillainGroup"
- D-22 : exactement 6 locales en/es/fr/de/it/ja, jamais moins

### DbContext
- Expression-bodied DbSet : `public DbSet<X> Xs => Set<X>();`
- Enregistrement via `ApplyConfigurationsFromAssembly` — pas de `modelBuilder.Entity<X>()` inline

### Tests SDK.Data.Tests
- SqliteTestFixture :memory: déjà disponible dans le projet
- `<Using Include="Xunit" />` déjà dans SDK.Data.Tests.csproj — ne pas redéclarer
</context>

<acceptance_criteria>

## AC-1: Entités SDK.Core sans dépendance NuGet
```gherkin
Given SDK.Core.csproj sans aucune dépendance NuGet externe
When Character, VillainGroup, VillainMember sont ajoutés dans SDK.Core/Entities/
Then dotnet list src/SDK.Core/SDK.Core.csproj package retourne vide (D-01 intact)
```

## AC-2: Migration EF Core créée et appliquée
```gherkin
Given PokemonDbContext étendu + 3 fichiers Configuration ajoutés
When dotnet ef migrations add AddCharacterData --project src/SDK.Data --startup-project src/SDK.Data
Then migration générée sans erreur
And dotnet ef database update réussit
And tables characters / villain_groups / villain_members existent dans la DB
```

## AC-3: CharacterTranslationsD22Tests — D-22 obligatoire
```gherkin
Given CharacterDataSeeder.SeedAll() exécuté sur SqliteTestFixture :memory:
When on requête translations GROUP BY entity_id WHERE entity_type = 'Character'
Then chaque Character a exactement 6 rows (locales : en, es, fr, de, it, ja)
And chaque VillainGroup a exactement 6 rows
And les 6 locales obligatoires sont toutes présentes pour entity_id=1
```

## AC-4: Build + suite complète verts
```gherkin
Given dotnet build PokemonSDK.slnx
When dotnet test PokemonSDK.slnx
Then 0 Error(s) build + ≥ 125 tests verts (116 existants + ≥ 9 nouveaux), 0 failed
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T0: Entités Character, VillainGroup, VillainMember dans SDK.Core</name>
  <files>
    src/SDK.Core/Entities/Character.cs,
    src/SDK.Core/Entities/VillainGroup.cs,
    src/SDK.Core/Entities/VillainMember.cs
  </files>
  <action>
    Créer src/SDK.Core/Entities/Character.cs :
    ```csharp
    namespace SDK.Core.Entities;
    public class Character
    {
        public int Id { get; set; }
        public string Identifier { get; set; } = string.Empty;  // "ash-ketchum", "gary-oak"
        public string Role { get; set; } = string.Empty;        // "Rival", "GymLeader", "Champion", "Antagonist", "NPC"
        public int Generation { get; set; }                      // D-09
        public ICollection<VillainMember> VillainMemberships { get; set; } = new List<VillainMember>();
        // PAS de colonne Name — D-07 : translations table
    }
    ```

    Créer src/SDK.Core/Entities/VillainGroup.cs :
    ```csharp
    namespace SDK.Core.Entities;
    public class VillainGroup
    {
        public int Id { get; set; }
        public string Identifier { get; set; } = string.Empty;  // "team-rocket"
        public int Generation { get; set; }                      // D-09
        public ICollection<VillainMember> Members { get; set; } = new List<VillainMember>();
        // PAS de colonne Name — D-07
    }
    ```

    Créer src/SDK.Core/Entities/VillainMember.cs :
    ```csharp
    namespace SDK.Core.Entities;
    public class VillainMember
    {
        public int Id { get; set; }
        public int CharacterId { get; set; }
        public Character Character { get; set; } = null!;
        public int VillainGroupId { get; set; }
        public VillainGroup VillainGroup { get; set; } = null!;
        // Pas de Generation propre — hérite via Character
        // Pas de colonne Name — identité via Character.Identifier
    }
    ```

    NE PAS ajouter de NuGet à SDK.Core.csproj.
    NE PAS créer de colonne name_fr / name_en (D-07).
    Character.Role : string libre, pas enum — évite breaking change futur.
  </action>
  <verify>
    dotnet build src/SDK.Core/SDK.Core.csproj → Build succeeded
    dotnet list src/SDK.Core/SDK.Core.csproj package → (vide)
  </verify>
  <done>AC-1 satisfait : 3 entités créées, SDK.Core.csproj sans NuGet</done>
</task>

<task type="auto">
  <name>T1: Configurations EF + DbContext + Migration + CharacterDataSeeder</name>
  <files>
    src/SDK.Data/Configurations/CharacterConfiguration.cs,
    src/SDK.Data/Configurations/VillainGroupConfiguration.cs,
    src/SDK.Data/Configurations/VillainMemberConfiguration.cs,
    src/SDK.Data/PokemonDbContext.cs,
    src/SDK.Data/Seeding/CharacterDataSeeder.cs,
    src/SDK.Data/Seeding/DataSeeder.cs
  </files>
  <action>
    ### Configurations EF (pattern BadgeConfiguration)

    CharacterConfiguration.cs :
    - Table "characters", HasKey(Id)
    - Identifier : IsRequired, HasMaxLength(100), HasIndex unique
    - Role : IsRequired, HasMaxLength(50)
    - Generation : IsRequired
    - HasMany(VillainMemberships).WithOne(m => m.Character).HasForeignKey(m => m.CharacterId)

    VillainGroupConfiguration.cs :
    - Table "villain_groups", HasKey(Id)
    - Identifier : IsRequired, HasMaxLength(100), HasIndex unique
    - Generation : IsRequired
    - HasMany(Members).WithOne(m => m.VillainGroup).HasForeignKey(m => m.VillainGroupId)

    VillainMemberConfiguration.cs :
    - Table "villain_members", HasKey(Id)
    - CharacterId FK → characters (already configured via CharacterConfiguration)
    - VillainGroupId FK → villain_groups (already configured via VillainGroupConfiguration)
    - Ne pas re-déclarer les FK dans les deux directions — EF détecte via les deux configurations

    ### PokemonDbContext.cs — ajouter 3 DbSets après Badge :
    ```csharp
    public DbSet<Character> Characters => Set<Character>();
    public DbSet<VillainGroup> VillainGroups => Set<VillainGroup>();
    public DbSet<VillainMember> VillainMembers => Set<VillainMember>();
    ```

    ### Migration
    ```bash
    dotnet ef migrations add AddCharacterData --project src/SDK.Data --startup-project src/SDK.Data
    dotnet ef database update --project src/SDK.Data --startup-project src/SDK.Data
    ```

    ### CharacterDataSeeder.cs (namespace SDK.Data.Seeding)
    Seed 5 Characters (identifiants + rôles) :
    - id=1, "ash-ketchum",  Role="Rival",      Generation=1
    - id=2, "gary-oak",     Role="Rival",      Generation=1
    - id=3, "red",          Role="Champion",   Generation=1
    - id=4, "jessie",       Role="Antagonist", Generation=1
    - id=5, "james",        Role="Antagonist", Generation=1

    Translations Character (EntityType="Character") — 5 × 6 = 30 rows :
    ```
    (1, "Ash Ketchum",  "Ash Ketchum",  "Sacha Ketchum", "Ash Ketchum",  "Ash Ketchum",  "サトシ"),
    (2, "Gary Oak",     "Gary Oak",     "Pierre Carpin",  "Gary Oak",     "Gary Oak",     "シゲル"),
    (3, "Red",          "Rojo",         "Rouge",          "Rot",          "Rosso",        "レッド"),
    (4, "Jessie",       "Jessie",       "Jessie",         "Jessie",       "Jessie",       "ムサシ"),
    (5, "James",        "James",        "James",          "Jessie",       "James",        "コジロウ"),
    ```
    (pour James DE : utiliser "James" — copie autorisée pour noms propres)

    Seed 1 VillainGroup :
    - id=1, "team-rocket", Generation=1
    Translations VillainGroup (EntityType="VillainGroup") — 1 × 6 = 6 rows :
    ```
    (1, "Team Rocket", "Equipo Rocket", "Team Rocket", "Team Rocket", "Team Rocket", "ロケット団"),
    ```

    Seed 2 VillainMembers :
    - id=1, CharacterId=4 (Jessie), VillainGroupId=1
    - id=2, CharacterId=5 (James),  VillainGroupId=1

    Méthode publique `SeedAll(PokemonDbContext ctx)` qui appelle :
    - SeedCharacters(ctx), SeedCharacterTranslations(ctx)
    - SeedVillainGroups(ctx), SeedVillainGroupTranslations(ctx)
    - SeedVillainMembers(ctx)

    ### DataSeeder.cs — ajouter dans SeedAll() après ProgressionDataSeeder.SeedAll(ctx) :
    ```csharp
    CharacterDataSeeder.SeedAll(ctx);
    ```
  </action>
  <verify>
    dotnet ef database update --project src/SDK.Data --startup-project src/SDK.Data → "Done."
    dotnet build PokemonSDK.slnx → Build succeeded, 0 Error(s)
  </verify>
  <done>AC-2 satisfait : migration appliquée, tables créées, seeder intégré</done>
</task>

<task type="auto">
  <name>T2: Tests CharacterTranslationsD22Tests + intégration CRUD</name>
  <files>
    tests/SDK.Data.Tests/CharacterDataTests.cs
  </files>
  <action>
    Créer tests/SDK.Data.Tests/CharacterDataTests.cs dans le projet existant SDK.Data.Tests.

    Classe `CharacterTranslationsD22Tests` (xUnit, SqliteTestFixture :memory:) :
    - `AllCharacters_HaveExactlySixTranslations` :
      seed → COUNT(DISTINCT locale) GROUP BY entity_id WHERE entity_type='Character'
      → tous les groupes = 6. FluentAssertions : `counts.Should().AllSatisfy(c => c.Should().Be(6))`
    - `AllVillainGroups_HaveExactlySixTranslations` :
      idem pour entity_type='VillainGroup'
    - `AllSixLocales_PresentForCharacter_Ash` :
      WHERE entity_type='Character' AND entity_id=1 → sélectionner locales
      → Should().Contain(new[] { "en","es","fr","de","it","ja" })

    Classe `CharacterCrudTests` :
    - `Character_CanBeCreatedAndQueried` : ajouter un Character :memory:, SaveChanges, requêter → trouvé
    - `VillainMember_LinksCharacterToVillainGroup` : créer Character + VillainGroup + VillainMember → Include navigation → vérifié
    - `Character_Identifier_IsUnique` : insérer deux Character avec même Identifier → DbUpdateException attendue

    Pattern SqliteTestFixture à utiliser :
    ```csharp
    using var ctx = fixture.CreateContext();
    CharacterDataSeeder.SeedAll(ctx);
    // then query ctx.Characters, ctx.Translations, etc.
    ```

    Ne pas re-déclarer `using Xunit;` — déjà dans les ImplicitUsings du .csproj.
    Utiliser `using FluentAssertions;` et `using SDK.Core.Entities;`.
  </action>
  <verify>
    dotnet test tests/SDK.Data.Tests/ → ≥ 6 nouveaux tests verts + 0 régressions
    CharacterTranslationsD22Tests passe (AC-3 confirmé)
  </verify>
  <done>AC-3 satisfait : D-22 Characters validé, deferred issue résolu</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Core/Interfaces/` — contrats plugin (05-01/05-02, stabilisés)
- `src/plugins/` — plugins concrets (05-02, ne pas toucher)
- `src/SDK.Battle/` — battle engine Phase 2, stabilisé
- `src/SDK.Scripting/` — scripting Phase 4, stabilisé
- `src/SDK.Data/Migrations/` — jamais modifier une migration passée (001→004)
- `src/SDK.Data/Seeding/ProgressionDataSeeder.cs` — Trainers déjà seedés (brock, misty, etc.) — Character est une entité séparée, pas de doublon

## SCOPE LIMITS
- Pas de Lua API pour les personnages (Phase 6+ si besoin)
- Pas d'interactions en jeu (dialogue, GameState Character) — Phase 6
- Pas de stats de progression pour les rivaux — déféré Phase 6 (CHAR-02 partial)
- Pas de MonoGame sprite rendering (Phase 7)
- VillainMember = lien FK uniquement, aucune stat propre
- Character.Role = string libre — pas d'enum dans SDK.Core pour l'instant

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet list src/SDK.Core/SDK.Core.csproj package` → vide (D-01 intact)
- [ ] `dotnet ef database update` → "Done."
- [ ] `dotnet test tests/SDK.Data.Tests/` → CharacterTranslationsD22Tests : 3/3 verts
- [ ] `dotnet build PokemonSDK.slnx` → 0 Error(s), 0 Warning(s)
- [ ] `dotnet test PokemonSDK.slnx` → ≥ 125 tests, 0 failed (AC-4)
- [ ] Deferred issue D-22 Characters → marqué ✅ dans STATE.md
</verification>

<success_criteria>
- Character, VillainGroup, VillainMember dans SDK.Core.Entities
- SDK.Core.csproj toujours sans NuGet (D-01)
- Migration AddCharacterData appliquée, tables créées
- CharacterDataSeeder : 5 Characters + 1 VillainGroup, 6 locales chacun (D-22)
- CharacterTranslationsD22Tests : 3 tests verts
- 0 régression sur 116 tests existants
- Deferred issue D-22 Characters → ✅ résolu
</success_criteria>

<output>
Après complétion, créer `.paul/phases/05-plugins-characters/05-03-SUMMARY.md`
</output>

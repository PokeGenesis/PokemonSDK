---
phase: 07-developer-experience
plan: 03
type: tdd
wave: 3
depends_on: []
files_modified:
  - src/SDK.Core/Interfaces/IScriptEngine.cs
  - src/SDK.Scripting/Engine/LuaScriptEngine.cs
  - src/SDK.Scripting/HotReload/LuaHotReloader.cs
  - src/SDK.MonoGame/UI/LuaErrorOverlay.cs
  - src/SDK.MonoGame/Game1.cs
  - tests/SDK.Scripting.Tests/LuaScriptEngineReloadTests.cs
  - tests/SDK.Scripting.Tests/LuaHotReloaderTests.cs
autonomous: true
---

<objective>
## Goal
Implémenter le hot reload Lua en mode Debug : `IScriptEngine.Reload(path)`, `LuaHotReloader` (FileSystemWatcher, `#if DEBUG`) et `LuaErrorOverlay` (état d'erreur + stub Draw) câblés dans Game1.

## Purpose
Les game devs qui utilisent le SDK modifient souvent des scripts Lua à chaud. Sans hot reload, chaque modification oblige à relancer le jeu. Avec `LuaHotReloader`, enregistrer un `.lua` suffit — le script est rechargé en <500ms sans redémarrage. `LuaErrorOverlay` capture les erreurs de reload pour ne pas faire planter silencieusement.

## Output
- `IScriptEngine.Reload(path)` — nouveau contrat dans SDK.Core
- `LuaScriptEngine.Reload(path)` — réinitialise le script + recharge le fichier
- `LuaHotReloader` — FileSystemWatcher sur `*.lua`, appelle `Reload`, expose `OnReloadError` event
- `LuaErrorOverlay` — état `HasError`/`LastError`/`SetError`/`ClearError`/`Draw()` stub
- Game1 — `#if DEBUG` wiring : instancie HotReloader, souscrit OnReloadError → overlay.SetError
- 6+ tests nouveaux (SDK.Scripting.Tests) — 160+ total
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files Clés
@src/SDK.Core/Interfaces/IScriptEngine.cs
@src/SDK.Scripting/Engine/LuaScriptEngine.cs
@src/SDK.MonoGame/Game1.cs
@src/SDK.MonoGame/UI/DialogueBox.cs
@src/SDK.MonoGame/Program.cs

## Décisions critiques
- D-04 : MoonSharp Preset_SoftSandbox — ne jamais changer le preset lors du Reload
- D-06 : Game1.cs ne touche JAMAIS LuaScriptEngine directement — passe par Func<IScriptEngine> factory
- D-17 : SDK.Tools sans MonoGame — LuaHotReloader va dans SDK.Scripting (pas MonoGame)
- DialogueBox pattern : Draw() stub no-op jusqu'à SpriteFont/MGCB (Plan 07-04)
</context>

<acceptance_criteria>

## AC-1 : IScriptEngine.Reload + LuaScriptEngine clean reload
```gherkin
Given un LuaScriptEngine avec une variable Lua définie ("x = 42")
When Reload(path) est appelé avec un nouveau fichier Lua ("y = 99")
Then un nouveau Script SoftSandbox est créé, "x" n'est plus accessible, le nouveau fichier est chargé
```

## AC-2 : LuaHotReloader déclenche Reload sur changement de fichier .lua
```gherkin
Given LuaHotReloader construit avec un répertoire temporaire et un IScriptEngine mocké
When un fichier .lua dans ce répertoire est modifié (écrit)
Then IScriptEngine.Reload(path) est appelé avec le chemin absolu du fichier
```

## AC-3 : LuaHotReloader — OnReloadError fire si Reload lève une exception
```gherkin
Given LuaHotReloader avec un IScriptEngine mocké qui lève Exception("syntax error")
When un .lua change
Then OnReloadError event est déclenché avec le chemin + le message d'erreur, LastError est peuplé
```

## AC-4 : Build Debug propre, Game1 câblé, tous les tests passent
```gherkin
Given dotnet build PokemonSDK.slnx (configuration Debug)
When le build se termine
Then 0 erreurs, 0 warnings nouveaux, dotnet test retourne 0 failed, total ≥ 160 tests
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T0 : IScriptEngine.Reload + LuaScriptEngine + tests reload</name>
  <files>
    src/SDK.Core/Interfaces/IScriptEngine.cs,
    src/SDK.Scripting/Engine/LuaScriptEngine.cs,
    tests/SDK.Scripting.Tests/LuaScriptEngineReloadTests.cs
  </files>
  <action>
    1. `IScriptEngine` : ajouter `void Reload(string path);` à l'interface existante.

    2. `LuaScriptEngine.Reload(string path)` :
       - Créer un nouveau `Script(CoreModules.Preset_SoftSandbox)` (réinitialisation complète)
       - Assigner au champ `_script` existant (le champ doit devenir non-readonly)
       - Appeler `_script.DoFile(path)` sur le nouveau script
       - Garder D-04 : Preset_SoftSandbox inchangé

    3. Tests dans `tests/SDK.Scripting.Tests/LuaScriptEngineReloadTests.cs` :
       - **Test 1** — Reload_ClearsOldState : créer un .lua temp avec `x = 42`, Execute("x=42"), puis Reload avec un 2e fichier `y=99`, vérifier Evaluate<double>("y") == 99, Evaluate<object>("x") == null (ou lève exception — les deux sont OK, vérifier que le nouveau état est chargé)
       - **Test 2** — Reload_NonExistentPath_Throws : Reload("nonexistent.lua") → lève ScriptRuntimeException ou FileNotFoundException

    Éviter : modifier l'interface d'une façon qui casse NpcInteractionRunner ou les tests existants (ils utilisent Execute/RegisterApi, pas Reload).
  </action>
  <verify>dotnet test tests/SDK.Scripting.Tests/ --filter "FullyQualifiedName~LuaScriptEngineReload" — 2 tests verts</verify>
  <done>AC-1 satisfait : Reload crée un nouveau contexte SoftSandbox propre, charge le fichier</done>
</task>

<task type="auto">
  <name>T1 : LuaHotReloader (#if DEBUG) + tests</name>
  <files>
    src/SDK.Scripting/HotReload/LuaHotReloader.cs,
    tests/SDK.Scripting.Tests/LuaHotReloaderTests.cs
  </files>
  <action>
    Créer `src/SDK.Scripting/HotReload/LuaHotReloader.cs` :

    ```
    #if DEBUG
    namespace SDK.Scripting.HotReload;

    using SDK.Core.Interfaces;

    public sealed class LuaHotReloader : IDisposable
    {
        private readonly IScriptEngine _engine;
        private readonly FileSystemWatcher _watcher;
        public event Action<string, string>? OnReloadError;
        public string? LastError { get; private set; }

        public LuaHotReloader(string directory, IScriptEngine engine) {
            _engine  = engine;
            _watcher = new FileSystemWatcher(directory, "*.lua") {
                NotifyFilter            = NotifyFilters.LastWrite,
                EnableRaisingEvents     = true,
                IncludeSubdirectories   = true,
            };
            _watcher.Changed += OnChanged;
        }

        private void OnChanged(object sender, FileSystemEventArgs e) {
            try   { _engine.Reload(e.FullPath); LastError = null; }
            catch (Exception ex) {
                LastError = ex.Message;
                OnReloadError?.Invoke(e.FullPath, ex.Message);
            }
        }

        public void Dispose() {
            _watcher.Changed -= OnChanged;
            _watcher.Dispose();
        }
    }
    #endif
    ```

    Tests dans `tests/SDK.Scripting.Tests/LuaHotReloaderTests.cs` :
    - Classe wrapper `#if DEBUG` ou condition compile — les tests sont en Debug donc OK
    - Utiliser Moq pour `IScriptEngine`
    - **Test 1** — FileChanged_TriggersReload : écrire un .lua dans temp dir, attendre event FileSystemWatcher (Thread.Sleep(500)), vérifier mock.Verify(e => e.Reload(It.IsAny<string>()), Times.AtLeastOnce())
    - **Test 2** — NonLuaFile_IsIgnored : écrire un .txt dans temp dir, vérifier que Reload n'est PAS appelé
    - **Test 3** — ReloadError_RaisesOnReloadError : mock Reload lève Exception("bad syntax"), écrire .lua, vérifier OnReloadError event reçu avec le message
    - **Test 4** — Dispose_StopsWatcher : Dispose() puis écrire .lua, vérifier Reload non appelé

    Attention FileSystemWatcher sur WSL2 : les événements Changed peuvent se déclencher 2× sur write (créer une fois, modifier une fois). Times.AtLeastOnce() est la bonne assertion (pas Times.Once()).

    Éviter : Thread.Sleep > 600ms (CI timeout), utiliser ManualResetEventSlim pour synchroniser les tests watcher.
  </action>
  <verify>dotnet test tests/SDK.Scripting.Tests/ --filter "FullyQualifiedName~LuaHotReloader" — 4 tests verts</verify>
  <done>AC-2 et AC-3 satisfaits : watcher déclenche Reload, erreurs capturées dans OnReloadError</done>
</task>

<task type="auto">
  <name>T2 : LuaErrorOverlay + Game1 #if DEBUG wiring</name>
  <files>
    src/SDK.MonoGame/UI/LuaErrorOverlay.cs,
    src/SDK.MonoGame/Game1.cs
  </files>
  <action>
    1. Créer `src/SDK.MonoGame/UI/LuaErrorOverlay.cs` (pattern DialogueBox) :

    ```csharp
    namespace SDK.MonoGame.UI;

    public sealed class LuaErrorOverlay
    {
        public bool HasError { get; private set; }
        public string? LastError { get; private set; }

        public void SetError(string message) {
            LastError = message;
            HasError  = true;
            Console.Error.WriteLine($"[LUA HOT RELOAD ERROR] {message}");
        }

        public void ClearError() { HasError = false; LastError = null; }

        // Draw stub — SpriteFont/MGCB déféré Plan 07-04
        public void Draw() { }
    }
    ```

    Pas de `#if DEBUG` sur la classe — la classe est légère, toujours compilée.

    2. Modifier `src/SDK.MonoGame/Game1.cs` :
       - Ajouter `using SDK.Scripting.HotReload;` (sous `#if DEBUG`)
       - Ajouter `using SDK.MonoGame.UI;`
       - Champ `private LuaErrorOverlay _luaErrorOverlay = new();`
       - Sous `#if DEBUG`, champ `private LuaHotReloader? _hotReloader;`
       - Dans `Initialize()`, sous `#if DEBUG` :
         ```
         var engine = _scriptEngineFactory();
         _hotReloader = new LuaHotReloader("Content/Scripts", engine);
         _hotReloader.OnReloadError += (path, msg) => _luaErrorOverlay.SetError($"{System.IO.Path.GetFileName(path)}: {msg}");
         ```
       - Override `protected override void Dispose(bool disposing)` : sous `#if DEBUG`, `_hotReloader?.Dispose()`, appeler `base.Dispose(disposing)`
       - Dans `Draw()`, sous `#if DEBUG` : `_luaErrorOverlay.Draw();`

    IMPORTANT D-06 : le `engine` créé via `_scriptEngineFactory()` dans `#if DEBUG` Initialize est un engine séparé dédié au hot reload — il ne partage PAS le contexte avec les NPC interactions (chaque interaction crée son propre engine via la factory). C'est intentionnel : le hot reload valide le fichier dans un contexte isolé.

    Éviter : référencer `LuaScriptEngine` directement dans Game1 (D-06).
  </action>
  <verify>dotnet build PokemonSDK.slnx --configuration Debug 2>&amp;1 | grep -E "error|warning" | grep -v "0 Error" | head -20 &amp;&amp; echo "Build check done"</verify>
  <done>AC-4 satisfait : build Debug propre, Game1 câblé, LuaErrorOverlay opérationnel</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Core/Interfaces/IScriptEngine.cs` : seul ajout autorisé = `void Reload(string path);`. Ne pas modifier les méthodes existantes.
- `src/SDK.Scripting/Engine/LuaScriptEngine.cs` : D-04 Preset_SoftSandbox INCHANGÉ dans Reload
- `src/SDK.MonoGame/UI/DialogueBox.cs` : ne pas toucher
- Tous les fichiers de migration EF Core — aucune migration dans ce plan
- Tests existants dans `SDK.Scripting.Tests/` (BadgeApiTests, LuaScriptEngineTests, ProdScriptTests) : ne pas modifier

## SCOPE LIMITS
- Pas de rendu réel dans LuaErrorOverlay.Draw() — SpriteFont/MGCB déféré Plan 07-04
- LuaHotReloader : uniquement `#if DEBUG` — zéro code de hot reload en Release
- Pas de nouveau NuGet dans ce plan
- Pas d'intégration avec NpcInteractionRunner — le hot reload utilise un engine isolé
- Pas de console REPL (Plan 07-04)

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx --configuration Debug` → 0 erreurs
- [ ] `dotnet build PokemonSDK.slnx --configuration Release` → 0 erreurs (#if DEBUG correctement gardé)
- [ ] `dotnet test tests/SDK.Scripting.Tests/` → tous verts (anciens + 6+ nouveaux)
- [ ] `dotnet test PokemonSDK.slnx` → 0 failed, total ≥ 160
- [ ] `grep -r "LuaScriptEngine" src/SDK.MonoGame/Game1.cs` → aucun résultat (D-06)
- [ ] `grep "Preset_SoftSandbox" src/SDK.Scripting/Engine/LuaScriptEngine.cs` → présent dans le constructeur ET dans Reload
</verification>

<success_criteria>
- IScriptEngine.Reload(path) contrat ajouté + LuaScriptEngine implémente (2 tests verts)
- LuaHotReloader FileSystemWatcher opérationnel (#if DEBUG, 4 tests verts)
- LuaErrorOverlay état + stub Draw créé
- Game1 câblé en #if DEBUG sans référence directe à LuaScriptEngine (D-06)
- 0 failed tests, total ≥ 160
</success_criteria>

<output>
Après completion, créer `.paul/phases/07-developer-experience/07-03-SUMMARY.md`
</output>

---
phase: 07-developer-experience
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/SDK.Tools/Validation/SpriteEntry.cs
  - src/SDK.Tools/Validation/SpriteValidationResult.cs
  - src/SDK.Tools/Validation/SpriteValidator.cs
  - src/SDK.Tools/Validation/SpriteScanner.cs
  - src/SDK.Tools/Program.cs
  - tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj
  - tests/SDK.Tools.Tests/SpriteValidatorTests.cs
  - PokemonSDK.slnx
autonomous: true
---

<objective>
## Goal

SpriteValidator valide les assets PNG selon la convention D-16 (nommage, taille, canal alpha) via parsing BCL pur — exit code 1 si ERROR, intégrable en CI GitHub Actions sans NuGet ajouté.

## Purpose

DX-01 première brique : les développeurs qui ajoutent des sprites au jeu reçoivent un feedback immédiat sur les erreurs de nommage/taille en CI avant que les assets cassent le jeu à runtime.

## Output

- `src/SDK.Tools/Validation/` : SpriteEntry, SpriteValidationResult, SpriteValidator, SpriteScanner
- `src/SDK.Tools/Program.cs` : commande `asset-validate [path]` (exit code 1 si ERROR)
- `tests/SDK.Tools.Tests/` : projet test + SpriteValidatorTests (≥12 tests)
- SDK.Tools : zéro nouveau NuGet (D-17 intact)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Architecture DX
@.claude/DX.md

## Prior Work
@.paul/phases/05-plugins-characters/05-03-SUMMARY.md

## Source Files
@src/SDK.Tools/Program.cs
@src/SDK.Tools/SDK.Tools.csproj
</context>

<acceptance_criteria>

## AC-1: D-17 intact — zéro NuGet ajouté à SDK.Tools

```gherkin
Given SDK.Tools n'a aucune dépendance NuGet actuellement
When on lit SDK.Tools.csproj après le plan
Then `dotnet list src/SDK.Tools/SDK.Tools.csproj package` retourne une liste vide
  And le parsing PNG utilise FileStream BCL (lecture des 26 premiers bytes du header IHDR)
```

## AC-2: CLI asset-validate avec exit codes corrects

```gherkin
Given un dossier assets/sprites/ contenant des PNG
When `dotnet run --project src/SDK.Tools -- asset-validate assets/sprites/`
Then exit code = 1 si au moins un résultat ERROR présent
  And exit code = 0 si tous les résultats sont OK ou WARN
  And chaque résultat imprime : [ERROR|WARN|OK] chemin_fichier — message
```

## AC-3: Règles de validation D-16 correctes

```gherkin
Given les règles DX.md :
  - Nommage non conforme → WARN (ex: pikachu_front.png au lieu de 00025_pikachu_front.png)
  - Taille incorrecte (front/back ≠ 96×96, overworld ≠ 48×48, portrait ≠ 128×128, icon ≠ 32×32) → ERROR
  - Canal alpha absent (color type PNG ≠ 4 ou 6) → ERROR
  - PNG corrompu (signature invalide) → ERROR
  - Fichier conforme → OK
When SpriteValidator.Validate(filePath) est appelé
Then le SeverityLevel retourné correspond exactement à ces règles
```

## AC-4: SpriteValidatorTests ≥12 tests verts

```gherkin
Given tests/SDK.Tools.Tests/ créé avec SpriteValidatorTests
When `dotnet test tests/SDK.Tools.Tests/`
Then ≥12 tests, 0 failed
  And cas couverts : nommage valide/invalide, taille correcte/incorrecte, alpha présent/absent, PNG corrompu
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T0: Types de validation + SpriteValidator (logique pure)</name>
  <files>
    src/SDK.Tools/Validation/SpriteEntry.cs,
    src/SDK.Tools/Validation/SpriteValidationResult.cs,
    src/SDK.Tools/Validation/SpriteValidator.cs
  </files>
  <action>
    Créer `src/SDK.Tools/Validation/` avec 3 fichiers :

    **SpriteEntry.cs** — record immutable :
    ```csharp
    namespace SDK.Tools.Validation;
    public sealed record SpriteEntry(
        string FilePath,
        string FileName,
        string? DexId,        // null si tile/portrait sans dexid
        string? Identifier,   // null si nommage non D-16
        string? View          // front|back|overworld|portrait|icon|null
    );
    ```

    **SpriteValidationResult.cs** — record immutable :
    ```csharp
    namespace SDK.Tools.Validation;
    public enum SeverityLevel { Ok, Warn, Error }
    public sealed record SpriteValidationResult(
        SpriteEntry Entry,
        SeverityLevel Severity,
        string Message
    );
    ```

    **SpriteValidator.cs** — classe sealed, méthode principale :
    ```csharp
    namespace SDK.Tools.Validation;
    public sealed class SpriteValidator
    {
        // Regex D-16 : {dexid5}_{identifier}_{view}.png
        // DexId = exactement 5 chiffres, identifier = alphanum+underscore, view = front|back|overworld|portrait|icon
        private static readonly System.Text.RegularExpressions.Regex D16Pattern =
            new(@"^(\d{5})_([a-z0-9_]+)_(front|back|overworld|portrait|icon)\.png$",
                System.Text.RegularExpressions.RegexOptions.Compiled);

        private static readonly byte[] PngSignature = [137, 80, 78, 71, 13, 10, 26, 10];

        // Tailles attendues par view (width × height)
        private static readonly Dictionary<string, (int W, int H)> ExpectedSizes = new()
        {
            ["front"]     = (96, 96),
            ["back"]      = (96, 96),
            ["overworld"] = (48, 48),
            ["portrait"]  = (128, 128),
            ["icon"]      = (32, 32),
        };

        public IReadOnlyList<SpriteValidationResult> ValidateAll(IEnumerable<string> filePaths)
            => filePaths.Select(Validate).ToList();

        public SpriteValidationResult Validate(string filePath)
        {
            var fileName = Path.GetFileName(filePath);
            var entry    = ParseEntry(filePath, fileName);

            // 1. Vérifier si le fichier est lisible (PNG signature)
            byte[] header;
            try { header = ReadPngHeader(filePath); }
            catch { return new(entry, SeverityLevel.Error, "PNG corrompu ou illisible"); }

            if (!IsPngSignatureValid(header))
                return new(entry, SeverityLevel.Error, "Signature PNG invalide — fichier corrompu");

            // 2. Nommage D-16 — WARN si non conforme
            if (entry.View is null)
                return new(entry, SeverityLevel.Warn,
                    $"Nommage non conforme D-16 (attendu: {{dexid5}}_{{identifier}}_{{view}}.png)");

            // 3. Canal alpha (color type byte 25 — bits 0..3, bit 2 = alpha)
            // Color type: 0=gray, 2=RGB, 3=palette, 4=gray+alpha, 6=RGBA
            var colorType = header[25];
            bool hasAlpha = colorType == 4 || colorType == 6;
            if (!hasAlpha)
                return new(entry, SeverityLevel.Error, $"Canal alpha absent (color type={colorType}, attendu 4 ou 6)");

            // 4. Taille (width = bytes 16-19 big-endian, height = bytes 20-23)
            int width  = ReadInt32BigEndian(header, 16);
            int height = ReadInt32BigEndian(header, 20);

            if (ExpectedSizes.TryGetValue(entry.View, out var expected))
            {
                if (width != expected.W || height != expected.H)
                    return new(entry, SeverityLevel.Error,
                        $"Taille incorrecte {width}×{height} (attendu {expected.W}×{expected.H} pour view={entry.View})");
            }

            return new(entry, SeverityLevel.Ok, "OK");
        }

        private static SpriteEntry ParseEntry(string filePath, string fileName)
        {
            var m = D16Pattern.Match(fileName);
            if (!m.Success) return new(filePath, fileName, null, null, null);
            return new(filePath, fileName, m.Groups[1].Value, m.Groups[2].Value, m.Groups[3].Value);
        }

        private static byte[] ReadPngHeader(string filePath)
        {
            // Lire les 26 premiers bytes : 8 signature + 4 length + 4 "IHDR" + 4 width + 4 height + 1 bitdepth + 1 colortype
            using var fs = new FileStream(filePath, FileMode.Open, FileAccess.Read);
            var buf = new byte[26];
            int read = fs.Read(buf, 0, 26);
            if (read < 26) throw new InvalidDataException("Fichier trop court");
            return buf;
        }

        private static bool IsPngSignatureValid(byte[] header)
        {
            for (int i = 0; i < PngSignature.Length; i++)
                if (header[i] != PngSignature[i]) return false;
            return true;
        }

        private static int ReadInt32BigEndian(byte[] buf, int offset) =>
            (buf[offset] << 24) | (buf[offset + 1] << 16) | (buf[offset + 2] << 8) | buf[offset + 3];
    }
    ```

    **Eviter :** System.Drawing.Common (NuGet, Linux non supporté), SkiaSharp (NuGet). Uniquement FileStream BCL.
  </action>
  <verify>
    `dotnet build src/SDK.Tools/SDK.Tools.csproj` → Build succeeded, 0 errors.
    `dotnet list src/SDK.Tools/SDK.Tools.csproj package` → liste vide (D-17).
  </verify>
  <done>Types SpriteEntry, SpriteValidationResult, SpriteValidator créés. AC-1 (D-17 intact) et AC-3 (règles validation) pré-satisfaits.</done>
</task>

<task type="auto">
  <name>T1: SpriteScanner + CLI asset-validate dans Program.cs</name>
  <files>
    src/SDK.Tools/Validation/SpriteScanner.cs,
    src/SDK.Tools/Program.cs
  </files>
  <action>
    **SpriteScanner.cs** — découverte récursive PNG :
    ```csharp
    namespace SDK.Tools.Validation;
    public sealed class SpriteScanner
    {
        public IEnumerable<string> Scan(string rootPath)
        {
            if (!Directory.Exists(rootPath))
                throw new DirectoryNotFoundException($"Dossier assets introuvable : {rootPath}");
            return Directory.EnumerateFiles(rootPath, "*.png", SearchOption.AllDirectories);
        }
    }
    ```

    **Program.cs** — ajouter branche `asset-validate` avant le `return 1` de la commande inconnue.
    Conserver exactement le code `seed` existant. Ajouter après le bloc seed :

    ```csharp
    if (args[0] == "asset-validate")
    {
        var assetsPath = args.Length > 1 ? args[1] : "assets/sprites";
        var scanner    = new SDK.Tools.Validation.SpriteScanner();
        var validator  = new SDK.Tools.Validation.SpriteValidator();

        IEnumerable<string> files;
        try { files = scanner.Scan(assetsPath); }
        catch (DirectoryNotFoundException ex)
        {
            Console.Error.WriteLine(ex.Message);
            return 1;
        }

        var results = validator.ValidateAll(files);
        bool hasError = false;

        foreach (var r in results)
        {
            var color = r.Severity switch
            {
                SDK.Tools.Validation.SeverityLevel.Error => ConsoleColor.Red,
                SDK.Tools.Validation.SeverityLevel.Warn  => ConsoleColor.Yellow,
                _                                        => ConsoleColor.Green,
            };
            Console.ForegroundColor = color;
            Console.WriteLine($"[{r.Severity.ToString().ToUpper()}] {r.Entry.FileName} — {r.Message}");
            Console.ResetColor();

            if (r.Severity == SDK.Tools.Validation.SeverityLevel.Error) hasError = true;
        }

        Console.WriteLine($"\nTotal : {results.Count} fichiers analysés.");
        return hasError ? 1 : 0;
    }
    ```

    Mettre à jour le Usage string en haut de Program.cs pour inclure `asset-validate [assets-path]`.

    **Ne pas** modifier le comportement de `seed`. **Ne pas** ajouter de NuGet.
  </action>
  <verify>
    `dotnet build src/SDK.Tools/SDK.Tools.csproj` → Build succeeded.
    `dotnet run --project src/SDK.Tools -- asset-validate` (sans dossier assets) → exit code 1 + message d'erreur.
    `dotnet run --project src/SDK.Tools --` → affiche Usage mis à jour avec asset-validate.
  </verify>
  <done>SpriteScanner et CLI asset-validate opérationnels. AC-2 satisfait.</done>
</task>

<task type="auto">
  <name>T2: tests/SDK.Tools.Tests — SpriteValidatorTests (≥12 tests)</name>
  <files>
    tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj,
    tests/SDK.Tools.Tests/SpriteValidatorTests.cs,
    PokemonSDK.slnx
  </files>
  <action>
    **SDK.Tools.Tests.csproj** — nouveau projet test :
    ```xml
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <IsPackable>false</IsPackable>
      </PropertyGroup>
      <ItemGroup>
        <Using Include="Xunit" />
      </ItemGroup>
      <ItemGroup>
        <PackageReference Include="xunit" Version="2.9.3" />
        <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
          <PrivateAssets>all</PrivateAssets>
        </PackageReference>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
        <PackageReference Include="FluentAssertions" Version="8.4.0" />
      </ItemGroup>
      <ItemGroup>
        <ProjectReference Include="..\..\src\SDK.Tools\SDK.Tools.csproj" />
      </ItemGroup>
    </Project>
    ```

    **SpriteValidatorTests.cs** — tests via PNG synthétiques (bytes construits en mémoire) :

    Les tests NE créent PAS de vrais fichiers PNG sur disque — ils utilisent une méthode helper `BuildPng(int w, int h, byte colorType)` qui construit un byte[] de 26 octets valide (signature + IHDR) et l'écrit dans un fichier temp.

    Classes de tests :
    - `NamingValidationTests` (5 tests) : nom conforme D-16 → OK, pas de dexid → WARN, view manquante → WARN, extension majuscule → WARN, tiles sans dexid → WARN
    - `SizeValidationTests` (5 tests) : front 96×96 → OK, front 64×64 → ERROR, overworld 48×48 → OK, portrait 128×128 → OK, icon 32×32 → OK
    - `AlphaChannelTests` (2 tests) : color type 6 (RGBA) → OK, color type 2 (RGB) → ERROR
    - `CorruptionTests` (2 tests) : signature PNG valide → non-ERROR, signature invalide → ERROR

    Pattern pour les tests avec fichiers temp :
    ```csharp
    private static string CreateTempPng(string fileName, int w, int h, byte colorType, bool validSignature = true)
    {
        var path = Path.Combine(Path.GetTempPath(), fileName);
        var header = new byte[26];
        // PNG signature
        byte[] sig = [137, 80, 78, 71, 13, 10, 26, 10];
        if (validSignature) sig.CopyTo(header, 0);
        // IHDR length = 0x0000000D
        header[8] = 0; header[9] = 0; header[10] = 0; header[11] = 13;
        // "IHDR"
        header[12] = 73; header[13] = 72; header[14] = 68; header[15] = 82;
        // Width big-endian
        header[16] = (byte)(w >> 24); header[17] = (byte)(w >> 16);
        header[18] = (byte)(w >> 8);  header[19] = (byte)w;
        // Height big-endian
        header[20] = (byte)(h >> 24); header[21] = (byte)(h >> 16);
        header[22] = (byte)(h >> 8);  header[23] = (byte)h;
        // Bit depth = 8
        header[24] = 8;
        // Color type
        header[25] = colorType;
        File.WriteAllBytes(path, header);
        return path;
    }
    ```

    **PokemonSDK.slnx** — ajouter le projet test dans le noeud `<Folder Id="/tests/">`.
    Pattern exact des autres projets test dans slnx — ajouter :
    `<Project Path="tests/SDK.Tools.Tests/SDK.Tools.Tests.csproj" />`

    **Eviter :** vrais fichiers PNG téléchargés, dépendances réseau, test écriture DB.
  </action>
  <verify>
    `dotnet build PokemonSDK.slnx` → Build succeeded, 0 errors.
    `dotnet test tests/SDK.Tools.Tests/` → ≥12 tests, 0 failed.
    `dotnet test PokemonSDK.slnx` → tous les tests (≥138 total), 0 failed.
  </verify>
  <done>tests/SDK.Tools.Tests opérationnel. AC-4 satisfait (≥12 tests verts). AC-1 vérifié sur full solution.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Core/` — D-01 zéro NuGet, aucune interface SpriteValidator ici (DX tool ≠ domaine core)
- `src/SDK.Data/` — migrations et configurations non touchées (SqliteSyncer = plan 07-02)
- `src/SDK.Battle/`, `src/SDK.MonoGame/`, `src/SDK.Scripting/` — hors scope
- Logique `seed` existante dans Program.cs — conserver exactement
- Tests existants dans les autres projets — ne pas modifier

## SCOPE LIMITS
- Plan 07-01 = SpriteValidator + SpriteScanner + CLI `asset-validate` uniquement
- AtlasPacker et SqliteSyncer → plan 07-02
- `asset-sync` (flux complet) → plan 07-02
- Hot reload Lua → plan 07-03
- Console REPL Lua → plan 07-04
- SDK.Tools n'acquiert AUCUN nouveau NuGet (D-17) — uniquement BCL FileStream
- `import.json` → plan 07-02
- Aucune dépendance MonoGame dans SDK.Tools

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet list src/SDK.Tools/SDK.Tools.csproj package` → liste vide (D-17)
- [ ] `dotnet build PokemonSDK.slnx` → Build succeeded
- [ ] `dotnet test tests/SDK.Tools.Tests/` → ≥12 tests, 0 failed
- [ ] `dotnet test PokemonSDK.slnx` → ≥138 tests total, 0 failed
- [ ] `dotnet run --project src/SDK.Tools -- asset-validate` avec dossier inexistant → exit code 1
- [ ] `dotnet run --project src/SDK.Tools --` → Usage inclut `asset-validate`
- [ ] Tous les AC (1→4) satisfaits
</verification>

<success_criteria>
- AC-1 : D-17 intact — SDK.Tools.csproj sans NuGet après plan
- AC-2 : CLI asset-validate exit code 0/1 selon présence ERROR
- AC-3 : Règles validation DX.md (naming WARN, size/alpha/corruption ERROR)
- AC-4 : ≥12 tests SpriteValidatorTests, 0 failed
- Solution complète build + test propres
</success_criteria>

<output>
Après completion, créer `.paul/phases/07-developer-experience/07-01-SUMMARY.md`
</output>

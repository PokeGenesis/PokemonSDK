---
phase: 04-scripting-progression
plan: 03
type: tdd
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/SDK.Core/Services/SaveSystem.cs
  - tests/SDK.Core.Tests/SaveSystemTests.cs
  - src/SDK.MonoGame/SDK.MonoGame.csproj
  - src/SDK.MonoGame/Program.cs
  - src/SDK.MonoGame/Game1.cs
  - src/SDK.MonoGame/HeadlessRunner.cs
  - src/SDK.MonoGame/Rendering/RenderPipeline.cs
  - src/SDK.MonoGame/World/WorldSystem.cs
  - src/SDK.MonoGame/UI/DialogueBox.cs
  - src/SDK.MonoGame/Content/Scripts/gym_brock.lua
  - tests/SDK.MonoGame.Tests/DialogueBoxTests.cs
  - tests/SDK.Scripting.Tests/ProdScriptTests.cs
autonomous: true
---

<objective>
## Goal

Implémenter ISaveSystem (SaveSystem JSON), DialogueBox stub, câbler Game1 (ISaveSystem + Func<IScriptEngine> — D-06), et valider avec scripts Lua de production.

## Purpose

Finalise Phase 4 : un script Lua peut attribuer un badge, le GameState est persisté via SaveSystem JSON, Game1 orchestre save/load. La boucle SCRIPT-01→03 est bouclée.

## Output

- `SaveSystem.cs` — implémente ISaveSystem via System.Text.Json
- `DialogueBox.cs` — stub état machine (Open/Close/IsOpen)
- `Program.cs` — enregistre `ISaveSystem` + `Func<IScriptEngine>` factory (D-06)
- `Game1.cs` — reçoit `ISaveSystem` + `Func<IScriptEngine>` via DI (jamais LuaScriptEngine directement)
- `gym_brock.lua` — script prod Gen1 boulder badge
- 7 tests nouveaux (4 Core + 1 MonoGame + 2 Scripting)
</objective>

<context>
## Project Context

@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work

@.paul/phases/04-scripting-progression/04-01-SUMMARY.md
@.paul/phases/04-scripting-progression/04-02-SUMMARY.md

## Source Files

@src/SDK.Core/Interfaces/ISaveSystem.cs
@src/SDK.Core/ValueObjects/GameState.cs
@src/SDK.Scripting/Bindings/NpcInteractionRunner.cs
@src/SDK.MonoGame/Game1.cs
@src/SDK.MonoGame/Program.cs
@src/SDK.MonoGame/SDK.MonoGame.csproj
</context>

<acceptance_criteria>

## AC-1: SaveSystem round-trip JSON

```gherkin
Given un GameState avec PlayerName="Ash", PlaytimeSeconds=3600, flag badge_boulder=true
When SaveSystem.Save(state, path) puis SaveSystem.Load(path)
Then l'état chargé est identique : PlayerName="Ash", PlaytimeSeconds=3600, GetFlag<bool>("badge_boulder") == true
```

## AC-2: SaveSystem — Load retourne null si fichier absent

```gherkin
Given un path vers un fichier inexistant
When SaveSystem.Load(path)
Then retourne null (pas d'exception)
```

## AC-3: D-06 — Game1.cs n'utilise pas SDK.Scripting directement

```gherkin
Given Game1.cs après wiring
When on inspecte les `using` et les types utilisés dans Game1.cs
Then aucun using SDK.Scripting.*, aucun LuaScriptEngine — seulement IScriptEngine (SDK.Core.Interfaces) et ISaveSystem
```

## AC-4: DialogueBox — état machine Open/Close

```gherkin
Given un DialogueBox fraîchement instancié (IsOpen = false)
When Open("Félicitations ! Tu as battu Brock.") est appelé
Then IsOpen == true et CurrentText == "Félicitations ! Tu as battu Brock."
When Close() est appelé
Then IsOpen == false
```

## AC-5: Script prod gym_brock.lua — badge boulder attribué via NpcInteractionRunner

```gherkin
Given un LuaScriptEngine vierge et un GameState sans flags
When NpcInteractionRunner.Run(engine, state, contenu de gym_brock.lua)
Then result.GetFlag<bool>("badge_boulder") == true
```

## AC-6: Suite complète verte

```gherkin
Given la suite après toutes modifications
When dotnet build && dotnet test
Then >= 96 tests passent, 0 failures, 0 régressions
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T1: SaveSystem.cs + SaveSystemTests.cs</name>
  <files>
    src/SDK.Core/Services/SaveSystem.cs,
    tests/SDK.Core.Tests/SaveSystemTests.cs
  </files>
  <action>
    Créer `src/SDK.Core/Services/SaveSystem.cs` :
    - namespace SDK.Core.Services
    - implémente ISaveSystem
    - Save() : JsonSerializer.Serialize(state, new JsonSerializerOptions { WriteIndented = true }) → File.WriteAllText(path, json)
    - Load() : si !File.Exists(path) return null ; sinon JsonSerializer.Deserialize<GameState>(File.ReadAllText(path))
    - AUCUN NuGet externe (System.Text.Json est BCL .NET 10) — SDK.Core.csproj reste vide de dépendances

    Créer `tests/SDK.Core.Tests/SaveSystemTests.cs` avec 4 tests xUnit + FluentAssertions :
    - `Save_Load_RoundTrip_PreservesAllFields` : state avec PlayerName + PlaytimeSeconds + badge_boulder flag, vérifie round-trip complet
    - `Load_ReturnsNull_WhenFileNotFound` : path inexistant → null
    - `Save_Overwrites_PreviousSave` : deux sauvegardes successives sur même path → dernier état chargé
    - `Load_PreservesFlags_D12` : Flags dict avec 3 entrées → toutes présentes après Load

    Chaque test utilise `Path.GetTempFileName()` et supprime le fichier en finally.
    Vérifier : `dotnet test tests/SDK.Core.Tests --no-build` (après build) → 4 nouveaux tests verts
  </action>
  <verify>dotnet build PokemonSDK.slnx && dotnet test tests/SDK.Core.Tests --no-build</verify>
  <done>AC-1 et AC-2 satisfaits : 4 nouveaux tests verts dans SDK.Core.Tests</done>
</task>

<task type="auto">
  <name>T2: Cherry-pick 98c3299 + Game1/Program.cs wiring (D-06)</name>
  <files>
    src/SDK.MonoGame/Game1.cs,
    src/SDK.MonoGame/HeadlessRunner.cs,
    src/SDK.MonoGame/Rendering/RenderPipeline.cs,
    src/SDK.MonoGame/World/WorldSystem.cs,
    src/SDK.MonoGame/SDK.MonoGame.csproj,
    src/SDK.MonoGame/Program.cs
  </files>
  <action>
    ÉTAPE A — Cherry-pick CodeQL fixes :
    `git cherry-pick 98c3299`
    Ce commit corrige 4 fichiers :
    - Game1.cs : `_graphics` → `readonly`
    - HeadlessRunner.cs : supprime `var clock` inutilisé
    - RenderPipeline.cs : `_internalTarget`/`_xbrEffect` → `readonly`, catch ContentLoadException + FileNotFoundException
    - WorldSystem.cs : foreach → `zones.Any(zone => ...)` LINQ

    ÉTAPE B — SDK.MonoGame.csproj : ajouter référence SDK.Scripting :
    ```xml
    <ProjectReference Include="..\SDK.Scripting\SDK.Scripting.csproj" />
    ```
    NOTE D-06 : le csproj PEUT référencer SDK.Scripting. La contrainte D-06 est que Game1.cs n'utilise JAMAIS LuaScriptEngine directement — seulement IScriptEngine (interface de SDK.Core).
    Cf. CLAUDE.md architecture section 3 : `SDK.MonoGame ← SDK.Core + MonoGame + SDK.Battle + SDK.Scripting (via Func factory)`

    ÉTAPE C — Program.cs : enregistrer les services scripting + save :
    Ajouter en tête du fichier : `using SDK.Scripting.Engine;`
    Ajouter dans la ServiceCollection :
    ```csharp
    services.AddSingleton<Func<IScriptEngine>>(_ => () => new LuaScriptEngine());
    services.AddSingleton<ISaveSystem, SaveSystem>();
    ```
    ISaveSystem = SDK.Core.Interfaces.ISaveSystem, SaveSystem = SDK.Core.Services.SaveSystem (déjà accessible via référence SDK.Core existante).
    LuaScriptEngine référencée UNIQUEMENT ici dans Program.cs — jamais dans Game1.cs.

    ÉTAPE D — Game1.cs : ajouter ISaveSystem + Func<IScriptEngine> :
    Ajouter champs :
    ```csharp
    private ISaveSystem _saveSystem = null!;
    private Func<IScriptEngine> _scriptEngineFactory = null!;
    ```
    Dans Initialize() :
    ```csharp
    _saveSystem = _services.GetRequiredService<ISaveSystem>();
    _scriptEngineFactory = _services.GetRequiredService<Func<IScriptEngine>>();
    ```
    INTERDITS dans Game1.cs : `using SDK.Scripting`, `LuaScriptEngine`, tout type de SDK.Scripting.
    Game1.cs utilise uniquement : IScriptEngine (SDK.Core.Interfaces), ISaveSystem (SDK.Core.Interfaces), Func<IScriptEngine>.

    VÉRIFICATION D-06 : après modification, grep Game1.cs pour "LuaScriptEngine" doit retourner vide.
  </action>
  <verify>
    git cherry-pick --no-commit 98c3299 (si pas encore appliqué) ou vérifier que les 4 fichiers ont les fixes.
    grep "LuaScriptEngine" src/SDK.MonoGame/Game1.cs → doit être vide.
    dotnet build PokemonSDK.slnx → 0 erreurs, 0 warnings.
  </verify>
  <done>AC-3 satisfait : build propre, Game1.cs sans SDK.Scripting direct, cherry-pick CodeQL appliqué</done>
</task>

<task type="auto">
  <name>T3: DialogueBox stub + gym_brock.lua + tests</name>
  <files>
    src/SDK.MonoGame/UI/DialogueBox.cs,
    src/SDK.MonoGame/Content/Scripts/gym_brock.lua,
    tests/SDK.MonoGame.Tests/DialogueBoxTests.cs,
    tests/SDK.Scripting.Tests/ProdScriptTests.cs
  </files>
  <action>
    Créer `src/SDK.MonoGame/UI/DialogueBox.cs` :
    ```csharp
    namespace SDK.MonoGame.UI;
    public class DialogueBox {
        public bool IsOpen { get; private set; }
        public string CurrentText { get; private set; } = string.Empty;
        public void Open(string text) { CurrentText = text; IsOpen = true; }
        public void Close() { IsOpen = false; }
        // Draw() : stub — SpriteFont compilation MGCB déférée Phase 7 DX
        public void Draw() { /* no-op : font absente */ }
    }
    ```

    Créer `src/SDK.MonoGame/Content/Scripts/gym_brock.lua` :
    ```lua
    -- Interaction NPC Brock — Arène de Argenta, badge Roc
    badges:AwardBadge('boulder')
    dialogue:Open('Félicitations ! Tu es digne de porter le Badge Roc.')
    ```
    NOTE : le script utilise `badges` ET `dialogue` APIs. Les tests testent uniquement `badges` (NpcInteractionRunner n'enregistre pas `dialogue` dans les tests — le script doit être tolérant ou les tests utilisent une version simplifiée).

    → Créer `src/SDK.MonoGame/Content/Scripts/gym_brock.lua` avec contenu UNIQUEMENT `badges:AwardBadge('boulder')` (pas de dialogue — tolérance Lua sandboxée : appel sur nil lèverait une exception).

    Créer `tests/SDK.MonoGame.Tests/DialogueBoxTests.cs` :
    - `Open_SetsIsOpenTrue_AndCurrentText` : vérifie IsOpen==true et CurrentText après Open()
    - `Close_SetsIsOpenFalse` : vérifie IsOpen==false après Close()

    Créer `tests/SDK.Scripting.Tests/ProdScriptTests.cs` :
    - `GymBrockScript_AwardsBoulderBadge` : inline la chaîne `"badges:AwardBadge('boulder')"`, Run via NpcInteractionRunner, vérifie GetFlag<bool>("badge_boulder") == true
    - `GymBrockScript_IsIdempotent` : deux runs successifs sur même engine → flag toujours true

    Vérifier : `dotnet test PokemonSDK.slnx` → >= 96 tests, 0 failures.
  </action>
  <verify>
    dotnet build PokemonSDK.slnx && dotnet test PokemonSDK.slnx --no-build
    Attendu : >= 96 tests passent (89 existants + 4 SaveSystem + 2 DialogueBox + 2 ProdScript - 1 doublon éventuel = ~97)
    grep "LuaScriptEngine" src/SDK.MonoGame/Game1.cs → vide (contrôle D-06)
  </verify>
  <done>AC-4, AC-5, AC-6 satisfaits : DialogueBox testée, gym_brock.lua valide, suite verte</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `src/SDK.Core/SDK.Core.csproj` — doit rester à ZÉRO dépendances NuGet externes (D-01, D-10)
- `src/SDK.Core/ValueObjects/GameState.cs` — ne pas modifier le contrat (record + Flags + WithFlag/GetFlag)
- `src/SDK.Core/Interfaces/ISaveSystem.cs` — interface déjà posée en Plan 04-01, ne pas modifier
- `src/SDK.Scripting/Bindings/BadgeApi.cs` — stable depuis Plan 04-02
- `src/SDK.Scripting/Bindings/NpcInteractionRunner.cs` — stable depuis Plan 04-02
- `src/SDK.Data/Migrations/` — aucune migration dans ce plan
- Tests existants (89 tests) — zéro régression tolérée

## SCOPE LIMITS

- `DialogueBox.Draw()` est un stub no-op — rendu SpriteFont déféré Phase 7 DX
- SaveSystem ne persiste PAS `IGameClock.GameElapsed` dans ce plan — déféré Phase 5+
- `gym_brock.lua` utilise UNIQUEMENT `badges:AwardBadge()` — pas de `dialogue:Open()` (DialogueBox non câblée au NpcInteractionRunner)
- Pas de sauvegarde automatique in-game (F5 save / F9 load) — câblage clavier déféré Phase 5
- Aucune nouvelle dépendance NuGet

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx` → 0 erreurs, 0 warnings
- [ ] `dotnet test PokemonSDK.slnx` → >= 96 tests, 0 failures
- [ ] `grep "LuaScriptEngine" src/SDK.MonoGame/Game1.cs` → vide (D-06)
- [ ] `dotnet list src/SDK.Core/SDK.Core.csproj package` → vide (zéro NuGet)
- [ ] `git cherry-pick` 98c3299 appliqué : _graphics readonly, HeadlessRunner sans clock inutilisé, RenderPipeline readonly + catch spécifiques, WorldSystem LINQ Any
- [ ] `Content/Scripts/gym_brock.lua` créé
- [ ] Tous les AC-1 → AC-6 vérifiés
</verification>

<success_criteria>
- SaveSystem serialise/désérialise GameState complet (PlayerName + PlaytimeSeconds + Flags) sans perte
- Game1.cs câblé avec ISaveSystem + Func<IScriptEngine> — aucune référence directe à LuaScriptEngine (D-06)
- CodeQL fixes 98c3299 présents dans le branch
- DialogueBox état machine Open/Close testée
- gym_brock.lua attribue le badge boulder via NpcInteractionRunner
- Suite complète >= 96 tests verts, 0 régressions
- SCRIPT-01→03 requirements satisfaits (Phase 4 complète)
</success_criteria>

<output>
After completion, create `.paul/phases/04-scripting-progression/04-03-SUMMARY.md`
</output>

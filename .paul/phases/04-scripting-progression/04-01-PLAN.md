---
phase: 04-scripting-progression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SDK.Core/Interfaces/IScriptEngine.cs
  - src/SDK.Core/Interfaces/ISaveSystem.cs
  - src/SDK.Core/ValueObjects/GameState.cs
  - src/SDK.Scripting/SDK.Scripting.csproj
  - src/SDK.Scripting/Engine/LuaScriptEngine.cs
  - tests/SDK.Scripting.Tests/SDK.Scripting.Tests.csproj
  - tests/SDK.Scripting.Tests/LuaScriptEngineTests.cs
  - PokemonSDK.slnx
autonomous: true
---

<objective>
## Goal
Créer les interfaces SDK.Core (IScriptEngine, ISaveSystem, GameState) et implémenter LuaScriptEngine MoonSharp SoftSandbox dans SDK.Scripting, avec tests xUnit.

## Purpose
Phase 4 livraison SCRIPT-01 : le SDK peut exécuter des scripts Lua sandboxés. GameState (D-12) est le conteneur de flags/badges partagé. IScriptEngine + ISaveSystem dans SDK.Core.Interfaces permettent à SDK.MonoGame de dépendre uniquement des abstractions (D-06).

## Output
- `SDK.Core.Interfaces` : IScriptEngine, ISaveSystem
- `SDK.Core.ValueObjects` : GameState (D-12 : Flags = Dictionary<string, JsonElement>)
- `SDK.Scripting.Engine` : LuaScriptEngine (Preset_SoftSandbox, D-04)
- `tests/SDK.Scripting.Tests` : projet test + 4+ tests couvrant l'engine et GameState
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/SDK.Scripting/SDK.Scripting.csproj
@src/SDK.Core/Interfaces/IBattleEngine.cs
@src/SDK.Core/ValueObjects/BattleConfig.cs
@tests/SDK.Core.Tests/SDK.Core.Tests.csproj
</context>

<acceptance_criteria>

## AC-1: GameState flags round-trip
```gherkin
Given un GameState initialisé vide
When on appelle SetFlag("badge_boulder", true) puis GetFlag<bool>("badge_boulder")
Then la valeur retournée est true
```

## AC-2: LuaScriptEngine execute sans exception
```gherkin
Given un LuaScriptEngine créé avec Preset_SoftSandbox
When on appelle Execute("return 1 + 1")
Then aucune exception n'est levée
```

## AC-3: SoftSandbox — os.exit() bloqué
```gherkin
Given un LuaScriptEngine avec Preset_SoftSandbox
When on exécute un script Lua appelant os.exit(0)
Then une ScriptRuntimeException est levée (process non terminé)
```

## AC-4: Evaluate<T> retourne valeur Lua typée
```gherkin
Given un LuaScriptEngine
When on appelle Evaluate<int>("return 42")
Then le résultat C# est 42
```

## AC-5: SDK.Core sans dépendance NuGet (D-01)
```gherkin
Given SDK.Core.csproj modifié
When on lance dotnet list src/SDK.Core/SDK.Core.csproj package
Then aucun paquet NuGet n'est listé
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : SDK.Core — IScriptEngine + ISaveSystem + GameState</name>
  <files>
    src/SDK.Core/Interfaces/IScriptEngine.cs,
    src/SDK.Core/Interfaces/ISaveSystem.cs,
    src/SDK.Core/ValueObjects/GameState.cs
  </files>
  <action>
    Créer IScriptEngine dans SDK.Core.Interfaces :
    ```
    void Execute(string luaCode);
    T? Evaluate<T>(string luaExpression);
    void RegisterApi(string name, object api);
    void LoadFile(string path);
    ```
    Créer ISaveSystem dans SDK.Core.Interfaces :
    ```
    void Save(GameState state, string path);
    GameState? Load(string path);
    ```
    Créer GameState dans SDK.Core.ValueObjects (record class, pas record struct — mutable via with) :
    - `string PlayerName { get; init; }` = string.Empty
    - `int PlaytimeSeconds { get; init; }` = 0
    - `Dictionary<string, System.Text.Json.JsonElement> Flags { get; init; }` = new() [D-12]
    - Méthode helper `void SetFlag(string key, bool value)` → WRONG, GameState est immuable.
      Donc : méthodes statiques d'extension ou helper NON-mutatifs.
      Pattern correct : `GameState WithFlag(string key, bool value)` retourne un new GameState.
      Et `T? GetFlag<T>(string key)` pour lecture.
    Contraintes : ZÉRO NuGet externe dans SDK.Core — System.Text.Json est dans le BCL .NET 10.
  </action>
  <verify>dotnet build src/SDK.Core && dotnet list src/SDK.Core/SDK.Core.csproj package (doit être vide)</verify>
  <done>AC-5 : SDK.Core compile sans NuGet. IScriptEngine + ISaveSystem + GameState présents.</done>
</task>

<task type="auto">
  <name>Task 2 : SDK.Scripting — MoonSharp + LuaScriptEngine</name>
  <files>
    src/SDK.Scripting/SDK.Scripting.csproj,
    src/SDK.Scripting/Engine/LuaScriptEngine.cs
  </files>
  <action>
    Dans SDK.Scripting.csproj :
    - Ajouter `<PackageReference Include="MoonSharp" Version="2.0.0" />`
    - Le projet référence déjà SDK.Core — ne pas changer
    Créer src/SDK.Scripting/Engine/LuaScriptEngine.cs :
    - Implémente IScriptEngine
    - Constructor : `new Script(CoreModules.Preset_SoftSandbox)` — D-04
    - Execute(string luaCode) : `_script.DoString(luaCode)` (peut lancer ScriptRuntimeException)
    - Evaluate<T>(string luaExpression) : DoString retourne DynValue → `.ToObject<T>()`
    - RegisterApi(string name, object api) : `_script.Globals[name] = api` (UserData.RegisterType si nécessaire)
    - LoadFile(string path) : `_script.DoFile(path)`
    Contraintes :
    - NE PAS utiliser Preset_HardSandbox (trop restrictif pour NPC scripts) ni Preset_Default (non sandboxé)
    - NE PAS ajouter de binding Lua dans ce plan — c'est Plan 04-02
    - NE PAS modifier SDK.MonoGame — wiring dans Plan 04-03
  </action>
  <verify>dotnet build src/SDK.Scripting (0 erreurs, 0 warnings)</verify>
  <done>AC-2 + AC-3 : LuaScriptEngine implémente IScriptEngine, build propre.</done>
</task>

<task type="auto">
  <name>Task 3 : SDK.Scripting.Tests — projet + tests</name>
  <files>
    tests/SDK.Scripting.Tests/SDK.Scripting.Tests.csproj,
    tests/SDK.Scripting.Tests/LuaScriptEngineTests.cs,
    PokemonSDK.slnx
  </files>
  <action>
    Créer tests/SDK.Scripting.Tests/SDK.Scripting.Tests.csproj :
    - Copier la structure de tests/SDK.Core.Tests/SDK.Core.Tests.csproj
    - Ajouter PackageReference Moq 4.20.72 (utile Plan 04-02+)
    - ProjectReference vers src/SDK.Scripting + src/SDK.Core
    - `<Using Include="Xunit" />` OBLIGATOIRE (ImplicitUsings n'inclut pas Xunit)
    Créer LuaScriptEngineTests.cs :
    - Test_Execute_NoException : new LuaScriptEngine(), Execute("return 1+1"), aucune exception
    - Test_Evaluate_ReturnsInt : Evaluate<int>("return 42") == 42
    - Test_OsExit_ThrowsScriptRuntimeException : Execute("os.exit(0)") doit lancer ScriptRuntimeException (MoonSharp.Interpreter namespace)
    - Test_GameState_WithFlag_RoundTrip : new GameState().WithFlag("badge_boulder", true), GetFlag<bool>("badge_boulder") == true
    - Test_GameState_FlagsImmutable : WithFlag retourne NEW GameState (original inchangé)
    Ajouter dans PokemonSDK.slnx, sous Folder "/tests/" :
    `<Project Path="tests/SDK.Scripting.Tests/SDK.Scripting.Tests.csproj" />`
    Contraintes :
    - NE PAS référencer SDK.MonoGame dans ce projet test
    - NE PAS tester SaveSystem ici — Plan 04-03
  </action>
  <verify>dotnet test tests/SDK.Scripting.Tests (N/N vert) && dotnet test (suite complète 77+N vert)</verify>
  <done>AC-1 + AC-2 + AC-3 + AC-4 : tous tests verts. Suite globale régression zéro.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Core/Entities/* (entités DB — ajouts Phase 04-02 uniquement)
- src/SDK.Data/* (migrations — Phase 04-02)
- src/SDK.MonoGame/* (wiring scripting — Phase 04-03)
- src/SDK.Battle/* (moteur combat — stable)
- .github/workflows/ci.yml (CI stable post-Phase 3)

## SCOPE LIMITS
- Pas de binding Lua (GameApi, DialogApi, BadgeApi) — Phase 04-02
- Pas de SaveSystem implementation — Phase 04-03
- Pas de coroutines Lua avancées (resume/yield depuis C#) — implémentation basique Execute/Evaluate suffit pour ce plan
- Pas de `data/scripts/` Lua prod — Phase 04-03

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx` — 0 errors, 0 warnings
- [ ] `dotnet list src/SDK.Core/SDK.Core.csproj package` — liste vide (AC-5, D-01)
- [ ] `dotnet test tests/SDK.Scripting.Tests` — tous verts
- [ ] `dotnet test` — régression zéro (≥77 tests existants + N nouveaux)
- [ ] IScriptEngine + ISaveSystem dans SDK.Core.Interfaces
- [ ] GameState.Flags = Dictionary<string, JsonElement> (D-12)
- [ ] LuaScriptEngine utilise Preset_SoftSandbox (D-04)
</verification>

<success_criteria>
- Toutes les tâches complétées
- Tous les ACs verts (AC-1 → AC-5)
- 0 régression sur les 77 tests existants
- SDK.Core zéro NuGet (D-01 respecté)
- MoonSharp SoftSandbox actif (D-04 respecté)
</success_criteria>

<output>
Après completion : `.paul/phases/04-scripting-progression/04-01-SUMMARY.md`
</output>

---
phase: 01-sdk-core-data
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/SDK.Data/SDK.Data.csproj
  - src/SDK.Data/PokemonDbContext.cs
  - src/SDK.Data/Configurations/PokemonSpeciesConfiguration.cs
  - src/SDK.Data/Configurations/PokemonFormConfiguration.cs
  - src/SDK.Data/Configurations/PokemonBaseStatsConfiguration.cs
  - src/SDK.Data/Configurations/TranslationConfiguration.cs
  - src/SDK.Data/Configurations/PokemonTypeConfiguration.cs
  - src/SDK.Data/Configurations/TypeEffectivenessConfiguration.cs
  - src/SDK.Data/DesignTime/PokemonDbContextFactory.cs
  - src/SDK.Data/Migrations/
  - tests/SDK.Data.Tests/SDK.Data.Tests.csproj
  - tests/SDK.Data.Tests/SqliteTestFixture.cs
  - tests/SDK.Data.Tests/PokemonDbContextTests.cs
  - PokemonSDK.slnx
autonomous: true
---

<objective>
## Goal
Connecter les 6 entités SDK.Core (Plan 01-01) à EF Core 10 via PokemonDbContext + Fluent API, générer la Migration 001 InitialCreate, appliquer le schéma sur SQLite, et valider l'isolation test via SqliteTestFixture.

## Purpose
Un maker peut créer un Pokémon et le persister en SQLite avec un appel `context.SaveChanges()`. Ce plan pose la couche Data complète dont Plan 01-03 (DataSeeder + requêtes) a besoin. La contrainte UNIQUE sur `translations` (D-07) et les composite keys (TypeEffectiveness) sont vérifiées en test.

## Output
- `src/SDK.Data/PokemonDbContext.cs` + 6 configurations Fluent API
- `src/SDK.Data/DesignTime/PokemonDbContextFactory.cs` (IDesignTimeDbContextFactory — migrations sans startup-project)
- `src/SDK.Data/Migrations/` — InitialCreate générée + appliquée
- `data/PokemonSDK.db` — 7 tables SQLite (6 entités + __EFMigrationsHistory)
- `tests/SDK.Data.Tests/` — 3 tests verts
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/phases/01-sdk-core-data/01-01-SUMMARY.md
</context>

<acceptance_criteria>

## AC-1: PokemonDbContext compile avec 6 DbSet
```gherkin
Given SDK.Data.csproj avec EF Core 10 + Fluent API via ApplyConfigurationsFromAssembly
When dotnet build src/SDK.Data
Then 0 erreurs — PokemonDbContext expose DbSet pour les 6 entités SDK.Core
```

## AC-2: Contrainte UNIQUE Translation dans la migration
```gherkin
Given TranslationConfiguration avec HasIndex unique sur (EntityType, EntityId, Locale, Field)
When dotnet ef migrations add InitialCreate --project src/SDK.Data
Then fichier migration contient un index unique sur la table translations (D-07)
```

## AC-3: Migration InitialCreate appliquée sur SQLite
```gherkin
Given IDesignTimeDbContextFactory pointant vers data/PokemonSDK.db
When dotnet ef database update --project src/SDK.Data
Then 7 tables créées dans data/PokemonSDK.db (6 entités + __EFMigrationsHistory), 0 erreur
```

## AC-4: SqliteTestFixture isole 3 tests
```gherkin
Given SDK.Data.Tests avec SqliteTestFixture (SQLite :memory: via SqliteConnection ouverte)
When dotnet test tests/SDK.Data.Tests
Then 3 tests verts : CanCreateDatabase, CanAddAndQueryPokemonSpecies, Translation_DuplicateKey_ThrowsDbUpdateException
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T1: EF Core 10 packages + PokemonDbContext + 6 configurations + DesignTimeFactory</name>
  <files>
    src/SDK.Data/SDK.Data.csproj,
    src/SDK.Data/PokemonDbContext.cs,
    src/SDK.Data/Configurations/PokemonSpeciesConfiguration.cs,
    src/SDK.Data/Configurations/PokemonFormConfiguration.cs,
    src/SDK.Data/Configurations/PokemonBaseStatsConfiguration.cs,
    src/SDK.Data/Configurations/TranslationConfiguration.cs,
    src/SDK.Data/Configurations/PokemonTypeConfiguration.cs,
    src/SDK.Data/Configurations/TypeEffectivenessConfiguration.cs,
    src/SDK.Data/DesignTime/PokemonDbContextFactory.cs
  </files>
  <action>
    1. Ajouter packages EF Core 10 dans SDK.Data.csproj :
       dotnet add src/SDK.Data/SDK.Data.csproj package Microsoft.EntityFrameworkCore --version "10.*"
       dotnet add src/SDK.Data/SDK.Data.csproj package Microsoft.EntityFrameworkCore.Sqlite --version "10.*"
       dotnet add src/SDK.Data/SDK.Data.csproj package Microsoft.EntityFrameworkCore.Design --version "10.*"

       Après ajout, éditer SDK.Data.csproj pour ajouter PrivateAssets sur Design :
       ```xml
       <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.*">
         <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
         <PrivateAssets>all</PrivateAssets>
       </PackageReference>
       ```

    2. Créer src/SDK.Data/PokemonDbContext.cs :
       namespace SDK.Data;

       using Microsoft.EntityFrameworkCore;
       using SDK.Core.Entities;

       public class PokemonDbContext : DbContext
       {
           public PokemonDbContext(DbContextOptions<PokemonDbContext> options) : base(options) { }

           public DbSet<PokemonSpecies> PokemonSpecies => Set<PokemonSpecies>();
           public DbSet<PokemonForm> PokemonForms => Set<PokemonForm>();
           public DbSet<PokemonBaseStats> PokemonBaseStats => Set<PokemonBaseStats>();
           public DbSet<Translation> Translations => Set<Translation>();
           public DbSet<PokemonType> PokemonTypes => Set<PokemonType>();
           public DbSet<TypeEffectiveness> TypeEffectiveness => Set<TypeEffectiveness>();

           protected override void OnModelCreating(ModelBuilder modelBuilder)
               => modelBuilder.ApplyConfigurationsFromAssembly(typeof(PokemonDbContext).Assembly);
       }

    3. Créer src/SDK.Data/Configurations/PokemonSpeciesConfiguration.cs :
       namespace SDK.Data.Configurations;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Metadata.Builders;
       using SDK.Core.Entities;

       public class PokemonSpeciesConfiguration : IEntityTypeConfiguration<PokemonSpecies>
       {
           public void Configure(EntityTypeBuilder<PokemonSpecies> builder)
           {
               builder.HasKey(e => e.Id);
               builder.Property(e => e.Identifier).IsRequired().HasMaxLength(100);
               builder.Property(e => e.Generation).IsRequired();
               builder.Property(e => e.OriginRegion).HasMaxLength(100);
               builder.HasIndex(e => e.Identifier).IsUnique();
           }
       }

    4. Créer src/SDK.Data/Configurations/PokemonFormConfiguration.cs :
       namespace SDK.Data.Configurations;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Metadata.Builders;
       using SDK.Core.Entities;

       public class PokemonFormConfiguration : IEntityTypeConfiguration<PokemonForm>
       {
           public void Configure(EntityTypeBuilder<PokemonForm> builder)
           {
               builder.HasKey(e => e.Id);
               builder.Property(e => e.AssetKey).IsRequired().HasMaxLength(200);
               builder.Property(e => e.FormKey).HasMaxLength(50);
               builder.Property(e => e.Generation).IsRequired();
           }
       }

    5. Créer src/SDK.Data/Configurations/PokemonBaseStatsConfiguration.cs :
       namespace SDK.Data.Configurations;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Metadata.Builders;
       using SDK.Core.Entities;

       public class PokemonBaseStatsConfiguration : IEntityTypeConfiguration<PokemonBaseStats>
       {
           public void Configure(EntityTypeBuilder<PokemonBaseStats> builder)
           {
               builder.HasKey(e => e.Id);
               builder.Property(e => e.Hp).IsRequired();
               builder.Property(e => e.Attack).IsRequired();
               builder.Property(e => e.Defense).IsRequired();
               builder.Property(e => e.SpecialAttack).IsRequired();
               builder.Property(e => e.SpecialDefense).IsRequired();
               builder.Property(e => e.Speed).IsRequired();
           }
       }

    6. Créer src/SDK.Data/Configurations/TranslationConfiguration.cs (D-07) :
       namespace SDK.Data.Configurations;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Metadata.Builders;
       using SDK.Core.Entities;

       public class TranslationConfiguration : IEntityTypeConfiguration<Translation>
       {
           public void Configure(EntityTypeBuilder<Translation> builder)
           {
               builder.HasKey(e => e.Id);
               builder.Property(e => e.EntityType).IsRequired().HasMaxLength(100);
               builder.Property(e => e.Locale).IsRequired().HasMaxLength(10);
               builder.Property(e => e.Field).IsRequired().HasMaxLength(100);
               builder.Property(e => e.Value).IsRequired();
               // D-07 — UNIQUE(EntityType, EntityId, Locale, Field) : collision = erreur
               builder.HasIndex(e => new { e.EntityType, e.EntityId, e.Locale, e.Field })
                      .IsUnique();
           }
       }

    7. Créer src/SDK.Data/Configurations/PokemonTypeConfiguration.cs :
       namespace SDK.Data.Configurations;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Metadata.Builders;
       using SDK.Core.Entities;

       public class PokemonTypeConfiguration : IEntityTypeConfiguration<PokemonType>
       {
           public void Configure(EntityTypeBuilder<PokemonType> builder)
           {
               builder.HasKey(e => e.Id);
               builder.Property(e => e.Identifier).IsRequired().HasMaxLength(50);
               builder.Property(e => e.Generation).IsRequired();
               builder.HasIndex(e => e.Identifier).IsUnique();
           }
       }

    8. Créer src/SDK.Data/Configurations/TypeEffectivenessConfiguration.cs :
       namespace SDK.Data.Configurations;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Metadata.Builders;
       using SDK.Core.Entities;

       public class TypeEffectivenessConfiguration : IEntityTypeConfiguration<TypeEffectiveness>
       {
           public void Configure(EntityTypeBuilder<TypeEffectiveness> builder)
           {
               // Clé composite incluant Generation : un même couple de types peut changer entre gens
               builder.HasKey(e => new { e.AttackerTypeId, e.DefenderTypeId, e.Generation });
               builder.Property(e => e.DamageFactor).HasPrecision(4, 2).IsRequired();
               builder.Property(e => e.Generation).IsRequired();
           }
       }

    9. Créer src/SDK.Data/DesignTime/PokemonDbContextFactory.cs :
       namespace SDK.Data.DesignTime;
       using Microsoft.EntityFrameworkCore;
       using Microsoft.EntityFrameworkCore.Design;

       public class PokemonDbContextFactory : IDesignTimeDbContextFactory<PokemonDbContext>
       {
           public PokemonDbContext CreateDbContext(string[] args)
           {
               var dbPath = Path.GetFullPath(
                   Path.Combine(Directory.GetCurrentDirectory(), "data", "PokemonSDK.db"));
               Directory.CreateDirectory(Path.GetDirectoryName(dbPath)!);
               var options = new DbContextOptionsBuilder<PokemonDbContext>()
                   .UseSqlite($"DataSource={dbPath}")
                   .Options;
               return new PokemonDbContext(options);
           }
       }

    NE PAS ajouter :
    - Aucun package dans SDK.Core (D-01 : zero NuGet)
    - Navigation properties sur les entités (SDK.Core doit rester pur)
    - Microsoft.EntityFrameworkCore.Tools (spécifique Visual Studio — non nécessaire ici)
  </action>
  <verify>dotnet build src/SDK.Data → "Build succeeded." 0 erreurs</verify>
  <done>AC-1 satisfait : PokemonDbContext compile avec 6 DbSet, Fluent API, IDesignTimeDbContextFactory</done>
</task>

<task type="auto">
  <name>T2: Migration 001 InitialCreate + dotnet ef database update</name>
  <files>
    src/SDK.Data/Migrations/
  </files>
  <action>
    1. Vérifier/installer dotnet-ef 10.x depuis la racine du repo :
       dotnet tool install --global dotnet-ef 2>/dev/null || dotnet tool update --global dotnet-ef
       dotnet ef --version   # doit afficher 10.x

       Si `dotnet ef` non trouvé dans PATH après installation globale :
       export PATH="$PATH:$HOME/.dotnet/tools"

    2. Générer la migration depuis la racine du repo :
       cd /home/subarnan/projects/PokemonSDK
       dotnet ef migrations add InitialCreate --project src/SDK.Data

       Cette commande utilise IDesignTimeDbContextFactory → aucun --startup-project requis.
       Crée src/SDK.Data/Migrations/
         - {timestamp}_InitialCreate.cs      (Up() + Down() schema SQL)
         - PokemonDbContextModelSnapshot.cs  (snapshot modèle EF Core)

    3. Vérifier que le fichier migration contient l'index unique pour translations :
       grep -i "unique" src/SDK.Data/Migrations/*_InitialCreate.cs
       → doit trouver au moins une ligne avec l'index unique sur Translations

    4. Appliquer la migration (crée data/PokemonSDK.db) :
       dotnet ef database update --project src/SDK.Data

    5. Vérifier les tables créées :
       sqlite3 data/PokemonSDK.db ".tables"
       → doit afficher 7 tables : PokemonBaseStats, PokemonForms, PokemonSpecies,
         PokemonTypes, Translations, TypeEffectiveness, __EFMigrationsHistory

    En cas d'erreur "no such command ef" : vérifier PATH ou utiliser le chemin complet
    ~/.dotnet/tools/dotnet-ef
  </action>
  <verify>
    sqlite3 data/PokemonSDK.db ".tables" → 7 tables listées
    grep -i "unique" src/SDK.Data/Migrations/*_InitialCreate.cs → au moins 1 match
  </verify>
  <done>AC-2 + AC-3 satisfaits : migration générée avec index unique translations, SQLite 7 tables créées</done>
</task>

<task type="auto">
  <name>T3: SDK.Data.Tests + SqliteTestFixture + 3 tests isolation</name>
  <files>
    tests/SDK.Data.Tests/SDK.Data.Tests.csproj,
    tests/SDK.Data.Tests/SqliteTestFixture.cs,
    tests/SDK.Data.Tests/PokemonDbContextTests.cs,
    PokemonSDK.slnx
  </files>
  <action>
    1. Créer le projet SDK.Data.Tests :
       dotnet new xunit -n SDK.Data.Tests -f net10.0 -o tests/SDK.Data.Tests
       rm tests/SDK.Data.Tests/UnitTest1.cs

    2. Ajouter les packages :
       dotnet add tests/SDK.Data.Tests/SDK.Data.Tests.csproj reference src/SDK.Data/SDK.Data.csproj
       dotnet add tests/SDK.Data.Tests/SDK.Data.Tests.csproj package FluentAssertions
       dotnet add tests/SDK.Data.Tests/SDK.Data.Tests.csproj package Microsoft.Data.Sqlite
       dotnet add tests/SDK.Data.Tests/SDK.Data.Tests.csproj package coverlet.collector

    3. Ajouter à la solution :
       dotnet sln PokemonSDK.slnx add tests/SDK.Data.Tests/SDK.Data.Tests.csproj

    4. Créer tests/SDK.Data.Tests/SqliteTestFixture.cs :
       namespace SDK.Data.Tests;
       using Microsoft.Data.Sqlite;
       using Microsoft.EntityFrameworkCore;

       public sealed class SqliteTestFixture : IDisposable
       {
           private readonly SqliteConnection _connection;

           public SqliteTestFixture()
           {
               _connection = new SqliteConnection("DataSource=:memory:");
               _connection.Open();
               using var ctx = CreateContext();
               ctx.Database.EnsureCreated();
           }

           public PokemonDbContext CreateContext()
           {
               var options = new DbContextOptionsBuilder<PokemonDbContext>()
                   .UseSqlite(_connection)
                   .Options;
               return new PokemonDbContext(options);
           }

           public void Dispose() => _connection.Dispose();
       }

    5. Créer tests/SDK.Data.Tests/PokemonDbContextTests.cs :
       namespace SDK.Data.Tests;
       using FluentAssertions;
       using Microsoft.EntityFrameworkCore;
       using SDK.Core.Entities;

       public class PokemonDbContextTests : IClassFixture<SqliteTestFixture>
       {
           private readonly SqliteTestFixture _fixture;

           public PokemonDbContextTests(SqliteTestFixture fixture) => _fixture = fixture;

           [Fact]
           public void CanCreateDatabase_AllTablesExist()
           {
               using var ctx = _fixture.CreateContext();
               // EnsureCreated() déjà appelé dans fixture — vérifier que les DbSets sont accessibles
               ctx.PokemonSpecies.Should().NotBeNull();
               ctx.PokemonForms.Should().NotBeNull();
               ctx.PokemonBaseStats.Should().NotBeNull();
               ctx.Translations.Should().NotBeNull();
               ctx.PokemonTypes.Should().NotBeNull();
               ctx.TypeEffectiveness.Should().NotBeNull();
           }

           [Fact]
           public void CanAddAndQueryPokemonSpecies()
           {
               using var ctx = _fixture.CreateContext();
               var species = new PokemonSpecies
               {
                   Identifier = "bulbasaur",
                   Generation = 1,
                   Type1Id = 1
               };
               ctx.PokemonSpecies.Add(species);
               ctx.SaveChanges();

               using var ctx2 = _fixture.CreateContext();
               var result = ctx2.PokemonSpecies.Single(s => s.Identifier == "bulbasaur");
               result.Generation.Should().Be(1);
               result.Type1Id.Should().Be(1);
           }

           [Fact]
           public void Translation_DuplicateUniqueKey_ThrowsDbUpdateException()
           {
               using var ctx = _fixture.CreateContext();
               var t1 = new Translation
               {
                   EntityType = "PokemonSpecies", EntityId = 999,
                   Locale = "fr", Field = "name", Value = "Bulbizarre"
               };
               var t2 = new Translation
               {
                   EntityType = "PokemonSpecies", EntityId = 999,
                   Locale = "fr", Field = "name", Value = "Bulbizarre2"
               };
               ctx.Translations.Add(t1);
               ctx.SaveChanges();
               ctx.Translations.Add(t2);
               var act = () => ctx.SaveChanges();
               act.Should().Throw<DbUpdateException>("D-07 : clé unique (EntityType, EntityId, Locale, Field) doit rejeter les doublons");
           }
       }

    NE PAS ajouter :
    - Tests utilisant data/PokemonSDK.db (isolation = :memory: uniquement)
    - SDK.Data.Tests dans les boundaries du plan 01-01
  </action>
  <verify>dotnet test tests/SDK.Data.Tests --logger "console;verbosity=normal" → "Passed: 3"</verify>
  <done>AC-4 satisfait : 3 tests verts, SqliteTestFixture isole via :memory:</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Core/ (entités et interfaces — zéro modification, zéro NuGet ajouté — D-01)
- .paul/ (géré par PAUL — hors mise à jour STATE.md si besoin)
- tests/SDK.Core.Tests/CoreDependencyTests.cs (garde zero-NuGet ne doit pas régresser)

## SCOPE LIMITS
- SDK.MonoGame : NOT créé — différé Phase 3
- DataSeeder : NOT implémenté — Plan 01-03
- DbContextExtensions (GetByGeneration, GetTranslations) : NOT implémentés — Plan 01-03
- Navigation properties sur les entités SDK.Core : NOT ajoutées (SDK.Core reste pur)
- Seed data dans la migration : NOT inclus — Plan 01-03

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] dotnet build PokemonSDK.slnx → "Build succeeded." (toute la solution)
- [ ] dotnet build src/SDK.Data → 0 erreurs, 0 warnings
- [ ] dotnet list src/SDK.Core/SDK.Core.csproj package → liste vide (D-01 inchangé)
- [ ] dotnet test tests/SDK.Core.Tests → 1/1 Passed (non-régression)
- [ ] dotnet ef --version → 10.x.x
- [ ] src/SDK.Data/Migrations/*_InitialCreate.cs existe
- [ ] grep -i "unique" Migrations/*_InitialCreate.cs → 1+ match (D-07)
- [ ] sqlite3 data/PokemonSDK.db ".tables" → 7 tables
- [ ] dotnet test tests/SDK.Data.Tests → 3/3 Passed
</verification>

<success_criteria>
- PokemonDbContext avec 6 DbSets + Fluent API compilant sans erreur
- IDesignTimeDbContextFactory permettant migrations sans startup-project
- Migration 001 générée + appliquée → SQLite 7 tables
- Index UNIQUE translations présent (D-07 vérifiable en test)
- SqliteTestFixture en :memory: avec 3 tests verts
- CoreDependencyTests toujours vert (SDK.Core zero-NuGet non régressé)
</success_criteria>

<output>
Après completion : créer .paul/phases/01-sdk-core-data/01-02-SUMMARY.md
</output>

---
phase: 01-sdk-core-data
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/SDK.Data/Extensions/DbContextExtensions.cs
  - src/SDK.Data/Seeding/DataSeeder.cs
  - src/SDK.Tools/SDK.Tools.csproj
  - src/SDK.Tools/Program.cs
  - tests/SDK.Data.Tests/DbContextExtensionsTests.cs
  - tests/SDK.Data.Tests/DataSeederTests.cs
  - PokemonSDK.slnx
autonomous: true
---

<objective>
## Goal

Ajouter `DbContextExtensions` (filtre génération + lookup traductions), `DataSeeder` (18 types + traductions fr/en), et `SDK.Tools` console `dotnet run -- seed <path>`.

## Purpose

Remplit le critère Phase 1 : "requêter avec filtre génération, lire son nom en 5 langues". La DB est créée (01-02) ; ce plan la rend interrogeable via des extensions typées et la pré-remplit avec des données réelles.

## Output

- `src/SDK.Data/Extensions/DbContextExtensions.cs` — 3 méthodes d'extension
- `src/SDK.Data/Seeding/DataSeeder.cs` — seeder idempotent 18 types + traductions
- `src/SDK.Tools/` — console app .NET 10, dépendance SDK.Core + SDK.Data uniquement
- `tests/SDK.Data.Tests/DbContextExtensionsTests.cs` — 3 tests verts
- `tests/SDK.Data.Tests/DataSeederTests.cs` — 2 tests verts
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-sdk-core-data/01-02-SUMMARY.md

## Source Files
@src/SDK.Data/PokemonDbContext.cs
@src/SDK.Core/Entities/PokemonSpecies.cs
@src/SDK.Core/Entities/PokemonType.cs
@src/SDK.Core/Entities/Translation.cs
@tests/SDK.Data.Tests/SqliteTestFixture.cs
</context>

<acceptance_criteria>

## AC-1: GetSpeciesByGeneration filtre par génération <= N

```gherkin
Given une DB avec Bulbasaur (gen=1) et Togepi (gen=2) et Ralts (gen=3)
When GetSpeciesByGeneration(ctx, 2)
Then retourne Bulbasaur et Togepi, pas Ralts
```

## AC-2: GetTranslations retourne les traductions correctes

```gherkin
Given une Translation {EntityType="PokemonType", EntityId=1, Locale="fr", Field="name", Value="Plante"}
When GetTranslation(ctx, "PokemonType", 1, "fr", "name")
Then retourne "Plante"
```

## AC-3: DataSeeder.SeedAll crée 18 PokemonType idempotent

```gherkin
Given une DB vide (EnsureCreated)
When DataSeeder.SeedAll(ctx) appelé 2 fois de suite
Then ctx.PokemonTypes.Count() == 18, pas de doublon, pas d'exception
```

## AC-4: SDK.Tools seed CLI applique les seeds et sort 0

```gherkin
Given une DB SQLite vide avec migration appliquée
When dotnet run --project src/SDK.Tools -- seed <dbPath>
Then process exit code == 0, DB contient 18 PokemonType
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : DbContextExtensions + tests</name>
  <files>
    src/SDK.Data/Extensions/DbContextExtensions.cs,
    tests/SDK.Data.Tests/DbContextExtensionsTests.cs
  </files>
  <action>
    Créer `src/SDK.Data/Extensions/DbContextExtensions.cs` avec namespace `SDK.Data.Extensions` :

    ```csharp
    public static class DbContextExtensions
    {
        // Espèces introduites jusqu'à la génération maxGeneration (<=)
        public static IQueryable<PokemonSpecies> GetSpeciesByGeneration(
            this PokemonDbContext ctx, int maxGeneration)
            => ctx.PokemonSpecies.Where(s => s.Generation <= maxGeneration);

        // Types disponibles jusqu'à la génération maxGeneration (<=)
        // Ex : Fée (gen=6) exclue si maxGeneration=5
        public static IQueryable<PokemonType> GetTypesByGeneration(
            this PokemonDbContext ctx, int maxGeneration)
            => ctx.PokemonTypes.Where(t => t.Generation <= maxGeneration);

        // Toutes les traductions pour une entité + locale
        public static IQueryable<Translation> GetTranslations(
            this PokemonDbContext ctx, string entityType, int entityId, string locale)
            => ctx.Translations.Where(t =>
                t.EntityType == entityType &&
                t.EntityId == entityId &&
                t.Locale == locale);

        // Raccourci : valeur d'un champ unique (retourne null si absent)
        public static string? GetTranslation(
            this PokemonDbContext ctx, string entityType, int entityId, string locale, string field)
            => ctx.Translations
                .Where(t => t.EntityType == entityType && t.EntityId == entityId &&
                            t.Locale == locale && t.Field == field)
                .Select(t => t.Value)
                .FirstOrDefault();
    }
    ```

    Créer `tests/SDK.Data.Tests/DbContextExtensionsTests.cs` avec `IClassFixture<SqliteTestFixture>` :
    - Test 1 : insérer 3 espèces (gen 1, 2, 3) → GetSpeciesByGeneration(2) retourne 2 résultats
    - Test 2 : GetTypesByGeneration(5) exclut Fairy (gen=6), inclut Dark (gen=2)
    - Test 3 : insérer Translation, GetTranslation retourne "Plante"

    Utiliser `using SDK.Data.Extensions;` dans le fichier de test.
    Ne pas modifier les entités SDK.Core.
  </action>
  <verify>dotnet test tests/SDK.Data.Tests --filter "DbContextExtensionsTests" --no-build || dotnet test tests/SDK.Data.Tests</verify>
  <done>AC-1 et AC-2 satisfaits : 3 tests DbContextExtensionsTests verts</done>
</task>

<task type="auto">
  <name>Task 2 : DataSeeder + tests</name>
  <files>
    src/SDK.Data/Seeding/DataSeeder.cs,
    tests/SDK.Data.Tests/DataSeederTests.cs
  </files>
  <action>
    Créer `src/SDK.Data/Seeding/DataSeeder.cs` avec namespace `SDK.Data.Seeding` :

    ```csharp
    public static class DataSeeder
    {
        public static void SeedAll(PokemonDbContext ctx)
        {
            SeedTypes(ctx);
            SeedTypeTranslations(ctx);
        }
    }
    ```

    `SeedTypes` : insérer les 18 types si `ctx.PokemonTypes.Any() == false` (idempotent).
    Data obligatoire (Id, Identifier, Generation) :

    | Id | Identifier  | Gen |
    |----|-------------|-----|
    |  1 | normal      |  1  |
    |  2 | fire        |  1  |
    |  3 | water       |  1  |
    |  4 | electric    |  1  |
    |  5 | grass       |  1  |
    |  6 | ice         |  1  |
    |  7 | fighting    |  1  |
    |  8 | poison      |  1  |
    |  9 | ground      |  1  |
    | 10 | flying      |  1  |
    | 11 | psychic     |  1  |
    | 12 | bug         |  1  |
    | 13 | rock        |  1  |
    | 14 | ghost       |  1  |
    | 15 | dragon      |  1  |
    | 16 | dark        |  2  |
    | 17 | steel       |  2  |
    | 18 | fairy       |  6  |

    Utiliser `ctx.PokemonTypes.AddRange(types); ctx.SaveChanges();`

    `SeedTypeTranslations` : insérer les traductions fr + en pour les 18 types si
    `ctx.Translations.Any(t => t.EntityType == "PokemonType") == false`.
    Traductions minimales requises (EntityType="PokemonType", Field="name") :
    - Feu/Fire, Eau/Water, Plante/Grass, Normal/Normal, Fée/Fairy, Acier/Steel, Ténèbres/Dark (fr + en suffisant)
    - Couvrir les 18 types en fr + en.

    Note : ne pas seeder TypeEffectiveness (18×18 = 324 entrées) — différé à Plan 02-02 (BattleDataSeeder).
    Note : ne pas seeder PokemonSpecies ici — différé à Plan 01-04 (end-to-end test).

    Créer `tests/SDK.Data.Tests/DataSeederTests.cs` :
    - Test 1 : DataSeeder.SeedAll → ctx.PokemonTypes.Count() == 18
    - Test 2 : DataSeeder.SeedAll appelé 2× → toujours 18 types, pas d'exception (idempotence)

    Chaque test crée son propre `SqliteTestFixture` (ne pas utiliser `IClassFixture` partagée car
    SeedAll modifie l'état — utiliser une fixture locale `new SqliteTestFixture()` dans chaque test).
  </action>
  <verify>dotnet test tests/SDK.Data.Tests --filter "DataSeederTests"</verify>
  <done>AC-3 satisfait : 2 tests DataSeederTests verts, idempotence vérifiée</done>
</task>

<task type="auto">
  <name>Task 3 : SDK.Tools console + seed command</name>
  <files>
    src/SDK.Tools/SDK.Tools.csproj,
    src/SDK.Tools/Program.cs,
    PokemonSDK.slnx
  </files>
  <action>
    Créer le projet console `src/SDK.Tools/` :

    ```xml
    <!-- SDK.Tools.csproj -->
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
      </PropertyGroup>
      <ItemGroup>
        <ProjectReference Include="..\SDK.Core\SDK.Core.csproj" />
        <ProjectReference Include="..\SDK.Data\SDK.Data.csproj" />
      </ItemGroup>
      <!-- Pas de MonoGame — D-17 -->
    </Project>
    ```

    Créer `src/SDK.Tools/Program.cs` :

    ```csharp
    using Microsoft.EntityFrameworkCore;
    using SDK.Data;
    using SDK.Data.Seeding;

    if (args.Length == 0 || args[0] != "seed")
    {
        Console.Error.WriteLine("Usage: SDK.Tools seed <db-path>");
        Console.Error.WriteLine("  seed <db-path>  — applies migrations and seeds reference data");
        return 1;
    }

    var dbPath = args.Length > 1 ? args[1] : "data/PokemonSDK.db";
    Directory.CreateDirectory(Path.GetDirectoryName(Path.GetFullPath(dbPath))!);

    var options = new DbContextOptionsBuilder<PokemonDbContext>()
        .UseSqlite($"DataSource={dbPath}")
        .Options;

    using var ctx = new PokemonDbContext(options);
    ctx.Database.Migrate();
    DataSeeder.SeedAll(ctx);

    Console.WriteLine($"Seed complete: {ctx.PokemonTypes.Count()} types in {dbPath}");
    return 0;
    ```

    Ajouter SDK.Tools à `PokemonSDK.slnx` en suivant le pattern des autres projets (balise `<Project>`).

    Vérifier : `dotnet build src/SDK.Tools` → 0 erreurs.
    Vérifier : `dotnet list src/SDK.Tools/SDK.Tools.csproj package` → aucun package MonoGame (D-17).
    Test fonctionnel : `dotnet run --project src/SDK.Tools -- seed /tmp/test-seed.db` → exit 0, sortie "18 types".
  </action>
  <verify>dotnet build src/SDK.Tools && dotnet run --project src/SDK.Tools -- seed /tmp/test-seed-01-03.db && echo "EXIT:$?"</verify>
  <done>AC-4 satisfait : exit 0, sortie "18 types in /tmp/test-seed-01-03.db"</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Core/**` — zéro NuGet externe, entités non modifiées (D-01)
- `src/SDK.Data/PokemonDbContext.cs` — DbSets inchangés
- `src/SDK.Data/Configurations/**` — Fluent API vérouillée
- `src/SDK.Data/Migrations/**` — pas de nouvelle migration dans ce plan
- `src/SDK.Data/DesignTime/PokemonDbContextFactory.cs` — pattern établi, non touché

## SCOPE LIMITS
- TypeEffectiveness seeding (18×18) : différé à Plan 02-02 (BattleDataSeeder)
- PokemonSpecies seeding : différé à Plan 01-04 (end-to-end test)
- SDK.Battle, SDK.Scripting, SDK.MonoGame : hors scope Phase 1
- Pas de nouvelle migration EF Core dans ce plan

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx` → 0 erreur, 0 warning
- [ ] `dotnet test tests/SDK.Data.Tests` → tous les tests verts (incluant les 5 nouveaux + 3 existants)
- [ ] `dotnet list src/SDK.Core/SDK.Core.csproj package` → vide (D-01 non régressé)
- [ ] `dotnet list src/SDK.Tools/SDK.Tools.csproj package` → aucun MonoGame (D-17)
- [ ] `dotnet run --project src/SDK.Tools -- seed /tmp/test-01-03.db` → exit 0, "18 types"
- [ ] Idempotence : 2ème appel `seed` sur même DB → exit 0, toujours 18 types
</verification>

<success_criteria>
- Toutes les tâches complétées
- 8 tests verts dans SDK.Data.Tests (3 existants + 3 DbContextExtensions + 2 DataSeeder)
- SDK.Tools compile sans MonoGame
- `dotnet run -- seed` seed 18 types en DB et sort 0
- CoreDependencyTests toujours vert
</success_criteria>

<output>
Après complétion, créer `.paul/phases/01-sdk-core-data/01-03-SUMMARY.md`
</output>

---
phase: 01-sdk-core-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PokemonSDK.sln
  - src/SDK.Core/SDK.Core.csproj
  - src/SDK.Core/Entities/PokemonSpecies.cs
  - src/SDK.Core/Entities/PokemonForm.cs
  - src/SDK.Core/Entities/PokemonBaseStats.cs
  - src/SDK.Core/Entities/Translation.cs
  - src/SDK.Core/Entities/PokemonType.cs
  - src/SDK.Core/Entities/TypeEffectiveness.cs
  - src/SDK.Core/Enums/MoveType.cs
  - src/SDK.Core/Enums/TimeOfDay.cs
  - src/SDK.Core/Enums/DifficultyMode.cs
  - src/SDK.Core/Interfaces/IRepository.cs
  - src/SDK.Core/ValueObjects/BattleConfig.cs
  - src/SDK.Data/SDK.Data.csproj
  - src/SDK.Battle/SDK.Battle.csproj
  - src/SDK.Scripting/SDK.Scripting.csproj
  - tests/SDK.Core.Tests/SDK.Core.Tests.csproj
  - tests/SDK.Core.Tests/CoreDependencyTests.cs
autonomous: true
---

<objective>
## Goal
Scaffolder PokemonSDK.sln (.NET 10) avec 4 classlib + 1 xUnit, implémenter les 11 fichiers SDK.Core Wave 1, et valider la contrainte zero-NuGet via CoreDependencyTests.

## Purpose
Premier build vert du SDK. Un maker venant de PSDK/Essentials voit une solution propre qui compile immédiatement. SDK.Core pose les entités Data (PokemonSpecies, Form, Stats, Translation, Type, TypeEffectiveness) dont SDK.Data aura besoin au Plan 01-02.

## Output
- PokemonSDK.sln : 5 projets (SDK.Core, SDK.Data, SDK.Battle, SDK.Scripting, SDK.Core.Tests)
- src/SDK.Core/Entities/ : 6 fichiers
- src/SDK.Core/Enums/ : 3 fichiers
- src/SDK.Core/Interfaces/ : 1 fichier
- src/SDK.Core/ValueObjects/ : 1 fichier
- tests/SDK.Core.Tests/CoreDependencyTests.cs
- Résultat smoke test MonoGame logué dans STATE.md (hors solution)
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
</context>

<acceptance_criteria>

## AC-1: Solution compile proprement
```gherkin
Given PokemonSDK.sln scaffoldé avec 4 classlib + 1 xUnit net10.0
When dotnet build PokemonSDK.sln
Then 0 erreurs, 0 warnings bloquants
```

## AC-2: SDK.Core sans NuGet externe
```gherkin
Given SDK.Core.csproj créé sans PackageReference
When dotnet list src/SDK.Core/SDK.Core.csproj package
Then liste vide (aucun package listé)
```

## AC-3: CoreDependencyTests passent
```gherkin
Given CoreDependencyTests.cs présent dans SDK.Core.Tests
When dotnet test tests/SDK.Core.Tests
Then SdkCore_HasNoExternalNuGetPackages passe (1 test vert)
```

## AC-4: SDK.Core Wave 1 models compilent
```gherkin
Given 11 fichiers C# créés dans SDK.Core (6 entités + 3 enums + 1 interface + 1 value object)
When dotnet build src/SDK.Core
Then 0 erreurs — generation NOT NULL présent, pas de colonnes name_fr/name_en
```

## AC-5: Smoke test MonoGame documenté
```gherkin
Given projet console throwaway net10.0 + MonoGame.Framework.DesktopGL créé dans /tmp hors solution
When dotnet build sur le projet smoke
Then résultat (pass/fail + workaround si échec) logué dans .paul/STATE.md section Deferred Issues
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T1: Scaffold solution + 4 classlib net10.0 + projet test</name>
  <files>
    PokemonSDK.sln,
    src/SDK.Core/SDK.Core.csproj,
    src/SDK.Data/SDK.Data.csproj,
    src/SDK.Battle/SDK.Battle.csproj,
    src/SDK.Scripting/SDK.Scripting.csproj,
    tests/SDK.Core.Tests/SDK.Core.Tests.csproj
  </files>
  <action>
    Depuis la racine /home/subarnan/projects/PokemonSDK :

    1. Créer la solution :
       dotnet new sln -n PokemonSDK --force

    2. Créer les 4 classlib net10.0 :
       dotnet new classlib -n SDK.Core -f net10.0 -o src/SDK.Core
       dotnet new classlib -n SDK.Data -f net10.0 -o src/SDK.Data
       dotnet new classlib -n SDK.Battle -f net10.0 -o src/SDK.Battle
       dotnet new classlib -n SDK.Scripting -f net10.0 -o src/SDK.Scripting

       Supprimer Class1.cs généré par le template dans chacun des 4 projets.

    3. Créer le projet test xUnit :
       dotnet new xunit -n SDK.Core.Tests -f net10.0 -o tests/SDK.Core.Tests
       Supprimer UnitTest1.cs généré.
       Ajouter les packages test :
         dotnet add tests/SDK.Core.Tests/SDK.Core.Tests.csproj package FluentAssertions
         dotnet add tests/SDK.Core.Tests/SDK.Core.Tests.csproj package coverlet.collector

    4. Ajouter tous les projets à la solution :
       dotnet sln add src/SDK.Core/SDK.Core.csproj
       dotnet sln add src/SDK.Data/SDK.Data.csproj
       dotnet sln add src/SDK.Battle/SDK.Battle.csproj
       dotnet sln add src/SDK.Scripting/SDK.Scripting.csproj
       dotnet sln add tests/SDK.Core.Tests/SDK.Core.Tests.csproj

    5. Ajouter les références projet (règles de dépendances NON NÉGOCIABLES) :
       dotnet add src/SDK.Data/SDK.Data.csproj reference src/SDK.Core/SDK.Core.csproj
       dotnet add src/SDK.Battle/SDK.Battle.csproj reference src/SDK.Core/SDK.Core.csproj
       dotnet add src/SDK.Scripting/SDK.Scripting.csproj reference src/SDK.Core/SDK.Core.csproj
       dotnet add tests/SDK.Core.Tests/SDK.Core.Tests.csproj reference src/SDK.Core/SDK.Core.csproj

       NE PAS ajouter : SDK.Data → SDK.Battle, SDK.Data → SDK.Scripting,
       ni aucune référence à SDK.MonoGame (différé Phase 3).

    Vérifier immédiatement : dotnet build PokemonSDK.sln → "Build succeeded." avant de passer à T2.
  </action>
  <verify>dotnet build PokemonSDK.sln → sortie contient "Build succeeded." sans erreur</verify>
  <done>AC-1 satisfait : solution 5 projets net10.0 compile proprement</done>
</task>

<task type="auto">
  <name>T2: SDK.Core Wave 1 domain models (11 fichiers)</name>
  <files>
    src/SDK.Core/Entities/PokemonSpecies.cs,
    src/SDK.Core/Entities/PokemonForm.cs,
    src/SDK.Core/Entities/PokemonBaseStats.cs,
    src/SDK.Core/Entities/Translation.cs,
    src/SDK.Core/Entities/PokemonType.cs,
    src/SDK.Core/Entities/TypeEffectiveness.cs,
    src/SDK.Core/Enums/MoveType.cs,
    src/SDK.Core/Enums/TimeOfDay.cs,
    src/SDK.Core/Enums/DifficultyMode.cs,
    src/SDK.Core/Interfaces/IRepository.cs,
    src/SDK.Core/ValueObjects/BattleConfig.cs
  </files>
  <action>
    Namespace racine : SDK.Core
    ZÉRO using externe. SDK.Core.csproj NE DOIT RECEVOIR AUCUN PackageReference.

    --- Entités (namespace SDK.Core.Entities) ---

    PokemonSpecies.cs :
      public class PokemonSpecies
        int Id
        string Identifier
        int Generation          // NOT NULL — D-09
        string? OriginRegion
        int Type1Id
        int? Type2Id
      NE PAS ajouter NameFr, NameEn, ni aucune colonne par langue (D-07).

    PokemonForm.cs :
      public class PokemonForm
        int Id
        int SpeciesId
        string? FormKey         // null = forme par défaut
        string AssetKey
        bool IsDefault
        int Generation          // NOT NULL — D-09

    PokemonBaseStats.cs :
      public class PokemonBaseStats
        int Id
        int FormId
        int Hp
        int Attack
        int Defense
        int SpecialAttack
        int SpecialDefense
        int Speed

    Translation.cs :
      public class Translation
        int Id
        string EntityType       // ex. "PokemonSpecies", "Move"
        int EntityId
        string Locale           // ex. "fr", "en", "ja"
        string Field            // ex. "name", "description"
        string Value
      // UNIQUE(EntityType, EntityId, Locale, Field) — collision = erreur, pas d'override silencieux

    PokemonType.cs (NOMMER PokemonType, pas Type — éviter conflit System.Type) :
      public class PokemonType
        int Id
        string Identifier
        int Generation          // NOT NULL — D-09

    TypeEffectiveness.cs :
      public class TypeEffectiveness
        int AttackerTypeId
        int DefenderTypeId
        decimal DamageFactor    // ex. 2.0m, 0.5m, 0.0m
        int Generation          // NOT NULL — D-09

    --- Enums (namespace SDK.Core.Enums) ---

    MoveType.cs : Normal, Fire, Water, Electric, Grass, Ice, Fighting, Poison,
      Ground, Flying, Psychic, Bug, Rock, Ghost, Dragon, Dark, Steel, Fairy (18 valeurs exactes).

    TimeOfDay.cs : Morning, Day, Evening, Night.

    DifficultyMode.cs : Story, Hard.

    --- Interface (namespace SDK.Core.Interfaces) ---

    IRepository.cs :
      public interface IRepository<T>
        Task<T?> GetByIdAsync(int id)
        Task<IReadOnlyList<T>> GetAllAsync()
        Task AddAsync(T entity)
        Task UpdateAsync(T entity)
        Task DeleteAsync(int id)

    --- Value Object (namespace SDK.Core.ValueObjects) ---

    BattleConfig.cs :
      public sealed record BattleConfig(
        bool ItemsEnabled = true,
        bool FleeEnabled = true,
        bool WeatherEnabled = false,
        bool CritEnabled = true)
      // Référencé par IBattleEngine (Phase 2) — défini ici car SDK.Core only (D-05, D-06)
  </action>
  <verify>dotnet build src/SDK.Core/SDK.Core.csproj → 0 erreurs, 0 warnings</verify>
  <done>AC-4 satisfait : 11 fichiers C# compilent, generation NOT NULL présent sur 4 entités, pas de colonnes par langue</done>
</task>

<task type="auto">
  <name>T3: CoreDependencyTests + smoke test MonoGame net10.0</name>
  <files>
    tests/SDK.Core.Tests/CoreDependencyTests.cs
  </files>
  <action>
    1. Créer tests/SDK.Core.Tests/CoreDependencyTests.cs :

       namespace SDK.Core.Tests;

       public class CoreDependencyTests
       {
           [Fact]
           public void SdkCore_HasNoExternalNuGetPackages()
           {
               // Naviguer depuis le répertoire de build jusqu'à la racine (5× ..)
               var csprojPath = Path.GetFullPath(
                   Path.Combine(AppContext.BaseDirectory,
                       "..", "..", "..", "..", "..",
                       "src", "SDK.Core", "SDK.Core.csproj"));

               File.Exists(csprojPath).Should()
                   .BeTrue($"SDK.Core.csproj introuvable à {csprojPath}");

               var doc = System.Xml.Linq.XDocument.Load(csprojPath);
               var packageRefs = doc.Descendants("PackageReference").ToList();

               packageRefs.Should().BeEmpty(
                   "SDK.Core must have zero external NuGet packages (D-01 constraint)");
           }
       }

       Usings nécessaires (using global ou en tête de fichier) :
         using FluentAssertions;
         using System.Xml.Linq;

    2. Smoke test MonoGame (hors solution, hors repo) :
       Exécuter dans le shell :
         mkdir -p /tmp/mg-smoke-net10/MgSmokeTest
         cd /tmp/mg-smoke-net10 && dotnet new console -f net10.0 -n MgSmokeTest --force
         cd /tmp/mg-smoke-net10/MgSmokeTest && dotnet add package MonoGame.Framework.DesktopGL
         dotnet build /tmp/mg-smoke-net10/MgSmokeTest

       Capturer le résultat :
       - Si succès → mettre à jour STATE.md : ligne "Vérifier compat MonoGame.DesktopGL / .NET 10"
         → "MonoGame.Framework.DesktopGL compatible net10.0 ✓ (smoke test 2026-06-01)"
       - Si échec → noter l'erreur exacte + workaround : cibler net8.0 dans SDK.MonoGame.csproj
         (le package fonctionne via fallback net8.0, seul le template dotnet new mgdesktopgl est affecté)

       Ne PAS ajouter /tmp/mg-smoke-net10 à PokemonSDK.sln ni au repo git.
  </action>
  <verify>dotnet test tests/SDK.Core.Tests --logger "console;verbosity=normal" → "Passed: 1"</verify>
  <done>AC-2, AC-3, AC-5 satisfaits : CoreDependencyTests vert + résultat smoke test dans STATE.md</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- .paul/ (géré par PAUL — sauf mise à jour smoke test dans STATE.md)
- CLAUDE.md, PROJECT.md, REQUIREMENTS.md, ROADMAP.md (racine)
- /tmp/mg-smoke-net10/ (throwaway — ne pas ajouter à la solution)

## SCOPE LIMITS
- SDK.MonoGame : NOT créé dans ce plan — différé Phase 3
- EF Core packages : NOT ajoutés dans ce plan — Plan 01-02
- MoonSharp : NOT ajouté dans ce plan — Phase 2+
- Migrations EF Core : NOT créées dans ce plan — Plan 01-02
- SDK.Core doit rester SANS PackageReference à la fin du plan

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] dotnet build PokemonSDK.sln → "Build succeeded."
- [ ] dotnet list src/SDK.Core/SDK.Core.csproj package → liste vide
- [ ] dotnet test tests/SDK.Core.Tests → 1 test passed (SdkCore_HasNoExternalNuGetPackages)
- [ ] 6 entités présentes dans src/SDK.Core/Entities/
- [ ] 3 enums présents dans src/SDK.Core/Enums/
- [ ] IRepository.cs dans src/SDK.Core/Interfaces/
- [ ] BattleConfig.cs dans src/SDK.Core/ValueObjects/
- [ ] Entité nommée PokemonType (pas Type) pour éviter conflit System.Type
- [ ] generation NOT NULL (int, non nullable) sur : PokemonSpecies, PokemonForm, PokemonType, TypeEffectiveness
- [ ] Aucune propriété name_fr / name_en sur aucune entité (D-07)
- [ ] Résultat smoke test MonoGame logué dans STATE.md
</verification>

<success_criteria>
- PokemonSDK.sln avec 5 projets net10.0 qui compile
- SDK.Core : 11 fichiers C# conformes au design doc approuvé (2026-06-01)
- CoreDependencyTests : 1 test vert
- Smoke test MonoGame : résultat documenté (pass ou fail + workaround)
- Zéro NuGet dans SDK.Core
</success_criteria>

<output>
Après completion : créer .paul/phases/01-sdk-core-data/01-01-SUMMARY.md
</output>

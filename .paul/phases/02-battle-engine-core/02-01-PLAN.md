---
phase: 02-battle-engine-core
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - src/SDK.Core/Entities/Move.cs
  - src/SDK.Core/Entities/Ability.cs
  - src/SDK.Core/Entities/Learnset.cs
  - src/SDK.Core/Enums/MoveCategory.cs
  - src/SDK.Core/Enums/WeatherType.cs
  - src/SDK.Core/Interfaces/IBattleEngine.cs
  - src/SDK.Core/Interfaces/IBattlePlugin.cs
  - src/SDK.Core/ValueObjects/BattleResult.cs
  - src/SDK.Core/ValueObjects/DamageResult.cs
  - tests/SDK.Core.Tests/CoreBattleDependencyTests.cs
autonomous: true
---

<objective>
## Goal
Ajouter dans SDK.Core tous les modèles de domaine nécessaires au battle engine : entités Move/Ability/Learnset, enums MoveCategory/WeatherType, interfaces IBattleEngine/IBattlePlugin, value objects BattleResult/DamageResult.

## Purpose
SDK.Battle (Plan 02-03) référence uniquement SDK.Core — ces types doivent exister dans SDK.Core avant que le moteur puisse compiler. Zéro NuGet externe dans SDK.Core (D-01 confirmé par test).

## Output
9 nouveaux fichiers dans SDK.Core + 1 test automatisé vérifiant que SDK.Battle n'a aucun paquet NuGet externe.
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-sdk-core-data/01-04-SUMMARY.md

## Source Files
@src/SDK.Core/Entities/PokemonSpecies.cs
@src/SDK.Core/Enums/MoveType.cs
@src/SDK.Core/Interfaces/IRepository.cs
@src/SDK.Core/ValueObjects/BattleConfig.cs
@src/SDK.Battle/SDK.Battle.csproj
@tests/SDK.Core.Tests/PlatformTests.cs
</context>

<acceptance_criteria>

## AC-1: Entités battle présentes et compilables
```gherkin
Given SDK.Core sans Move/Ability/Learnset
When on ajoute les 3 entités avec leurs propriétés et nav properties
Then dotnet build SDK.Core passe sans erreur
     Move a : Id, Identifier, TypeId, Category(MoveCategory), Power(int?), Accuracy, PP, Generation, nav Type
     Ability a : Id, Identifier, Generation
     Learnset a : Id, SpeciesId, MoveId, LearnLevel, Generation, nav Species, nav Move
```

## AC-2: Enums, interfaces et value objects compilables
```gherkin
Given SDK.Core sans MoveCategory/WeatherType/IBattleEngine/IBattlePlugin/BattleResult/DamageResult
When on ajoute les 6 types
Then dotnet build SDK.Core passe sans erreur
     MoveCategory : Physical | Special | Status
     WeatherType  : None | Sun | Rain | Sand | Hail
     IBattleEngine.RunBattle(BattleConfig) → BattleResult
     IBattlePlugin : Name(string), OnBattleStart, OnTurnEnd, OnBattleEnd
     BattleResult  : sealed record (bool PlayerWon, int TurnsElapsed, string? EndReason)
     DamageResult  : sealed record (int Damage, bool IsCritical, decimal TypeMultiplier)
```

## AC-3: SDK.Battle zéro dépendance NuGet externe
```gherkin
Given SDK.Battle.csproj référençant uniquement SDK.Core
When CoreBattleDependencyTests scanne SDK.Battle.csproj via XDocument
Then 0 élément <PackageReference> trouvé dans SDK.Battle.csproj
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : Entités Move / Ability / Learnset + enums MoveCategory / WeatherType</name>
  <files>
    src/SDK.Core/Entities/Move.cs,
    src/SDK.Core/Entities/Ability.cs,
    src/SDK.Core/Entities/Learnset.cs,
    src/SDK.Core/Enums/MoveCategory.cs,
    src/SDK.Core/Enums/WeatherType.cs
  </files>
  <action>
    Créer src/SDK.Core/Enums/MoveCategory.cs :
    ```csharp
    namespace SDK.Core.Enums;
    public enum MoveCategory { Physical, Special, Status }
    ```

    Créer src/SDK.Core/Enums/WeatherType.cs :
    ```csharp
    namespace SDK.Core.Enums;
    public enum WeatherType { None, Sun, Rain, Sand, Hail }
    ```

    Créer src/SDK.Core/Entities/Move.cs :
    ```csharp
    namespace SDK.Core.Entities;
    using SDK.Core.Enums;

    public class Move
    {
        public int Id { get; set; }
        public string Identifier { get; set; } = string.Empty;
        public int TypeId { get; set; }
        public MoveCategory Category { get; set; }
        public int? Power { get; set; }       // null = moves de statut
        public int Accuracy { get; set; }     // 0 = ne rate jamais
        public int PP { get; set; }
        public int Generation { get; set; }
        public PokemonType Type { get; set; } = null!;
    }
    ```

    Créer src/SDK.Core/Entities/Ability.cs :
    ```csharp
    namespace SDK.Core.Entities;

    public class Ability
    {
        public int Id { get; set; }
        public string Identifier { get; set; } = string.Empty;
        public int Generation { get; set; }
    }
    ```

    Créer src/SDK.Core/Entities/Learnset.cs :
    ```csharp
    namespace SDK.Core.Entities;

    public class Learnset
    {
        public int Id { get; set; }
        public int SpeciesId { get; set; }
        public int MoveId { get; set; }
        public int LearnLevel { get; set; }   // 0 = CT/CS, >0 = montée de niveau
        public int Generation { get; set; }
        public PokemonSpecies Species { get; set; } = null!;
        public Move Move { get; set; } = null!;
    }
    ```

    Éviter : propriétés nullable superflues, dépendances NuGet, attributs EF Core (appartiennent à SDK.Data).
  </action>
  <verify>dotnet build src/SDK.Core/SDK.Core.csproj → 0 erreur, 0 warning</verify>
  <done>AC-1 satisfait : Move/Ability/Learnset/MoveCategory/WeatherType compilent dans SDK.Core</done>
</task>

<task type="auto">
  <name>Task 2 : Interfaces IBattleEngine / IBattlePlugin + value objects BattleResult / DamageResult + test dépendances</name>
  <files>
    src/SDK.Core/Interfaces/IBattleEngine.cs,
    src/SDK.Core/Interfaces/IBattlePlugin.cs,
    src/SDK.Core/ValueObjects/BattleResult.cs,
    src/SDK.Core/ValueObjects/DamageResult.cs,
    tests/SDK.Core.Tests/CoreBattleDependencyTests.cs
  </files>
  <action>
    Créer src/SDK.Core/ValueObjects/BattleResult.cs :
    ```csharp
    namespace SDK.Core.ValueObjects;

    public sealed record BattleResult(
        bool PlayerWon,
        int TurnsElapsed,
        string? EndReason = null);
    ```

    Créer src/SDK.Core/ValueObjects/DamageResult.cs :
    ```csharp
    namespace SDK.Core.ValueObjects;

    public sealed record DamageResult(
        int Damage,
        bool IsCritical,
        decimal TypeMultiplier);
    ```

    Créer src/SDK.Core/Interfaces/IBattleEngine.cs :
    ```csharp
    namespace SDK.Core.Interfaces;
    using SDK.Core.ValueObjects;

    public interface IBattleEngine
    {
        BattleResult RunBattle(BattleConfig config);
    }
    ```

    Créer src/SDK.Core/Interfaces/IBattlePlugin.cs :
    ```csharp
    namespace SDK.Core.Interfaces;
    using SDK.Core.ValueObjects;

    public interface IBattlePlugin
    {
        string Name { get; }
        void OnBattleStart(BattleConfig config);
        void OnTurnEnd(int turnNumber);
        void OnBattleEnd(BattleResult result);
    }
    ```

    Créer tests/SDK.Core.Tests/CoreBattleDependencyTests.cs — même pattern que PlatformTests.cs :
    ```csharp
    namespace SDK.Core.Tests;

    using System.Xml.Linq;
    using FluentAssertions;

    public class CoreBattleDependencyTests
    {
        [Fact]
        public void SdkBattle_HasZeroNuGetPackageReferences()
        {
            var root = GetRepoRoot();
            var csproj = Path.Combine(root, "src", "SDK.Battle", "SDK.Battle.csproj");
            File.Exists(csproj).Should().BeTrue($"SDK.Battle.csproj introuvable à {csproj}");

            var doc = XDocument.Load(csproj);
            var packages = doc.Descendants("PackageReference").ToList();
            packages.Should().BeEmpty("SDK.Battle doit rester sans NuGet externe (D-01 / règle dépendances)");
        }

        private static string GetRepoRoot()
        {
            var dir = new DirectoryInfo(AppContext.BaseDirectory);
            for (int i = 0; i < 6; i++)
            {
                if (File.Exists(Path.Combine(dir!.FullName, "PokemonSDK.slnx")))
                    return dir.FullName;
                dir = dir.Parent;
            }
            throw new InvalidOperationException("Racine du repo introuvable (PokemonSDK.slnx absent).");
        }
    }
    ```

    Éviter : logique métier dans les interfaces, paramètres BattleState (pas encore défini — Plan 02-03), dépendances croisées.
  </action>
  <verify>
    dotnet build src/SDK.Core/SDK.Core.csproj → 0 erreur
    dotnet test tests/SDK.Core.Tests/ → tous verts (y compris CoreBattleDependencyTests)
  </verify>
  <done>AC-2 + AC-3 satisfaits : interfaces/value objects compilent, SDK.Battle confirme 0 NuGet</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Core/Entities/PokemonSpecies.cs, PokemonType.cs, PokemonForm.cs, PokemonBaseStats.cs, TypeEffectiveness.cs, Translation.cs
- src/SDK.Core/Enums/MoveType.cs, DifficultyMode.cs, TimeOfDay.cs
- src/SDK.Core/Interfaces/IRepository.cs
- src/SDK.Core/ValueObjects/BattleConfig.cs
- src/SDK.Data/ (aucune modification — Plan 02-02 gère les migrations)
- src/SDK.Battle/ (aucun fichier C# — seulement .csproj existant)
- tests/SDK.Data.Tests/, tests/SDK.Battle.Tests/ (hors scope)

## SCOPE LIMITS
- Pas de configuration EF Core (Fluent API) — appartient à SDK.Data Plan 02-02
- Pas de BattleState immuable — appartient à SDK.Battle Plan 02-03
- Pas de IDamageFormula, IDifficultyMode — appartient à SDK.Battle Plan 02-03
- Pas de seeding de données Move/Ability — appartient à SDK.Data Plan 02-02

</boundaries>

<verification>
Avant de déclarer le plan terminé :
- [ ] dotnet build PokemonSDK.slnx → 0 erreur
- [ ] dotnet test tests/SDK.Core.Tests/ → tous verts (anciens + CoreBattleDependencyTests)
- [ ] dotnet test tests/SDK.Data.Tests/ → 12/12 verts (aucune régression)
- [ ] dotnet list src/SDK.Core/SDK.Core.csproj package → liste vide (SDK.Core zéro NuGet)
- [ ] AC-1, AC-2, AC-3 satisfaits
</verification>

<success_criteria>
- 9 fichiers créés dans SDK.Core (5 entités/enums + 4 interfaces/value objects)
- 1 test créé dans SDK.Core.Tests
- 0 erreur de build
- Tous les tests verts (SDK.Core.Tests + SDK.Data.Tests)
- SDK.Core conserve zéro dépendance NuGet externe
- SDK.Battle conserve zéro dépendance NuGet externe
</success_criteria>

<output>
Après complétion, créer `.paul/phases/02-battle-engine-core/02-01-SUMMARY.md`
</output>

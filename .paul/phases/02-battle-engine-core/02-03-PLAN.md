---
phase: 02-battle-engine-core
plan: 03
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
files_modified:
  - src/SDK.Core/Interfaces/IBattleEngine.cs
  - src/SDK.Core/Interfaces/ITypeChart.cs
  - src/SDK.Core/ValueObjects/BattlePokemon.cs
  - src/SDK.Core/ValueObjects/BattleMove.cs
  - src/SDK.Core/ValueObjects/BattleRequest.cs
  - src/SDK.Core/ValueObjects/BattleState.cs
  - src/SDK.Battle/TypeChart.cs
  - src/SDK.Battle/Formulas/IDamageFormula.cs
  - src/SDK.Battle/Formulas/Gen1DamageFormula.cs
  - src/SDK.Battle/Formulas/StandardDamageFormula.cs
  - src/SDK.Battle/Difficulty/IDifficultyMode.cs
  - src/SDK.Battle/Difficulty/StoryDifficultyMode.cs
  - src/SDK.Battle/Difficulty/HardDifficultyMode.cs
  - src/SDK.Battle/BattleEngine.cs
autonomous: true
---

<objective>
## Goal
Implémenter le battle engine headless dans SDK.Battle : BattleState immuable (D-05), IDamageFormula × 2 (Gen1 + Standard), IDifficultyMode × 2 (Story + Hard), BattleEngine. Un combat 1v1 complet peut s'exécuter de start à KO.

## Purpose
Satisfaire BATTLE-01 → 03 (moteur 1v1, formules de dégâts par génération, IA configurable). C'est le cœur du SDK — sans ça, aucun fan-game ne peut combattre.

## Output
- 5 nouveaux types dans SDK.Core (BattlePokemon, BattleMove, BattleRequest, BattleState, ITypeChart)
- IBattleEngine.RunBattle signature mise à jour
- 7 nouveaux fichiers dans SDK.Battle (TypeChart, formules, difficultés, BattleEngine)
- Build propre, 17 tests existants verts
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/02-battle-engine-core/02-01-SUMMARY.md

## Interfaces et types existants (SDK.Core)

IBattleEngine (à modifier) :
```csharp
public interface IBattleEngine
{
    BattleResult RunBattle(BattleConfig config);  // ← signature incomplète, pas de combattants
}
```

BattleConfig (inchangé) :
```csharp
public sealed record BattleConfig(
    bool ItemsEnabled = true,
    bool FleeEnabled = true,
    bool WeatherEnabled = false,
    bool CritEnabled = true);
```

BattleResult (inchangé) :
```csharp
public sealed record BattleResult(
    bool PlayerWon,
    int TurnsElapsed,
    string? EndReason = null);
```

DamageResult (inchangé) :
```csharp
public sealed record DamageResult(
    int Damage,
    bool IsCritical,
    decimal TypeMultiplier);
```

IBattlePlugin (inchangé) :
```csharp
public interface IBattlePlugin
{
    string Name { get; }
    void OnBattleStart(BattleConfig config);
    void OnTurnEnd(int turnNumber);
    void OnBattleEnd(BattleResult result);
}
```

DifficultyMode (inchangé) :
```csharp
public enum DifficultyMode { Story, Hard }
```

Move (inchangé) :
```csharp
public class Move
{
    public int Id { get; set; }
    public int TypeId { get; set; }
    public MoveCategory Category { get; set; }
    public int? Power { get; set; }
    public int Accuracy { get; set; }
    public int PP { get; set; }
    public int Generation { get; set; }
}
```

TypeEffectiveness :
```csharp
public class TypeEffectiveness
{
    public int AttackerTypeId { get; set; }
    public int DefenderTypeId { get; set; }
    public int Generation { get; set; }
    public decimal DamageFactor { get; set; }
}
```

BattleDataSeeder : 83 entrées TypeEffectiveness Gen 1 (factor 0/0.5/2 — défaut implicite 1.0)
</context>

<acceptance_criteria>

## AC-1 : Types runtime combat dans SDK.Core
```gherkin
Given SDK.Core ne contient pas de types runtime combat (BattlePokemon, BattleMove, etc.)
When on crée les value objects et interfaces manquants
Then BattlePokemon, BattleMove, BattleRequest, BattleState, ITypeChart existent dans SDK.Core
  Et IBattleEngine.RunBattle accepte BattleRequest (Player + Opponent + Config)
  Et SDK.Core build propre sans NuGet externe (CoreDependencyTests passe)
```

## AC-2 : Formules de dégâts par génération
```gherkin
Given un attaquant level 50, Attack=65, un défenseur Defense=45, move Power=40 (Normal/Physical)
When on calcule les dégâts avec Gen1DamageFormula ou StandardDamageFormula
Then le résultat est > 0, TypeMultiplier=1.0 pour Normal vs Normal
  Et Gen1DamageFormula.Generation == 1, StandardDamageFormula.Generation == 4
  Et un move Status (Power==null) retourne Damage=0
  Et ITypeChart.GetFactor(4, 3, 1) == 2.0m  (Electric vs Water, Gen1 = super efficace)
  Et ITypeChart.GetFactor(1, 1, 1) == 1.0m  (Normal vs Normal = neutre, entrée absente)
```

## AC-3 : BattleEngine RunBattle complet
```gherkin
Given deux BattlePokemon valides (Pikachu level 5 vs Bulbasaur level 5) et BattleConfig défaut
When BattleEngine.RunBattle(request) est appelé
Then le combat se termine (BattleResult non-null)
  Et TurnsElapsed >= 1 et TurnsElapsed <= 200
  Et PlayerWon == true ou false (déterministe selon stats + seed aléatoire)
  Et l'opération est synchrone et headless (pas d'I/O, pas de dépendances externes)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : Types runtime SDK.Core + IBattleEngine.RunBattle mise à jour</name>
  <files>
    src/SDK.Core/ValueObjects/BattlePokemon.cs,
    src/SDK.Core/ValueObjects/BattleMove.cs,
    src/SDK.Core/ValueObjects/BattleRequest.cs,
    src/SDK.Core/ValueObjects/BattleState.cs,
    src/SDK.Core/Interfaces/ITypeChart.cs,
    src/SDK.Core/Interfaces/IBattleEngine.cs
  </files>
  <action>
    Créer les 5 value objects/interfaces dans SDK.Core. Modifier IBattleEngine.

    **src/SDK.Core/ValueObjects/BattleMove.cs**
    ```csharp
    namespace SDK.Core.ValueObjects;
    using SDK.Core.Enums;
    public sealed record BattleMove(
        int MoveId,
        string Identifier,
        int TypeId,
        MoveCategory Category,
        int? Power,
        int Accuracy,
        int CurrentPP,
        int MaxPP);
    ```

    **src/SDK.Core/ValueObjects/BattlePokemon.cs**
    ```csharp
    namespace SDK.Core.ValueObjects;
    public sealed record BattlePokemon(
        int SpeciesId,
        string Nickname,
        int Level,
        int CurrentHp,
        int MaxHp,
        int Attack,
        int Defense,
        int SpecialAttack,
        int SpecialDefense,
        int Speed,
        int Type1Id,
        int? Type2Id,
        IReadOnlyList<BattleMove> Moves);
    ```

    **src/SDK.Core/ValueObjects/BattleRequest.cs**
    ```csharp
    namespace SDK.Core.ValueObjects;
    public sealed record BattleRequest(
        BattlePokemon Player,
        BattlePokemon Opponent,
        BattleConfig Config);
    ```

    **src/SDK.Core/ValueObjects/BattleState.cs** (D-05 : sealed record + with)
    ```csharp
    namespace SDK.Core.ValueObjects;
    using SDK.Core.Enums;
    public sealed record BattleState(
        BattlePokemon Player,
        BattlePokemon Opponent,
        int Turn,
        WeatherType Weather,
        BattleConfig Config,
        IReadOnlyList<string> Log);
    ```

    **src/SDK.Core/Interfaces/ITypeChart.cs**
    ```csharp
    namespace SDK.Core.Interfaces;
    public interface ITypeChart
    {
        decimal GetFactor(int attackerTypeId, int defenderTypeId, int generation);
    }
    ```

    **src/SDK.Core/Interfaces/IBattleEngine.cs** (modifier signature)
    ```csharp
    namespace SDK.Core.Interfaces;
    using SDK.Core.ValueObjects;
    public interface IBattleEngine
    {
        BattleResult RunBattle(BattleRequest request);
    }
    ```

    Éviter : ajouter des NuGet à SDK.Core (CoreDependencyTests échouera).
  </action>
  <verify>dotnet build src/SDK.Core/SDK.Core.csproj && dotnet test tests/SDK.Core.Tests --no-build 2>/dev/null || dotnet test tests/SDK.Core.Tests</verify>
  <done>AC-1 satisfait : 5 types créés, IBattleEngine mis à jour, 4 SDK.Core.Tests verts</done>
</task>

<task type="auto">
  <name>Task 2 : IDamageFormula × 2 + ITypeChart (TypeChart in-memory)</name>
  <files>
    src/SDK.Battle/Formulas/IDamageFormula.cs,
    src/SDK.Battle/Formulas/Gen1DamageFormula.cs,
    src/SDK.Battle/Formulas/StandardDamageFormula.cs,
    src/SDK.Battle/TypeChart.cs
  </files>
  <action>
    Créer IDamageFormula, deux implémentations, et TypeChart dans SDK.Battle.

    **src/SDK.Battle/Formulas/IDamageFormula.cs**
    ```csharp
    namespace SDK.Battle.Formulas;
    using SDK.Core.Interfaces;
    using SDK.Core.ValueObjects;
    public interface IDamageFormula
    {
        int Generation { get; }
        DamageResult Calculate(
            BattlePokemon attacker,
            BattlePokemon defender,
            BattleMove move,
            decimal typeMultiplier,
            BattleConfig config);
    }
    ```

    **src/SDK.Battle/Formulas/Gen1DamageFormula.cs**
    - `Generation = 1`
    - Si `move.Power == null` → retourner `new DamageResult(0, false, typeMultiplier)`
    - `A = Physical → attacker.Attack, Special → attacker.SpecialAttack`
    - `D = Physical → defender.Defense, Special → defender.SpecialAttack`
      (Gen 1 : la défense spéciale utilise le SpecialAttack de l'adversaire — une seule stat Special)
    - `baseDamage = (2 * attacker.Level / 5 + 2) * move.Power.Value * A / D / 50 + 2`
    - `isCritical = config.CritEnabled && Random.Shared.NextDouble() < 1.0 / 16.0`
    - `critMod = isCritical ? 2.0 : 1.0`
    - `randomFactor = Random.Shared.Next(217, 256) / 255.0`
    - `damage = Math.Max(1, (int)(baseDamage * (double)typeMultiplier * critMod * randomFactor))`
    - Retourner `new DamageResult(damage, isCritical, typeMultiplier)`

    **src/SDK.Battle/Formulas/StandardDamageFormula.cs** (Gen 4+)
    - `Generation = 4`
    - Identique mais :
      - `D = Physical → defender.Defense, Special → defender.SpecialDefense`
      - `critMod = isCritical ? 1.5 : 1.0`
      - `isCritical = config.CritEnabled && Random.Shared.NextDouble() < 1.0 / 24.0`

    **src/SDK.Battle/TypeChart.cs**
    ```csharp
    namespace SDK.Battle;
    using SDK.Core.Entities;
    using SDK.Core.Interfaces;
    public sealed class TypeChart : ITypeChart
    {
        private readonly Dictionary<(int, int, int), decimal> _chart;
        public TypeChart(IEnumerable<TypeEffectiveness> entries)
        {
            _chart = entries.ToDictionary(
                e => (e.AttackerTypeId, e.DefenderTypeId, e.Generation),
                e => e.DamageFactor);
        }
        public decimal GetFactor(int attackerTypeId, int defenderTypeId, int generation)
        {
            return _chart.TryGetValue((attackerTypeId, defenderTypeId, generation), out var factor)
                ? factor
                : 1.0m;  // défaut neutre si entrée absente (83 entrées non-neutres uniquement)
        }
    }
    ```

    Éviter : ajouter EF Core ou d'autres NuGet à SDK.Battle.csproj.
  </action>
  <verify>dotnet build src/SDK.Battle/SDK.Battle.csproj</verify>
  <done>AC-2 satisfait : Gen1DamageFormula.Generation==1, StandardDamageFormula.Generation==4, TypeChart.GetFactor retourne 1.0m pour entrée absente</done>
</task>

<task type="auto">
  <name>Task 3 : IDifficultyMode × 2 + BattleEngine</name>
  <files>
    src/SDK.Battle/Difficulty/IDifficultyMode.cs,
    src/SDK.Battle/Difficulty/StoryDifficultyMode.cs,
    src/SDK.Battle/Difficulty/HardDifficultyMode.cs,
    src/SDK.Battle/BattleEngine.cs
  </files>
  <action>
    Créer IDifficultyMode, deux stratégies IA, et BattleEngine.

    **src/SDK.Battle/Difficulty/IDifficultyMode.cs**
    ```csharp
    namespace SDK.Battle.Difficulty;
    using SDK.Core.Enums;
    using SDK.Core.ValueObjects;
    public interface IDifficultyMode
    {
        DifficultyMode Mode { get; }
        BattleMove SelectMove(BattlePokemon self, BattlePokemon opponent, BattleConfig config);
    }
    ```

    **src/SDK.Battle/Difficulty/StoryDifficultyMode.cs**
    - `Mode = DifficultyMode.Story`
    - Sélectionne un move aléatoire parmi `self.Moves` où `CurrentPP > 0`
    - Si aucun move dispo (PP tous à 0) : retourner le premier move (Struggle-like)
    - Utiliser `Random.Shared`

    **src/SDK.Battle/Difficulty/HardDifficultyMode.cs**
    - `Mode = DifficultyMode.Hard`
    - Sélectionne le move avec `Power` max parmi les moves PP > 0
    - Ties : prendre le premier
    - Si tous Status (Power == null) : retourner le premier disponible

    **src/SDK.Battle/BattleEngine.cs**
    ```csharp
    namespace SDK.Battle;
    using SDK.Battle.Difficulty;
    using SDK.Battle.Formulas;
    using SDK.Core.Interfaces;
    using SDK.Core.ValueObjects;

    public sealed class BattleEngine : IBattleEngine
    {
        private const int MaxTurns = 200;
        private readonly IDamageFormula _formula;
        private readonly IDifficultyMode _playerStrategy;
        private readonly IDifficultyMode _opponentStrategy;
        private readonly ITypeChart _typeChart;

        public BattleEngine(
            IDamageFormula formula,
            IDifficultyMode playerStrategy,
            IDifficultyMode opponentStrategy,
            ITypeChart typeChart)
        { ... }

        public BattleResult RunBattle(BattleRequest request)
        {
            var state = new BattleState(
                request.Player, request.Opponent, 0,
                WeatherType.Clear, request.Config, Array.Empty<string>());

            while (state.Turn < MaxTurns)
            {
                if (state.Player.CurrentHp <= 0)
                    return new BattleResult(false, state.Turn);
                if (state.Opponent.CurrentHp <= 0)
                    return new BattleResult(true, state.Turn);

                var playerMove = _playerStrategy.SelectMove(state.Player, state.Opponent, state.Config);
                var opponentMove = _opponentStrategy.SelectMove(state.Opponent, state.Player, state.Config);

                // Déterminer l'ordre (Speed)
                bool playerFirst = state.Player.Speed > state.Opponent.Speed
                    || (state.Player.Speed == state.Opponent.Speed && Random.Shared.Next(2) == 0);

                if (playerFirst)
                {
                    state = ApplyMove(state, isPlayer: true, playerMove);
                    if (state.Opponent.CurrentHp > 0)
                        state = ApplyMove(state, isPlayer: false, opponentMove);
                }
                else
                {
                    state = ApplyMove(state, isPlayer: false, opponentMove);
                    if (state.Player.CurrentHp > 0)
                        state = ApplyMove(state, isPlayer: true, playerMove);
                }

                state = state with { Turn = state.Turn + 1 };
            }

            return new BattleResult(false, MaxTurns, "MaxTurns");
        }

        private BattleState ApplyMove(BattleState state, bool isPlayer, BattleMove move)
        {
            var attacker = isPlayer ? state.Player : state.Opponent;
            var defender = isPlayer ? state.Opponent : state.Player;

            // Accuracy check
            if (Random.Shared.Next(0, 100) >= move.Accuracy)
                return state;  // miss

            // Type multiplier (defender peut avoir 2 types)
            var factor1 = _typeChart.GetFactor(move.TypeId, defender.Type1Id, 1);
            var factor2 = defender.Type2Id.HasValue
                ? _typeChart.GetFactor(move.TypeId, defender.Type2Id.Value, 1)
                : 1.0m;
            var typeMultiplier = factor1 * factor2;

            if (typeMultiplier == 0m)
                return state;  // immunité

            var result = _formula.Calculate(attacker, defender, move, typeMultiplier, state.Config);
            var newDefender = defender with { CurrentHp = Math.Max(0, defender.CurrentHp - result.Damage) };

            return isPlayer
                ? state with { Opponent = newDefender }
                : state with { Player = newDefender };
        }
    }
    ```

    Note generation dans ApplyMove : passer la génération issue de `state.Config` si on veut
    multi-gen. Pour l'instant, hardcoder gen=1 pour simplifier (les tests 02-04 utiliseront Gen1).
    On pourra ajouter `int Generation` à `BattleConfig` en plan 02-04 si besoin.

    Éviter : tout I/O, tout NuGet externe, toute dépendance à SDK.Data.
  </action>
  <verify>dotnet build PokemonSDK.slnx && dotnet test tests/ 2>/dev/null | tail -5</verify>
  <done>AC-3 satisfait : BattleEngine compile et implémente IBattleEngine, 17 tests existants verts</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Data/* (migrations, configurations, seeders — Plan 02-02 figé)
- tests/SDK.Data.Tests/* (13 tests existants — ne pas toucher)
- tests/SDK.Core.Tests/* (4 tests existants — ne pas casser)
- src/SDK.Core/ValueObjects/BattleConfig.cs (inchangé — pas de Pokémon dedans)
- src/SDK.Core/ValueObjects/BattleResult.cs (inchangé)
- src/SDK.Core/ValueObjects/DamageResult.cs (inchangé)
- src/SDK.Core/Interfaces/IBattlePlugin.cs (inchangé)

## SCOPE LIMITS
- Pas de tests unitaires dans ce plan (scope Plan 02-04)
- Pas de gestion des statuts (Sleep/Freeze/Burn/etc.) — Phase 5 via plugins
- Pas d'items, pas de fuite, pas de PP tracking réel dans le loop principal
- Pas de génération configurable dans BattleConfig (TODO 02-04 si besoin)
- Pas de SDK.MonoGame (Phase 3)
- Pas de dépendance SDK.Data dans SDK.Battle — le TypeChart reçoit IEnumerable<TypeEffectiveness> depuis l'extérieur

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] dotnet build PokemonSDK.slnx passe sans erreur
- [ ] dotnet test tests/SDK.Core.Tests — 4/4 verts (CoreDependencyTests inclus)
- [ ] dotnet test tests/SDK.Data.Tests — 13/13 verts
- [ ] SDK.Battle.csproj ne référence aucun NuGet externe (zéro PackageReference)
- [ ] IBattleEngine.RunBattle(BattleRequest) — signature correcte
- [ ] BattleState est un sealed record (D-05)
- [ ] Gen1DamageFormula.Generation == 1, StandardDamageFormula.Generation == 4
</verification>

<success_criteria>
- Toutes les tâches complétées
- Build propre PokemonSDK.slnx
- 17/17 tests verts (aucune régression)
- SDK.Battle zéro NuGet externe
- BattleEngine.RunBattle exécutable headless (pas d'I/O)
</success_criteria>

<output>
Après complétion, créer `.paul/phases/02-battle-engine-core/02-03-SUMMARY.md`
</output>

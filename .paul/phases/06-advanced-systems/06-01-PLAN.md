---
phase: 06-advanced-systems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - REQUIREMENTS.md
  - CLAUDE.md
  - src/SDK.Tools/Validation/SpriteValidator.cs
  - src/SDK.Core/Entities/FakemonSpecies.cs
  - src/SDK.Data/Models/FakemonSpeciesConfiguration.cs
  - src/SDK.Data/PokemonDbContext.cs
  - src/SDK.Data/Migrations/(nouveau timestamp)_AddFakemonSpecies.cs
  - src/SDK.Data/Seeding/FakemonDataSeeder.cs
autonomous: true
---

<objective>
## Goal
Corriger les docs de planification (REQUIREMENTS.md ADV-03/04, CLAUDE.md D-16), créer l'entité `FakemonSpecies`, la migration EF Core `AddFakemonSpecies`, et un seeder avec 1 Fakemon sample D-22 (6 translations).

## Purpose
Socle données Phase 6 : sans cette migration, les plans 06-02 et 06-03 (FakemonAssembler, CLI) n'ont pas de table cible. Les corrections doc évitent un D-16 blocker CI avant le prochain re-run Phase 7.

## Output
- `REQUIREMENTS.md` traceability ADV-03/04 corrigée (Post-v1.0 → v0.3)
- `CLAUDE.md` D-16 mis à jour (pattern `fk_`) + D-22 note Fakemon
- `SpriteValidator.cs` regex étendu pour `fk_` prefix
- `FakemonSpecies` entity (SDK.Core) + EF config (SDK.Data) + migration + seeder
- `dotnet test` vert — 0 régression
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/SDK.Core/Entities/PokemonSpecies.cs
@src/SDK.Data/PokemonDbContext.cs
@src/SDK.Tools/Validation/SpriteValidator.cs
@CLAUDE.md
@REQUIREMENTS.md
</context>

<acceptance_criteria>

## AC-1: REQUIREMENTS.md corrigé
```gherkin
Given ADV-03 et ADV-04 sont marqués "Post-v1.0" dans REQUIREMENTS.md
When on corrige la traceability table
Then ADV-03 et ADV-04 montrent "v0.3 / Phase 6" dans la colonne version
```

## AC-2: D-16 et SpriteValidator mis à jour
```gherkin
Given SpriteValidator accepte seulement "{dexid5}_{identifier}_{view}.png"
When on met à jour le regex et CLAUDE.md D-16
Then SpriteValidator accepte aussi "fk_{identifier}_{view}.png"
  Et CLAUDE.md D-16 documente les deux patterns
```

## AC-3: Migration AddFakemonSpecies appliquée
```gherkin
Given SDK.Data sans table fakemon_species
When dotnet ef migrations add AddFakemonSpecies && dotnet ef database update
Then table fakemon_species existe avec colonnes (Id, Identifier, Generation, BaseHp, BaseAttack, BaseDefense, BaseSpecialAtk, BaseSpecialDef, BaseSpeed, Type1Id, Type2Id, EggGroup1, EggGroup2, IsLegendary, PartsManifest)
  Et FK Type1Id → PokemonTypes(Id)
  Et FK Type2Id → PokemonTypes(Id) nullable
```

## AC-4: FakemonDataSeeder D-22 valide
```gherkin
Given FakemonDataSeeder.Seed() avec 1 Fakemon sample "test-dragon"
When on exécute le seeder
Then fakemon_species contient 1 row "test-dragon"
  Et translations contient 6 rows EntityType="FakemonSpecies" (en/es/fr/de/it/ja)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Corrections docs (REQUIREMENTS.md + CLAUDE.md D-16 + SpriteValidator)</name>
  <files>REQUIREMENTS.md, CLAUDE.md, src/SDK.Tools/Validation/SpriteValidator.cs</files>
  <action>
    **REQUIREMENTS.md** : Dans la traceability table, trouver les lignes ADV-03 et ADV-04.
    Changer la colonne version/milestone de "Post-v1.0" → "v0.3 / Phase 6".

    **CLAUDE.md** :
    - Décision D-16 : étendre la description pour inclure le pattern Fakemon.
      Avant : `{dexid5}_{identifier}_{view}.png` convention stricte
      Après : ajouter "Fakemons: `fk_{identifier}_{view}.png` dans `assets/sprites/fakemons/`"
    - Mettre à jour le regex mentionné dans D-23 si présent pour inclure `fk_`.
    - Ajouter D-22 note sur Fakemons : "Fakemon translations : 6 locales obligatoires, EntityType='FakemonSpecies'"

    **SpriteValidator.cs** : Trouver le regex de validation de nommage des sprites.
    Le regex actuel valide `{dexid5}_{identifier}_{view}.png`.
    Étendre pour accepter AUSSI `fk_{identifier}_{view}.png` via une alternance regex.
    Pattern cible : `^(\d{5}_[a-z0-9-]+|fk_[a-z0-9-]+)_(front|back|overworld|portrait|icon)\.png$`
    Éviter : ne pas casser la validation existante pour les sprites officiels.
  </action>
  <verify>
    dotnet build src/SDK.Tools/SDK.Tools.csproj → 0 erreur.
    grep "fk_" CLAUDE.md → présent.
    grep "v0.3" REQUIREMENTS.md → ADV-03 et ADV-04 visibles.
  </verify>
  <done>AC-1 + AC-2 satisfaits : REQUIREMENTS.md corrigé, D-16 mis à jour, SpriteValidator étendu.</done>
</task>

<task type="auto">
  <name>Task 2: FakemonSpecies entity + EF migration + FakemonDataSeeder</name>
  <files>
    src/SDK.Core/Entities/FakemonSpecies.cs,
    src/SDK.Data/Models/FakemonSpeciesConfiguration.cs,
    src/SDK.Data/PokemonDbContext.cs,
    src/SDK.Data/Seeding/FakemonDataSeeder.cs
  </files>
  <action>
    **src/SDK.Core/Entities/FakemonSpecies.cs** (nouvelle entité) :
    ```csharp
    namespace SDK.Core.Entities;
    public class FakemonSpecies
    {
        public int Id { get; set; }
        public string Identifier { get; set; } = string.Empty;  // unique, ex: "blaziken-eagle"
        public int Generation { get; set; }                      // D-09
        public int BaseHp { get; set; }
        public int BaseAttack { get; set; }
        public int BaseDefense { get; set; }
        public int BaseSpecialAtk { get; set; }
        public int BaseSpecialDef { get; set; }
        public int BaseSpeed { get; set; }
        public int Type1Id { get; set; }
        public int? Type2Id { get; set; }
        public string EggGroup1 { get; set; } = string.Empty;   // ex: "field", "fairy"
        public string? EggGroup2 { get; set; }
        public bool IsLegendary { get; set; }
        public string? PartsManifest { get; set; }               // JSON nullable: [{Path,ZOrder}]
        public PokemonType? Type1 { get; set; }
        public PokemonType? Type2 { get; set; }
    }
    ```

    **src/SDK.Data/Models/FakemonSpeciesConfiguration.cs** (EF Fluent API) :
    - `ToTable("fakemon_species")`
    - `Identifier` : `IsRequired`, `HasMaxLength(100)`, `IsUnique`
    - `EggGroup1` : `IsRequired`, `HasMaxLength(50)`
    - `EggGroup2` : nullable, `HasMaxLength(50)`
    - FK `Type1Id` → `PokemonTypes` (required), `OnDelete(DeleteBehavior.Restrict)`
    - FK `Type2Id` → `PokemonTypes` (nullable), `OnDelete(DeleteBehavior.Restrict)`
    - `PartsManifest` : `HasColumnType("TEXT")`, nullable

    **PokemonDbContext.cs** : Ajouter `public DbSet<FakemonSpecies> FakemonSpecies => Set<FakemonSpecies>();`
    Appeler `modelBuilder.ApplyConfiguration(new FakemonSpeciesConfiguration())` dans `OnModelCreating`.

    **Migration** : Depuis la racine du repo :
    ```
    dotnet ef migrations add AddFakemonSpecies \
      --project src/SDK.Data \
      --startup-project src/SDK.Data
    ```
    Vérifier que le fichier généré contient `CreateTable("fakemon_species", ...)` avec les 2 FKs vers `PokemonTypes`.
    Puis : `dotnet ef database update --project src/SDK.Data --startup-project src/SDK.Data`

    **src/SDK.Data/Seeding/FakemonDataSeeder.cs** (nouveau fichier) :
    - Méthode `public static void Seed(PokemonDbContext ctx)`
    - Insérer 1 FakemonSpecies sample : `Identifier="test-dragon"`, `Generation=1`,
      `BaseHp=45`, `BaseAttack=60`, `BaseDefense=55`, `BaseSpecialAtk=70`, `BaseSpecialDef=55`, `BaseSpeed=50`,
      `Type1Id=<id Dragon si existe, sinon 1>`, `IsLegendary=false`, `EggGroup1="dragon"`, `PartsManifest=null`
    - Insérer 6 translations D-22 : EntityType="FakemonSpecies", EntityId=id du sample,
      Locale: en="Test Dragon", es="Dragón Prueba", fr="Dragon Test", de="Testdrache", it="Drago Test", ja="テストドラゴン"
    - Guard : si `ctx.FakemonSpecies.Any()` → return (idempotent)
    - Éviter : `SaveChanges` partiel — tout en une transaction (ou laisser EF gérer).
  </action>
  <verify>
    dotnet ef database update --project src/SDK.Data --startup-project src/SDK.Data → success.
    dotnet test tests/SDK.Data.Tests/ --no-build --configuration Release → vert.
    sqlite3 src/SDK.Data/data/PokemonSDK.db ".tables" | grep fakemon → "fakemon_species" présent.
  </verify>
  <done>AC-3 + AC-4 satisfaits : migration appliquée, seeder insère 1 Fakemon + 6 translations.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Core/Interfaces/* — aucune modification (INarrationPlugin = Plan 06-04)
- src/SDK.Battle/* — hors scope
- Migrations existantes 20260602→20260606* — ne jamais modifier migrations existantes
- src/SDK.Data/Seeding/DataSeeder.cs — seeder officiel Pokédex, ne pas toucher

## SCOPE LIMITS
- Pas de FakemonAssembler ni CLI fakemon (06-02, 06-03)
- Pas de TTS (06-04, 06-05)
- EggGroup = string TEXT — pas de table egg_groups (simplification volontaire Phase 6)
- Ne pas ajouter EggGroup à PokemonSpecies (hors scope Phase 6)

</boundaries>

<verification>
- [ ] dotnet build PokemonSDK.slnx --configuration Release → 0 erreur, 0 warning nouveau
- [ ] dotnet test PokemonSDK.slnx --configuration Release → tous verts (aucune régression)
- [ ] grep "fk_" CLAUDE.md → présent (D-16 mis à jour)
- [ ] grep "v0.3" REQUIREMENTS.md | grep "ADV-03\|ADV-04" → présent
- [ ] sqlite3 data/PokemonSDK.db ".schema fakemon_species" → table avec toutes les colonnes
</verification>

<success_criteria>
- REQUIREMENTS.md ADV-03/ADV-04 marqués v0.3
- CLAUDE.md D-16 documente pattern fk_
- SpriteValidator accepte fk_{identifier}_{view}.png
- Migration AddFakemonSpecies appliquée sans erreur
- FakemonDataSeeder insère 1 row + 6 translations D-22
- dotnet test → 0 régression
</success_criteria>

<output>
After completion, create `.paul/phases/06-advanced-systems/06-01-SUMMARY.md`
</output>

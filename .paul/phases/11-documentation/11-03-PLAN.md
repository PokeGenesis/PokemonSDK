---
phase: 11-documentation
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - docs/docs/guides/index.md
  - docs/docs/guides/battle-engine.md
  - docs/docs/guides/lua-scripting.md
  - docs/docs/guides/plugins.md
  - docs/docs/guides/asset-pipeline.md
  - docs/docs/guides/rendering-hd.md
  - docs/docs/guides/tts-narration.md
  - docs/docs/guides/fakemons.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/index.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/battle-engine.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/lua-scripting.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/plugins.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/asset-pipeline.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/rendering-hd.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/tts-narration.md
  - docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/fakemons.md
  - docs/sidebars.ts
  - docs/i18n/fr/docusaurus-plugin-content-docs/current.json
autonomous: true
---

<objective>
## Goal
Create a bilingual Guides section (8 pages EN + 8 pages FR) covering the 7 major SDK feature areas: BattleEngine, Lua scripting, plugins, asset pipeline, HD rendering, TTS narration, and Fakemons. Wire the Guides sidebar category between Tutorial and Packages.

## Purpose
The Tutorial (11-02) teaches the 30-minute path from zero to running game. Guides fill the next layer: task-oriented how-to pages answering "how do I do X in my own project?" for each major SDK subsystem. Required before 11-04 API reference.

## Output
- 8 EN guide pages under `docs/docs/guides/`
- 8 FR guide mirrors under `docs/i18n/fr/.../guides/`
- Guides category in `sidebars.ts` (position: after Tutorial, before Packages)
- "Guides" label in FR `current.json`
- Build exits 0 EN+FR, 0 broken links, 0 em dashes
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/11-documentation/11-02-SUMMARY.md

## Source Files
@docs/sidebars.ts
@docs/i18n/fr/docusaurus-plugin-content-docs/current.json
</context>

<acceptance_criteria>

## AC-1: 8 EN guide pages with real content
```gherkin
Given docs/docs/guides/ does not exist
When Task 1 completes
Then 8 files exist: index.md + 7 topic guides
  And each page has at least one C# or Lua code block showing real SDK usage
  And no page contains placeholder text ("TODO", "Coming soon", "stub")
  And 0 em dashes appear in any file (grep -r "—" docs/docs/guides/ = empty)
```

## AC-2: 8 FR guide pages with bilingual parity
```gherkin
Given 8 EN guides exist
When Task 2 completes
Then 8 FR mirrors exist under docs/i18n/fr/.../guides/
  And each FR file has identical structure (same sections, same code blocks)
  And prose is translated to French (titles, paragraphs)
  And code blocks and identifiers remain in English
  And 0 em dashes in any FR file
```

## AC-3: Guides category wired in sidebar + build passes
```gherkin
Given sidebars.ts and current.json are updated
When BASE_URL=/ npm run build runs (both locales)
Then build exits 0 with 0 errors and 0 broken links
  And the Guides category appears between Tutorial and Packages in the sidebar
  And FR sidebar label reads "Guides"
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 EN guide pages</name>
  <files>
    docs/docs/guides/index.md,
    docs/docs/guides/battle-engine.md,
    docs/docs/guides/lua-scripting.md,
    docs/docs/guides/plugins.md,
    docs/docs/guides/asset-pipeline.md,
    docs/docs/guides/rendering-hd.md,
    docs/docs/guides/tts-narration.md,
    docs/docs/guides/fakemons.md
  </files>
  <action>
    Create `docs/docs/guides/` directory and write 8 guide pages.

    STYLE RULES (CLAUDE.md §11, D-21):
    - Zero em dashes (— or —>) anywhere in prose, titles, tables. Use : or , instead.
    - No placeholder text. Every section has real content.
    - Docs cover stable public surface only (D-21). Internal implementation classes not covered.
    - sidebar_position frontmatter required on every file.

    LINK RULE (Docusaurus quirk from 11-02):
    - Links FROM guides/index.md use absolute doc IDs: `guides/battle-engine` not `./battle-engine`
      because index.md is served at /docs/guides (no trailing slash) — relative `./battle-engine`
      would resolve to /docs/battle-engine (wrong).
    - Links FROM other guide pages can use relative slugs: `./lua-scripting`

    --- FILE: docs/docs/guides/index.md ---
    sidebar_position: 1
    Title: "Guides"
    Intro: "Task-oriented guides for integrating each PokemonSDK subsystem into your fan game. The Tutorial covers the 30-minute path from zero to running game. These guides go deeper on each feature area."
    Table of contents linking all 7 guides with a one-line description each.
    Use absolute doc IDs: guides/battle-engine, guides/lua-scripting, etc.

    --- FILE: docs/docs/guides/battle-engine.md ---
    sidebar_position: 2
    Title: "BattleEngine: Wire 1v1 Combat"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.Battle
    2. "Configure formulas and difficulty" — StandardDamageFormula, Gen1DamageFormula, NormalDifficultyMode, HardDifficultyMode, BattleConfig constructor
    3. "Run a battle loop" — BattleState.Start(), engine.ExecuteTurn(), state.IsOver, state.Winner
    4. "Read battle events" — brief note that BattleState.LastEvent carries move/damage/status info
    5. "Integrate with MonoGame" — call ExecuteTurn in Update(), render state in Draw()
    C# code blocks showing real SDK types from SDK.Battle package.
    Mention BattleState immuable record (D-05): with-expressions, state never mutated.

    --- FILE: docs/docs/guides/lua-scripting.md ---
    sidebar_position: 3
    Title: "Lua Scripting: NPC Events and Badges"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.Scripting
    2. "Create the engine" — new LuaScriptEngine(gameState, saveSystem)
    3. "Write a script" — Lua example: game.give_badge, game.set_flag, player.heal_party, game.save
    4. "Sandbox constraints" — SoftSandbox: no os, no io, no network; standard Lua library available
    5. "GameState flags" — game.set_flag("KEY", true), game.get_flag("KEY"), typed flags
    6. "Hot reload (Debug)" — LuaHotReloader re-runs script on file change, <500ms
    Real Lua code blocks. Mention D-04 (Preset_SoftSandbox) and D-12 (GameState flags).

    --- FILE: docs/docs/guides/plugins.md ---
    sidebar_position: 4
    Title: "Plugins: Extend the Battle Engine"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.Plugins
    2. "IBattlePlugin interface" — brief interface listing: OnBattleStart, OnTurnStart, OnDamageApplied, OnBattleEnd
    3. "Implement a plugin" — C# example: class MyPlugin : IBattlePlugin with one hook
    4. "Register with PluginRegistry" — registry.Register(new MyPlugin()), registry.ApplyAll(state)
    5. "Bundled plugins" — NuzlockePlugin (permadeath), RandomizerPlugin (seed-deterministic species), TurboPlugin (TextSpeedMultiplier)
    Mention D-13: Nuzlocke/Randomizer/Turbo are plugins, never hardcoded modes.

    --- FILE: docs/docs/guides/asset-pipeline.md ---
    sidebar_position: 5
    Title: "Asset Pipeline: Sprites and Atlases"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.Tools
    2. "Sprite naming convention" — D-16 rule: {dexid5}_{identifier}_{view}.png, views: front/back/overworld/portrait/icon, icon = 32x32 (D-23)
    3. "Validate sprites" — pokeforge asset-sync (or CLI asset-validate), what errors look like
    4. "Generate atlas" — AtlasPacker output: sprites.png + import.json
    5. "Sync to SQLite" — SqliteSyncer writes sprite_path to DB
    6. "CLI asset-sync exit codes" — exit 1 if any ERROR, exit 0 if only WARN
    Include the naming regex from D-16 in a code block.

    --- FILE: docs/docs/guides/rendering-hd.md ---
    sidebar_position: 6
    Title: "HD Rendering: 480x270 to 1080p"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.MonoGame
    2. "Resolution contract" — D-14: internal 480x270, xBR x4 upscale to 1920x1080
    3. "RenderPipeline setup" — new RenderPipeline(graphicsDevice), pass to Game1
    4. "3-pass Draw()" — Pass 1: render world to RenderTarget2D at 480x270; Pass 2: apply xBR shader; Pass 3: overlay UI at native resolution
    5. "Day/night tint" — DayNight shader tints based on IGameClock.Hour; configure via DayNightConfig
    6. "Shader fallback" — shaders compiled via MGCB; PointClamp fallback if MGCB unavailable
    Mention D-15 (xBR shader).

    --- FILE: docs/docs/guides/tts-narration.md ---
    sidebar_position: 7
    Title: "TTS Narration: Voice Dialogue"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.Plugins.TTS
    2. "Prerequisites" — piper binary + aplay (Linux) or equivalent; pokeforge doctor to verify
    3. "Register PiperNarrationPlugin" — DI registration, DoctorCommand.TtsCheck
    4. "Lua API" — sdk.tts.speak("text"), sdk.tts.stop(), sdk.tts.is_speaking()
    5. "SdkGlobals pattern" — sdk.tts.speak() uses SdkGlobals with property tts (lowercase for MoonSharp)
    6. "Custom backend" — implement INarrationPlugin, see Advanced APIs / INarrationPlugin
    Link to advanced/narration-plugin for full interface details.

    --- FILE: docs/docs/guides/fakemons.md ---
    sidebar_position: 8
    Title: "Fakemons: Custom Species"
    Sections:
    1. "Install" — dotnet add package PokeForge.SDK.Tools
    2. "Sprite naming" — D-16: fk_{identifier}_{view}.png under assets/sprites/fakemons/
    3. "List available parts" — pokeforge fakemon list-parts, what a catalog scan returns
    4. "Assemble a sprite" — pokeforge fakemon assemble --base fk_dragon_body --head fk_cat_head --output fk_mymon_front.png
    5. "Export to SQLite" — FakemonExporter writes FakemonSpecies entity + D-22 translations (6 locales)
    6. "FakemonAssemblyPipeline in code" — C# usage: FakemonPartsCatalog.Scan(), FakemonFilter, FakemonAssembler, FakemonExporter
    Mention D-22 (6 locales required on export) and fk_ prefix rule from D-16.
  </action>
  <verify>
    grep -r "—" docs/docs/guides/ | wc -l  # must be 0
    grep -r "TODO\|Coming soon\|stub\|placeholder" docs/docs/guides/ | wc -l  # must be 0
    ls docs/docs/guides/ | wc -l  # must be 8
  </verify>
  <done>AC-1 satisfied: 8 EN guide pages with real SDK content, 0 em dashes, 0 placeholders</done>
</task>

<task type="auto">
  <name>Task 2: FR mirrors + sidebar Guides category + i18n label</name>
  <files>
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/index.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/battle-engine.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/lua-scripting.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/plugins.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/asset-pipeline.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/rendering-hd.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/tts-narration.md,
    docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/fakemons.md,
    docs/sidebars.ts,
    docs/i18n/fr/docusaurus-plugin-content-docs/current.json
  </files>
  <action>
    PART A: FR mirrors

    Create `docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/` and write 8 FR files.

    Rules:
    - Same sidebar_position frontmatter as EN counterpart
    - Same structure: same H2 sections, same code blocks
    - Code blocks and identifiers stay in English (C#, Lua, CLI commands)
    - Prose (titles, paragraphs, table cells) translated to French
    - Zero em dashes (— or —>) in prose
    - Links: same pattern as EN (absolute doc IDs from index, relative slugs from topic pages)

    Titles:
    - index: "Guides"
    - battle-engine: "BattleEngine : combat 1v1"
    - lua-scripting: "Scripting Lua : événements NPC et badges"
    - plugins: "Plugins : étendre le moteur de combat"
    - asset-pipeline: "Pipeline d'assets : sprites et atlas"
    - rendering-hd: "Rendu HD : 480x270 vers 1080p"
    - tts-narration: "Narration TTS : dialogue vocal"
    - fakemons: "Fakemons : espèces personnalisées"

    Note on colon placement in French titles: no space before colon (FR typography uses
    non-breaking space before colon in body text, but Docusaurus page titles render
    the H1 as-is). Use a regular colon `:`  with a space after, no em dash.

    PART B: sidebars.ts

    Insert Guides category between Tutorial and Packages:
    ```typescript
    {
      type: 'category',
      label: 'Guides',
      collapsed: false,
      items: [
        'guides/index',
        'guides/battle-engine',
        'guides/lua-scripting',
        'guides/plugins',
        'guides/asset-pipeline',
        'guides/rendering-hd',
        'guides/tts-narration',
        'guides/fakemons',
      ],
    },
    ```

    PART C: current.json

    Add Guides entry after the Tutorial entry:
    ```json
    "sidebar.docsSidebar.category.Guides": {
      "message": "Guides",
      "description": "Libellé de la catégorie Guides dans la barre latérale"
    }
    ```
  </action>
  <verify>
    ls docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/ | wc -l  # must be 8
    grep -r "—" docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/ | wc -l  # must be 0
    grep '"Guides"' docs/sidebars.ts | wc -l  # must be >= 1
    grep '"Guides"' docs/i18n/fr/docusaurus-plugin-content-docs/current.json | wc -l  # must be 1
  </verify>
  <done>AC-2 + partial AC-3 satisfied: 8 FR mirrors complete, sidebar wired, i18n label added</done>
</task>

<task type="auto">
  <name>Task 3: Build verify EN+FR + em dash audit + commit</name>
  <files>none (verification only)</files>
  <action>
    1. Build EN: cd docs && BASE_URL=/ npm run build
       - Must exit 0
       - 0 broken links (onBrokenLinks: 'throw')
       - 0 TypeScript errors

    2. Build FR: cd docs && BASE_URL=/ npm run build -- --locale fr
       - Must exit 0

    3. Em dash audit across all guide files:
       grep -r "—" docs/docs/guides/ docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/
       Must return 0 matches. Fix any found.

    4. Commit: git add docs/ && git commit -m "docs(phase11): 11-03 Wave 3 — guides section EN+FR (8 pages, sidebar, i18n)"
       Wait — the commit message itself contains em dashes inside the string, which is acceptable
       as it is a git commit message (not a markdown doc). Proceed.
       Actually per CLAUDE.md §11 the rule is for .md files. Commit messages are fine with em dashes.
  </action>
  <verify>
    cd docs && BASE_URL=/ npm run build 2>&1 | tail -5  # must show "Generated static files"
    echo "Exit code: $?"  # must be 0
    grep -r "—" docs/docs/guides/ docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/ | wc -l  # must be 0
  </verify>
  <done>AC-3 satisfied: build exits 0 EN+FR, 0 broken links, 0 em dashes in guides</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- docs/docs/tutorial/ (Tutorial complete, 11-02 closed)
- docs/i18n/fr/docusaurus-plugin-content-docs/current/tutorial/ (same)
- docs/docs/packages/ (reference stubs complete from 11-01)
- docs/docs/cli/ (CLI reference stubs complete from 11-01)
- docs/docs/advanced/ (Advanced APIs stubs complete from 11-01)
- docs/docusaurus.config.ts (baseUrl logic stable, do not touch)
- docs/tsconfig.json (fixed in 11-01, do not touch)
- Any C# source files under src/ or tests/ (docs phase, no code changes)

## SCOPE LIMITS
- No API reference section yet (deferred to 11-04)
- No new sidebar categories beyond Guides
- No changes to existing package/CLI/advanced stubs
- FR mirrors cover the same 8 guides — no new FR pages outside guides/

</boundaries>

<verification>
Before declaring plan complete:
- [ ] ls docs/docs/guides/ shows 8 files
- [ ] ls docs/i18n/fr/.../guides/ shows 8 files
- [ ] grep "Guides" docs/sidebars.ts returns a match
- [ ] BASE_URL=/ npm run build exits 0 (EN)
- [ ] BASE_URL=/ npm run build -- --locale fr exits 0 (FR)
- [ ] grep -r "—" docs/docs/guides/ docs/i18n/fr/.../guides/ returns 0 lines
- [ ] git log shows commit for 11-03
</verification>

<success_criteria>
- All 3 tasks completed
- 16 guide files created (8 EN + 8 FR)
- sidebars.ts updated with Guides category
- current.json updated with Guides label
- Build exits 0 EN+FR
- 0 em dashes, 0 placeholders, 0 broken links
</success_criteria>

<output>
After completion, create `.paul/phases/11-documentation/11-03-SUMMARY.md`
</output>

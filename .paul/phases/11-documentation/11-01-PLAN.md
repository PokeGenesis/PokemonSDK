---
phase: 11-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docusaurus.config.ts
  - docs/package.json
  - docs/sidebars.ts
  - docs/docs/intro.md
  - docs/docs/packages/index.md
  - docs/static/img/.gitkeep
  - .github/workflows/deploy-docs.yml
autonomous: false
---

<objective>
## Goal
Scaffolder le site Docusaurus dans `docs/` et le déployer automatiquement sur GitHub Pages à chaque push sur `main`.

## Purpose
Wave 1 de Phase 11 : infrastructure docs. Sans ce scaffold, les Waves 2/3/4 n'ont nulle part où écrire. Respecte D-21 : seules les APIs stables sont documentées (8 packages NuGet + CLI + INarrationPlugin + FakemonAssemblyPipeline).

## Output
- `docs/` — site Docusaurus fonctionnel buildable en local
- `.github/workflows/deploy-docs.yml` — deploy auto GitHub Pages sur push main
- Sidebar D-21 définie : arborescence des 8 packages + CLI + APIs avancées
- Checkpoint humain : build local OK + structure sidebar validée
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## D-21 Scope (APIs stables uniquement)
8 packages NuGet publics :
1. PokeForge.SDK.Core — entités, interfaces, value objects
2. PokeForge.SDK.Data — EF Core 10 + SQLite, migrations
3. PokeForge.SDK.Battle — moteur 1v1, BattleConfig, IDamageFormula, IDifficultyMode
4. PokeForge.SDK.Scripting — MoonSharp SoftSandbox, GameState, ISaveSystem
5. PokeForge.SDK.MonoGame — RenderPipeline HD, WorldSystem
6. PokeForge.SDK.Tools — SpriteValidator, AtlasPacker, SqliteSyncer, FakemonAssemblyPipeline
7. PokeForge.SDK.Plugins (Nuzlocke + Randomizer + Turbo)
8. PokeForge.SDK.Plugins.TTS — INarrationPlugin, NarrationQueue

CLI pokeforge : seed / doctor / fakemon list-parts+assemble / asset-sync / asset-validate
APIs avancées : INarrationPlugin, FakemonAssemblyPipeline, FakemonPartsCatalog
</context>

<acceptance_criteria>

## AC-1: Build Docusaurus local OK
```gherkin
Given docs/ est scaffoldé avec Node.js + Docusaurus 3
When on lance `npm run build` dans docs/
Then exit code 0, dossier docs/build/ créé, zero error TypeScript/MDX
```

## AC-2: Sidebar D-21 présente
```gherkin
Given le site est démarré avec `npm start`
When on ouvre http://localhost:3000
Then la sidebar liste : Getting Started, Packages (8 entrées), CLI, Advanced APIs
```

## AC-3: Workflow deploy-docs.yml valide
```gherkin
Given .github/workflows/deploy-docs.yml est committé
When on lance `gh workflow view deploy-docs` ou on pousse sur main
Then le workflow est lisible par GitHub Actions, cible gh-pages, steps node-setup + build + deploy présents
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : Scaffold Docusaurus 3 dans docs/</name>
  <files>
    docs/docusaurus.config.ts,
    docs/package.json,
    docs/tsconfig.json,
    docs/sidebars.ts,
    docs/docs/intro.md,
    docs/docs/packages/index.md,
    docs/docs/cli/index.md,
    docs/docs/advanced/index.md,
    docs/static/img/.gitkeep,
    docs/.gitignore
  </files>
  <action>
    Créer `docs/` avec un scaffold Docusaurus 3 minimal (PAS npx create — créer les fichiers à la main pour contrôle total) :

    **docs/package.json** :
    ```json
    {
      "name": "pokemonsdk-docs",
      "version": "0.0.1",
      "private": true,
      "scripts": {
        "docusaurus": "docusaurus",
        "start": "docusaurus start",
        "build": "docusaurus build",
        "swizzle": "docusaurus swizzle",
        "deploy": "docusaurus deploy",
        "clear": "docusaurus clear",
        "serve": "docusaurus serve",
        "write-translations": "docusaurus write-translations",
        "write-heading-ids": "docusaurus write-heading-ids"
      },
      "dependencies": {
        "@docusaurus/core": "3.8.1",
        "@docusaurus/preset-classic": "3.8.1",
        "@mdx-js/react": "^3.0.0",
        "clsx": "^2.0.0",
        "prism-react-renderer": "^2.3.0",
        "react": "^18.0.0",
        "react-dom": "^18.0.0"
      },
      "devDependencies": {
        "@docusaurus/types": "3.8.1",
        "typescript": "~5.6.2"
      },
      "engines": {
        "node": ">=20.0"
      }
    }
    ```

    **docs/docusaurus.config.ts** :
    ```typescript
    import { themes as prismThemes } from 'prism-react-renderer';
    import type { Config } from '@docusaurus/types';
    import type * as Preset from '@docusaurus/preset-classic';

    const config: Config = {
      title: 'PokemonSDK',
      tagline: 'SDK open-source C# / .NET 10 pour fan-games Pokémon',
      favicon: 'img/favicon.ico',
      url: 'https://PokeGenesis.github.io',
      baseUrl: '/PokemonSDK/',
      organizationName: 'PokeGenesis',
      projectName: 'PokemonSDK',
      trailingSlash: false,
      onBrokenLinks: 'throw',
      onBrokenMarkdownLinks: 'warn',
      i18n: { defaultLocale: 'en', locales: ['en'] },
      presets: [
        [
          'classic',
          {
            docs: {
              sidebarPath: './sidebars.ts',
              editUrl: 'https://github.com/PokeGenesis/PokemonSDK/tree/main/docs/',
            },
            blog: false,
            theme: { customCss: './src/css/custom.css' },
          } satisfies Preset.Options,
        ],
      ],
      themeConfig: {
        navbar: {
          title: 'PokemonSDK',
          items: [
            { type: 'docSidebar', sidebarId: 'docsSidebar', position: 'left', label: 'Docs' },
            { href: 'https://github.com/PokeGenesis/PokemonSDK', label: 'GitHub', position: 'right' },
          ],
        },
        footer: {
          style: 'dark',
          links: [
            {
              title: 'Docs',
              items: [{ label: 'Getting Started', to: '/docs/intro' }],
            },
            {
              title: 'Community',
              items: [{ label: 'GitHub', href: 'https://github.com/PokeGenesis/PokemonSDK' }],
            },
          ],
          copyright: `Copyright © ${new Date().getFullYear()} PokeGenesis. MIT License.`,
        },
        prism: {
          theme: prismThemes.github,
          darkTheme: prismThemes.dracula,
          additionalLanguages: ['csharp', 'lua', 'bash'],
        },
      } satisfies Preset.ThemeConfig,
    };

    export default config;
    ```

    **docs/sidebars.ts** (structure D-21) :
    ```typescript
    import type { SidebarsConfig } from '@docusaurus/plugin-content-docs';

    const sidebars: SidebarsConfig = {
      docsSidebar: [
        'intro',
        {
          type: 'category',
          label: 'Packages',
          collapsed: false,
          items: [
            'packages/index',
            'packages/core',
            'packages/data',
            'packages/battle',
            'packages/scripting',
            'packages/monogame',
            'packages/tools',
            'packages/plugins',
            'packages/plugins-tts',
          ],
        },
        {
          type: 'category',
          label: 'CLI pokeforge',
          items: [
            'cli/index',
            'cli/seed',
            'cli/doctor',
            'cli/fakemon',
            'cli/asset-sync',
          ],
        },
        {
          type: 'category',
          label: 'Advanced APIs',
          items: [
            'advanced/index',
            'advanced/narration-plugin',
            'advanced/fakemon-pipeline',
            'advanced/fakemon-catalog',
          ],
        },
      ],
    };

    export default sidebars;
    ```

    **docs/tsconfig.json** :
    ```json
    {
      "extends": "@docusaurus/tsconfig",
      "compilerOptions": {
        "baseUrl": "."
      }
    }
    ```

    **docs/src/css/custom.css** :
    ```css
    :root {
      --ifm-color-primary: #e63946;
      --ifm-color-primary-dark: #d62d3a;
      --ifm-color-primary-darker: #ca2a37;
      --ifm-color-primary-darkest: #a7222d;
      --ifm-color-primary-light: #ea505c;
      --ifm-color-primary-lighter: #ec5c68;
      --ifm-color-primary-lightest: #f07882;
      --ifm-code-font-size: 95%;
    }
    ```

    **docs/docs/intro.md** — landing page stub :
    ```markdown
    ---
    sidebar_position: 1
    ---

    # PokemonSDK

    SDK open-source C# / .NET 10 pour fan-games Pokémon.

    ## Install

    ```bash
    dotnet add package PokeForge.SDK.Core
    dotnet add package PokeForge.SDK.Battle
    dotnet add package PokeForge.SDK.Scripting
    ```

    ## Quick Start

    See the [Getting Started tutorial](/docs/tutorial/intro) (Wave 2).

    ## Packages

    | Package | Description |
    |---------|-------------|
    | PokeForge.SDK.Core | Entities, interfaces, value objects — zero NuGet dependencies |
    | PokeForge.SDK.Data | EF Core 10 + SQLite, migrations, seeding |
    | PokeForge.SDK.Battle | 1v1 headless battle engine |
    | PokeForge.SDK.Scripting | MoonSharp Lua sandbox, GameState, SaveSystem |
    | PokeForge.SDK.MonoGame | MonoGame DesktopGL runtime, HD render pipeline |
    | PokeForge.SDK.Tools | Asset validation, atlas packing, Fakemon pipeline |
    | PokeForge.SDK.Plugins | Nuzlocke, Randomizer, Turbo plugins |
    | PokeForge.SDK.Plugins.TTS | Narration plugin (Piper TTS + Windows Speech) |
    ```

    **docs/docs/packages/index.md** stub :
    ```markdown
    ---
    sidebar_position: 1
    ---

    # Packages Overview

    PokemonSDK distributes 8 NuGet packages under the `PokeForge.SDK.*` namespace.
    Each page covers the public API surface — classes, interfaces, configuration options.
    ```

    **docs/docs/cli/index.md** stub :
    ```markdown
    ---
    sidebar_position: 1
    ---

    # CLI pokeforge

    `pokeforge` is a global .NET tool for PokemonSDK projects.

    ```bash
    dotnet tool install -g PokeForge.CLI
    ```

    ## Commands

    | Command | Description |
    |---------|-------------|
    | `pokeforge new` | Scaffold a new StarterGame project |
    | `pokeforge seed` | Populate the SQLite database |
    | `pokeforge doctor` | Health check (SDL, piper, aplay) |
    | `pokeforge asset-sync` | Validate + pack + sync sprites |
    | `pokeforge fakemon list-parts` | List available Fakemon parts |
    | `pokeforge fakemon assemble` | Assemble a Fakemon sprite |
    ```

    **docs/docs/advanced/index.md** stub :
    ```markdown
    ---
    sidebar_position: 1
    ---

    # Advanced APIs

    APIs for extending PokemonSDK: narration, custom Fakemon generation.
    ```

    **docs/.gitignore** :
    ```
    node_modules/
    .docusaurus/
    build/
    ```

    **docs/static/img/.gitkeep** — fichier vide pour garder le dossier.

    Créer aussi `docs/src/pages/index.tsx` minimaliste qui redirige vers /docs/intro :
    ```tsx
    import { Redirect } from '@docusaurus/router';

    export default function Home() {
      return &lt;Redirect to="/docs/intro" /&gt;;
    }
    ```

    Éviter : ne pas utiliser `npx create-docusaurus` (ajoute trop de fichiers inutiles). Ne pas inclure de contenu WIP (D-21 : stubs sont OK, contenu réel Wave 2/3/4).
  </action>
  <verify>
    cd docs && npm install && npm run build
    → exit 0, dossier docs/build/ présent
  </verify>
  <done>AC-1 satisfait : build Docusaurus local exit 0</done>
</task>

<task type="auto">
  <name>Task 2 : Workflow GitHub Actions deploy-docs.yml</name>
  <files>.github/workflows/deploy-docs.yml</files>
  <action>
    Créer `.github/workflows/deploy-docs.yml` pour deploy automatique sur GitHub Pages :

    ```yaml
    name: Deploy Docs

    on:
      push:
        branches: [main]
        paths:
          - 'docs/**'
          - '.github/workflows/deploy-docs.yml'
      workflow_dispatch:

    permissions:
      contents: read
      pages: write
      id-token: write

    concurrency:
      group: pages
      cancel-in-progress: false

    jobs:
      build:
        name: Build docs
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: 20
              cache: npm
              cache-dependency-path: docs/package-lock.json

          - name: Install dependencies
            working-directory: docs
            run: npm ci

          - name: Build Docusaurus
            working-directory: docs
            run: npm run build

          - name: Upload artifact
            uses: actions/upload-pages-artifact@v3
            with:
              path: docs/build

      deploy:
        name: Deploy to GitHub Pages
        needs: build
        runs-on: ubuntu-latest
        environment:
          name: github-pages
          url: ${{ steps.deployment.outputs.page_url }}
        steps:
          - name: Deploy to GitHub Pages
            id: deployment
            uses: actions/deploy-pages@v4
    ```

    Contraintes :
    - `paths:` filtre sur `docs/**` — le workflow ne tourne QUE si docs changent (pas à chaque commit SDK)
    - `concurrency: group: pages` — évite deux deploys en parallèle
    - `npm ci` (pas `npm install`) — reproduit exactement le lockfile
    - `actions/upload-pages-artifact@v3` + `actions/deploy-pages@v4` — API officielle GitHub Pages (remplace gh-pages branch manuelle)
    - Permissions `pages: write` + `id-token: write` requises pour Pages OIDC
    - `workflow_dispatch` permet deploy manuel depuis GitHub UI sans commit
  </action>
  <verify>
    gh workflow view deploy-docs --yaml 2>/dev/null | grep -E "name|on:|push:|branches:" | head -10
    # Doit montrer le workflow lisible
    # Validation YAML : python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy-docs.yml'))"
  </verify>
  <done>AC-3 satisfait : workflow valide, permissions Pages présentes, path filter docs/**</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - `docs/` : scaffold Docusaurus 3 avec sidebar D-21 (8 packages + CLI + Advanced APIs)
    - `.github/workflows/deploy-docs.yml` : deploy auto GitHub Pages sur push main
  </what-built>
  <how-to-verify>
    1. `cd docs && npm install && npm run build` → exit 0
    2. `npm start` → ouvrir http://localhost:3000
    3. Vérifier sidebar : "Packages" (8 entrées), "CLI pokeforge" (5 entrées), "Advanced APIs" (4 entrées)
    4. `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy-docs.yml'))"` → pas d'erreur
    5. Vérifier que `docs/build/` existe et contient `index.html`
  </how-to-verify>
  <resume-signal>Taper "approved" pour continuer, ou décrire les problèmes à corriger</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/**` — aucun fichier source SDK modifié dans ce plan
- `.github/workflows/ci.yml` — CI SDK non modifiée
- `.github/workflows/deploy-prod.yml` — pipeline prod non modifiée
- `CLAUDE.md`, `REQUIREMENTS.md`, `README.md` — mise à jour Phase 11 → Wave 4 (UNIFY final)

## SCOPE LIMITS
- Pas de contenu réel dans ce plan — seuls les stubs et la structure. Le contenu narratif est Wave 2/3.
- Pas de docfx dans ce plan — l'API reference XML est Wave 4 (Plan 11-04).
- Pas d'activation manuelle de GitHub Pages — l'activation se fait via GitHub UI settings (une fois, hors CI).
- Pas de custom domain — hors scope Phase 11.

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `cd docs && npm install && npm run build` → exit 0
- [ ] `docs/build/index.html` existe
- [ ] `docs/sidebars.ts` liste exactement 8 packages + CLI + Advanced APIs
- [ ] `.github/workflows/deploy-docs.yml` passe validation YAML
- [ ] `git status` propre (tous les fichiers créés stagés)
- [ ] AC-1, AC-2, AC-3 satisfaits
</verification>

<success_criteria>
- Build Docusaurus local exit 0 (AC-1)
- Sidebar D-21 complète visible au démarrage (AC-2)
- Workflow deploy-docs.yml valide YAML, permissions Pages correctes (AC-3)
- Zéro modification aux fichiers src/ SDK
- Checkpoint humain approuvé
</success_criteria>

<output>
Après complétion, créer `.paul/phases/11-documentation/11-01-SUMMARY.md`
</output>

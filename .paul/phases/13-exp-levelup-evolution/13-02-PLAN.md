---
phase: 13-exp-levelup-evolution
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/SDK.MonoGame/UI/ExpBar.cs
  - src/SDK.MonoGame/UI/LevelUpOverlay.cs
  - src/SDK.MonoGame/Scenes/BattleScene.cs
autonomous: false
---

<objective>
## Goal
ExpBar UI + LevelUpOverlay dans SDK.MonoGame : barre EXP animée sous la HP bar joueur, overlay stat deltas après level-up, nouvelle phase BattleScene.ShowLevelUp.

## Purpose
Plan 13-01 livre le moteur headless (AwardExp, level-up, log messages). Plan 13-02 consomme ces log messages pour afficher au joueur les gains d'EXP et les progressions de niveau — c'est la couche visuelle qui rend BTLUI-02 jouable.

## Output
- `src/SDK.MonoGame/UI/ExpBar.cs` — composant stateless (même pattern que HpBar)
- `src/SDK.MonoGame/UI/LevelUpOverlay.cs` — overlay dismissable avec stat deltas
- `src/SDK.MonoGame/Scenes/BattleScene.cs` — phase ShowLevelUp + injection IExpFormula + wiring
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/13-exp-levelup-evolution/13-01-SUMMARY.md

## Source Files Clés
@src/SDK.MonoGame/Scenes/BattleScene.cs
@src/SDK.MonoGame/UI/HpBar.cs
@src/SDK.Core/Interfaces/IExpFormula.cs
@src/SDK.Core/ValueObjects/BattlePokemon.cs
</context>

<acceptance_criteria>

## AC-1: ExpBar visible sous HP joueur
```gherkin
Given BattleScene initialisée avec IExpFormula non-null
When Draw() appelé avec un BattleState (Player.Level < 100)
Then une barre EXP jaune s'affiche sous la HP bar PLR
     proportionnelle à (Player.CurrentExp / ExpThreshold(Player.Level+1, GrowthRate))
     avec label "Lv.{level}" visible si SpriteFont fourni
```

## AC-2: LevelUpOverlay déclenché après KO avec level-up
```gherkin
Given Player gagne suffisamment d'EXP pour monter de niveau
When ExecuteTurn() constate "grew to level" dans BattleState.Log
Then _leveledUp = true, LevelUpOverlay.Trigger(playerBefore, playerAfter) appelé
     Phase ShowLog normale d'abord, puis transition vers ShowLevelUp
```

## AC-3: Overlay affiche les stat deltas corrects
```gherkin
Given LevelUpOverlay.Trigger(before, after) appelé
When Draw() exécuté en phase ShowLevelUp
Then panneau bas affiche "Level Up! Lv.{newLevel}"
     et les deltas: +ATK, +DEF, +SpA, +SpD, +Spe, +HP (valeurs numériques)
     et "Space" prompt
```

## AC-4: Overlay dismissable → retour flux normal
```gherkin
Given phase ShowLevelUp active
When joueur appuie Space (front-edge uniquement)
Then IsVisible = false, phase = BattleEnd si opponent.CurrentHp <= 0
     sinon phase = SelectMove
```

## AC-5: Backward-compat sans IExpFormula
```gherkin
Given BattleScene(engine) sans IExpFormula (constructeur existant)
When Draw() appelé
Then aucune ExpBar dessinée, zéro régression tests existants
     build 0 erreurs 0 warnings
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Créer UI/ExpBar.cs</name>
  <files>src/SDK.MonoGame/UI/ExpBar.cs</files>
  <action>
    Composant stateless identique au pattern HpBar.

    Signature :
    ```csharp
    public sealed class ExpBar
    {
        private readonly Texture2D _pixel;
        public ExpBar(GraphicsDevice gd) { ... }
        public void Draw(SpriteBatch sb, int currentExp, int nextLevelExp, int level,
                         Vector2 position, int width, int height, SpriteFont? font) { ... }
        public void Dispose() => _pixel.Dispose();
    }
    ```

    Implémentation Draw :
    - Fond DarkGray plein (width × height)
    - Fill Yellow : `(int)(width * ratio)` où `ratio = nextLevelExp > 0 ? Math.Clamp((float)currentExp / nextLevelExp, 0f, 1f) : 1f`
    - Si font != null : DrawString `$"Lv.{level}"` à `(position.X, position.Y - 11)` scale 0.45f, Color.White
    - Si level >= 100 : fill plein, label "Lv.MAX"

    Namespace : `SDK.MonoGame.UI`
    Usings : Microsoft.Xna.Framework + Microsoft.Xna.Framework.Graphics
    Pas de référence SDK.Battle ni SDK.Core — composant pur MonoGame.
  </action>
  <verify>`dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -q` → 0 erreurs</verify>
  <done>AC-5 partiellement satisfait : composant compile</done>
</task>

<task type="auto">
  <name>Task 2: Créer UI/LevelUpOverlay.cs</name>
  <files>src/SDK.MonoGame/UI/LevelUpOverlay.cs</files>
  <action>
    Overlay dismissable, état interne, pattern similaire à BattleEndOverlay.

    ```csharp
    public sealed class LevelUpOverlay
    {
        public bool IsVisible { get; private set; }

        private string _nickname = "";
        private int _newLevel;
        private int _dAtk, _dDef, _dSpA, _dSpD, _dSpe, _dHp;

        public void Trigger(BattlePokemon before, BattlePokemon after)
        {
            _nickname = after.Nickname;
            _newLevel  = after.Level;
            _dAtk = after.Attack         - before.Attack;
            _dDef = after.Defense        - before.Defense;
            _dSpA = after.SpecialAttack  - before.SpecialAttack;
            _dSpD = after.SpecialDefense - before.SpecialDefense;
            _dSpe = after.Speed          - before.Speed;
            _dHp  = after.MaxHp         - before.MaxHp;
            IsVisible = true;
        }

        public void Update(KeyboardState ks, KeyboardState prevKs)
        {
            if (IsVisible && ks.IsKeyDown(InputMap.Confirm) && !prevKs.IsKeyDown(InputMap.Confirm))
                IsVisible = false;
        }

        public void Draw(SpriteBatch sb, Texture2D pixel, SpriteFont? font)
        {
            if (!IsVisible || font is null) return;
            // Fond panneau bas (identique BattleEnd)
            sb.Draw(pixel, new Rectangle(0, 179, 480, 91), new Color(15, 15, 15));
            // Titre
            sb.DrawString(font, $"{_nickname} grew to Lv.{_newLevel}!",
                new Vector2(8f, 185f), Color.Yellow, 0f, Vector2.Zero, 0.55f, SpriteEffects.None, 0f);
            // Stat deltas sur 2 lignes de 3
            string line1 = $"+ATK {_dAtk:+0;-0}   +DEF {_dDef:+0;-0}   +SpA {_dSpA:+0;-0}";
            string line2 = $"+SpD {_dSpD:+0;-0}   +Spe {_dSpe:+0;-0}   +HP {_dHp:+0;-0}";
            sb.DrawString(font, line1, new Vector2(8f, 205f), Color.White, 0f, Vector2.Zero, 0.50f, SpriteEffects.None, 0f);
            sb.DrawString(font, line2, new Vector2(8f, 220f), Color.White, 0f, Vector2.Zero, 0.50f, SpriteEffects.None, 0f);
            // Prompt
            sb.DrawString(font, "Space", new Vector2(432f, 255f), Color.DimGray, 0f, Vector2.Zero, 0.45f, SpriteEffects.None, 0f);
        }
    }
    ```

    Namespace : `SDK.MonoGame.UI`
    Usings : Microsoft.Xna.Framework + Microsoft.Xna.Framework.Input + Microsoft.Xna.Framework.Graphics + SDK.Core.ValueObjects + SDK.MonoGame.Input
  </action>
  <verify>`dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -q` → 0 erreurs</verify>
  <done>AC-3 + AC-4 : logique Trigger + Update implémentée</done>
</task>

<task type="auto">
  <name>Task 3: Modifier BattleScene.cs — phase ShowLevelUp + ExpBar + wiring</name>
  <files>src/SDK.MonoGame/Scenes/BattleScene.cs</files>
  <action>
    Modifications précises sur BattleScene existant.

    **1. Enum BattlePhase — ajouter ShowLevelUp :**
    ```csharp
    private enum BattlePhase { Init, SelectMove, ShowLog, ShowLevelUp, BattleEnd }
    ```

    **2. Nouveaux champs privés :**
    ```csharp
    private readonly IExpFormula? _expFormula;
    private ExpBar? _expBar;
    private LevelUpOverlay? _levelUpOverlay;
    private bool _leveledUp;
    private BattlePokemon _playerBeforeTurn;
    ```

    **3. Constructeur — paramètre optionnel :**
    ```csharp
    public BattleScene(IBattleEngine engine, IExpFormula? expFormula = null)
    {
        _engine = engine;
        _expFormula = expFormula;
    }
    ```

    **4. Initialize() — instancier les nouveaux composants :**
    Ajouter après `_statusIcon = new StatusIcon(graphicsDevice);` :
    ```csharp
    _expBar = new ExpBar(graphicsDevice);
    _levelUpOverlay = new LevelUpOverlay();
    ```

    **5. ExecuteTurn() — capturer état avant + détecter level-up :**
    ```csharp
    private void ExecuteTurn()
    {
        _playerBeforeTurn = _state!.Player;                          // capture AVANT
        var opponentMove = _engine.SelectOpponentMove(_state!);
        _state = _engine.RunTurn(_state!, _selectedMove!, opponentMove);
        _lastLog = _state.Log;
        _leveledUp = _state.Log.Any(m => m.Contains("grew to level")); // détecter
        if (_leveledUp)
            _levelUpOverlay!.Trigger(_playerBeforeTurn, _state.Player);
        _phase = BattlePhase.ShowLog;
        _prevKs = Keyboard.GetState();
    }
    ```

    **6. Update() — modifier la transition ShowLog et ajouter ShowLevelUp :**

    Dans le case `ShowLog`, remplacer la transition de phase :
    ```csharp
    case BattlePhase.ShowLog:
        var ksLog = Keyboard.GetState();
        if (ksLog.IsKeyDown(InputMap.Confirm) && !_prevKs.IsKeyDown(InputMap.Confirm))
        {
            _prevKs = Keyboard.GetState();
            if (_leveledUp)
            {
                _phase = BattlePhase.ShowLevelUp;
                _leveledUp = false;
            }
            else
            {
                _phase = (_state.Player.CurrentHp <= 0 || _state.Opponent.CurrentHp <= 0)
                    ? BattlePhase.BattleEnd
                    : BattlePhase.SelectMove;
            }
        }
        else
            _prevKs = ksLog;
        break;
    ```

    Ajouter le nouveau case `ShowLevelUp` :
    ```csharp
    case BattlePhase.ShowLevelUp:
        var ksLvl = Keyboard.GetState();
        _levelUpOverlay!.Update(ksLvl, _prevKs);
        _prevKs = ksLvl;
        if (!_levelUpOverlay.IsVisible)
            _phase = (_state.Player.CurrentHp <= 0 || _state.Opponent.CurrentHp <= 0)
                ? BattlePhase.BattleEnd
                : BattlePhase.SelectMove;
        break;
    ```

    **7. Draw() — ExpBar + ShowLevelUp :**

    Après le bloc PLR StatusIcon, ajouter la barre EXP :
    ```csharp
    if (_expFormula != null && _state != null && _state.Player.Level < 100)
    {
        int nextThreshold = _expFormula.ExpThreshold(_state.Player.Level + 1, _state.Player.GrowthRate);
        _expBar!.Draw(sb, _state.Player.CurrentExp, nextThreshold, _state.Player.Level,
            new Vector2(256f, 162f), 140, 4, _font);
    }
    ```

    Ajouter le rendu ShowLevelUp dans Draw() (après le bloc ShowLog) :
    ```csharp
    if (_phase == BattlePhase.ShowLevelUp)
    {
        DrawRect(sb, new Rectangle(0, 178, 480, 1), new Color(60, 60, 60));
        _levelUpOverlay!.Draw(sb, _pixel!, _font);
    }
    ```

    **Usings à ajouter si manquants :**
    ```csharp
    using SDK.Core.Interfaces;
    using SDK.MonoGame.UI;
    ```

    Vérifier que LINQ est accessible (`using System.Linq;` ou global usings déjà actifs).

    Ne pas modifier : HpBar, StatusIcon, MoveMenu, BattleEndOverlay, WorldScene, Game1, WipeTransition.
  </action>
  <verify>`dotnet build src/SDK.MonoGame/SDK.MonoGame.csproj -q` → 0 erreurs 0 warnings</verify>
  <done>AC-1 + AC-2 + AC-4 + AC-5 satisfaits : ExpBar dessinée si IExpFormula présent, ShowLevelUp phase câblée, backward-compat constructeur</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    ExpBar jaune sous HP joueur (visible en combat), LevelUpOverlay dans panneau bas
    (déclenché si le joueur monte de niveau après KO de l'adversaire), phase ShowLevelUp
    dismissable avec Space → retour au flux normal.
  </what-built>
  <how-to-verify>
    1. Lancer : `dotnet run --project src/SDK.MonoGame --configuration Debug`
    2. Déclencher un combat (marcher dans les herbes ou trigger Battle dans Game1)
    3. Vérifier en combat : barre jaune visible sous "PLR HP" à droite, label "Lv.N"
    4. Vaincre l'adversaire avec un Pokémon proche du prochain niveau
       (modifier temporairement CurrentExp dans le test si nécessaire)
    5. Vérifier : log affiché (ShowLog) → Space → LevelUpOverlay apparaît avec
       "grew to Lv.N!" + stat deltas + prompt Space → Space → BattleEnd
    6. Vérifier sans EXP suffisant : pas d'overlay après ShowLog → BattleEnd direct
    7. Vérifier build Release propre : `dotnet build PokemonSDK.slnx -c Release -q`
  </how-to-verify>
  <resume-signal>Taper "approved" pour continuer, ou décrire les problèmes visuels à corriger</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.MonoGame/UI/HpBar.cs` — composant existant inchangé
- `src/SDK.MonoGame/UI/BattleEndOverlay.cs` — overlay existant inchangé
- `src/SDK.MonoGame/UI/MoveMenu.cs` — move menu existant inchangé
- `src/SDK.MonoGame/UI/StatusIcon.cs` — status icon existant inchangé
- `src/SDK.Battle/BattleEngine.cs` — moteur headless verrouillé Plan 13-01
- `src/SDK.Core/**` — zéro modification SDK.Core dans ce plan
- `src/SDK.Data/**` — zéro migration dans ce plan
- `tests/**` — aucun test existant modifié

## SCOPE LIMITS
- Pas d'animation frame-by-frame (fill EXP "instantané" — interpolation différée Phase 12.x)
- Pas de son/SFX (Phase 18)
- LevelUpOverlay n'affiche pas les moves appris (log ShowLog les affiche déjà)
- ExpBar position approximative (fine-tuning pixel accepté sans re-planifier)
- Game1.cs non modifié : le constructeur `BattleScene(engine, expFormula?)` avec default null
  maintient la backward-compat existante sans toucher à Game1

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx -c Release -q` → 0 erreurs, 0 warnings
- [ ] `dotnet test tests/ -q` → tous les tests existants verts (230+)
- [ ] `dotnet run --project src/SDK.MonoGame -- --headless` → 0 crash
- [ ] ExpBar visible en combat si IExpFormula injecté (human-verify)
- [ ] LevelUpOverlay affiche les stat deltas corrects (human-verify)
- [ ] Overlay dismiss Space → transition correcte BattleEnd ou SelectMove
- [ ] AC-1 à AC-5 tous satisfaits
</verification>

<success_criteria>
- ExpBar.cs + LevelUpOverlay.cs créés, BattleScene.cs modifié
- Build solution Release propre (0 erreurs 0 warnings)
- Suite tests existante verte sans régression
- Checkpoint human-verify approuvé
</success_criteria>

<output>
Après complétion, créer `.paul/phases/13-exp-levelup-evolution/13-02-SUMMARY.md`
</output>

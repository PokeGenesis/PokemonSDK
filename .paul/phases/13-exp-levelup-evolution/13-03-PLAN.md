---
phase: 13-exp-levelup-evolution
plan: 03
type: execute
wave: 4
depends_on: ["13-02c"]
files_modified:
  - src/SDK.Core/ValueObjects/BattlePokemon.cs
  - src/SDK.Core/ValueObjects/BattleState.cs
  - src/SDK.Core/ValueObjects/EvolutionData.cs
  - src/SDK.Battle/BattleEngine.cs
  - src/SDK.MonoGame/UI/EvolutionOverlay.cs
  - src/SDK.MonoGame/Scenes/BattleScene.cs
  - src/SDK.MonoGame/Scenes/WorldScene.cs
autonomous: false
---

<objective>
## Goal
Evolution UI complète: détection côté BattleEngine, overlay flash + message + confirmation Space (annulable par X pendant le flash), application de l'évolution sur BattleState, debug F7.

## Purpose
Clôt BTLUI-02 (EXP + Level-up + Évolution) et complète Phase 13. Après un level-up, si le Pokémon atteint son niveau d'évolution, une séquence flash + message s'affiche. Le joueur peut annuler via X pendant le flash (ROADMAP SC5).

## Output
- `EvolutionData` record (SDK.Core)
- `BattlePokemon` avec champs évolution (EvolvesAtLevel/ToSpeciesId/ToName)
- `BattleState` avec `PendingEvolution`
- `BattleEngine.AwardExp` : détecte et set `PendingEvolution`
- `EvolutionOverlay` (flash 2s annulable X + message "grew to IVYSAUR!" + Space)
- `BattleScene` phase `ShowEvolution` + application species/name si non annulé
- Debug F7 : BULBASAUR Lv5 → Lv6 avec évolution vers IVYSAUR (annulable)
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/13-exp-levelup-evolution/13-02c-SUMMARY.md

## Source Files
@src/SDK.Core/ValueObjects/BattlePokemon.cs
@src/SDK.Core/ValueObjects/BattleState.cs
@src/SDK.Battle/BattleEngine.cs
@src/SDK.MonoGame/UI/LevelUpOverlay.cs
@src/SDK.MonoGame/Scenes/BattleScene.cs
@src/SDK.MonoGame/Scenes/WorldScene.cs
</context>

<acceptance_criteria>

## AC-1: BattleEngine détecte et publie PendingEvolution
```gherkin
Given BattlePokemon avec EvolvesAtLevel=6, EvolvesToSpeciesId=2, EvolvesToName="IVYSAUR", Level=5
When RunTurn() KO l'adversaire et l'EXP gagnée pousse le joueur à Lv6
Then BattleState.PendingEvolution est non-null avec OldName="BULBASAUR", NewName="IVYSAUR", NewSpeciesId=2
```

## AC-2: EvolutionOverlay affiche flash puis message, annulable pendant le flash
```gherkin
Given EvolutionOverlay.Trigger("BULBASAUR", "IVYSAUR") appelé
When Update() reçoit 2.0s de GameTime sans input
Then IsVisible=true, phase passe de Flashing à Done — message "BULBASAUR evolved into IVYSAUR!" visible
When Space pressé en phase Done
Then IsComplete=true, IsVisible=false, WasCancelled=false

Given EvolutionOverlay en phase Flashing (< 2s écoulées)
When X pressé pendant le flash
Then IsComplete=true, IsVisible=false, WasCancelled=true — évolution annulée avant la confirmation
```

## AC-3: BattleScene orchestre ShowEvolution après level-up, cancel respecté
```gherkin
Given combat F7 : BULBASAUR Lv5 proche du seuil, EvolvesAtLevel=6
When joueur KO adversaire → ShowLog → ShowLevelUp → [optionnel MoveLearn] → ShowEvolution
Then overlay flash 2s, message "BULBASAUR evolved into IVYSAUR!", Space applique évolution
And BattleState.Player.Nickname == "IVYSAUR", SpeciesId == 2 après confirmation
And retour à SelectAction si aucun KO

Given ShowEvolution actif, X pressé pendant le flash
Then IsComplete=true, WasCancelled=true
And BattleState.Player.Nickname == "BULBASAUR" (inchangé), SpeciesId == 1 (inchangé)
And retour à SelectAction
```

## AC-4: Pas de régression — tests 293+
```gherkin
Given BattlePokemon et BattleState avec nouveaux champs optionnels null par défaut
When dotnet test tests/
Then 293+ tests passent, 0 regressions
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Data layer — EvolutionData + BattlePokemon evolution fields + BattleState.PendingEvolution + BattleEngine detection</name>
  <files>src/SDK.Core/ValueObjects/EvolutionData.cs, src/SDK.Core/ValueObjects/BattlePokemon.cs, src/SDK.Core/ValueObjects/BattleState.cs, src/SDK.Battle/BattleEngine.cs</files>
  <action>
    **A — Créer `src/SDK.Core/ValueObjects/EvolutionData.cs`:**
    ```csharp
    namespace SDK.Core.ValueObjects;
    public sealed record EvolutionData(string OldName, string NewName, int NewSpeciesId);
    ```

    **B — Modifier `BattlePokemon.cs`:** ajouter 3 champs optionnels à la fin du record (après `FullLearnset`):
    ```csharp
    int? EvolvesAtLevel = null,
    int? EvolvesToSpeciesId = null,
    string? EvolvesToName = null
    ```
    Paramètres `using SDK.Core.Enums;` déjà présent — aucun nouveau using.

    **C — Modifier `BattleState.cs`:** ajouter après `PendingLearnedMoves`:
    ```csharp
    EvolutionData? PendingEvolution = null
    ```

    **D — Modifier `BattleEngine.cs`, méthode `AwardExp()`:**
    Dans la boucle `while (newLevel < 100 ...)`, après `player = updatedPlayer;` (fin de chaque level-up),
    ajouter la vérification d'évolution :
    ```csharp
    if (player.EvolvesAtLevel.HasValue
        && newLevel == player.EvolvesAtLevel.Value
        && player.EvolvesToSpeciesId.HasValue)
    {
        pendingEvolution = new EvolutionData(
            player.Nickname,
            player.EvolvesToName ?? player.Nickname,
            player.EvolvesToSpeciesId.Value);
    }
    ```
    Déclarer `EvolutionData? pendingEvolution = null;` au début de `AwardExp()`, avant la boucle.

    Dans le `return state with { ... }` final, ajouter:
    ```csharp
    PendingEvolution = pendingEvolution,
    ```

    Ajouter `using SDK.Core.ValueObjects;` si pas déjà présent en tête de fichier
    (BattleEngine importe déjà `SDK.Core.ValueObjects` — vérifier).

    Éviter: modifier des tests existants, modifier les champs non-optionnels de BattlePokemon.
  </action>
  <verify>dotnet build PokemonSDK.slnx — 0 erreurs, 0 warnings. dotnet test tests/ — 293+ tests verts.</verify>
  <done>AC-1 satisfait (BattleEngine publie PendingEvolution), AC-4 satisfait (0 regression)</done>
</task>

<task type="auto">
  <name>Task 2: EvolutionOverlay UI + BattleScene ShowEvolution phase + WorldScene F7</name>
  <files>src/SDK.MonoGame/UI/EvolutionOverlay.cs, src/SDK.MonoGame/Scenes/BattleScene.cs, src/SDK.MonoGame/Scenes/WorldScene.cs</files>
  <action>
    **A — Créer `src/SDK.MonoGame/UI/EvolutionOverlay.cs`:**
    ```csharp
    namespace SDK.MonoGame.UI;

    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;
    using Microsoft.Xna.Framework.Input;
    using SDK.MonoGame.Input;

    public sealed class EvolutionOverlay
    {
        private enum EvoPhase { Flashing, Done }

        private EvoPhase    _phase;
        private float       _flashTimer;
        private const float FlashDuration = 2.0f;
        private const float FlashInterval = 0.1f;
        private string      _oldName = "";
        private string      _newName = "";

        public bool IsVisible    { get; private set; }
        public bool IsComplete   { get; private set; }
        public bool WasCancelled { get; private set; }

        public void Trigger(string oldName, string newName)
        {
            _oldName     = oldName;
            _newName     = newName;
            _phase       = EvoPhase.Flashing;
            _flashTimer  = 0f;
            IsVisible    = true;
            IsComplete   = false;
            WasCancelled = false;
        }

        public void Update(KeyboardState ks, KeyboardState prevKs, GameTime gameTime)
        {
            if (!IsVisible) return;
            if (_phase == EvoPhase.Flashing)
            {
                _flashTimer += (float)gameTime.ElapsedGameTime.TotalSeconds;
                if (_flashTimer >= FlashDuration)
                    _phase = EvoPhase.Done;
                // X pendant le flash annule l'évolution (ROADMAP SC5)
                else if (ks.IsKeyDown(InputMap.Cancel) && !prevKs.IsKeyDown(InputMap.Cancel))
                {
                    IsVisible    = false;
                    IsComplete   = true;
                    WasCancelled = true;
                }
            }
            else if (ks.IsKeyDown(InputMap.Confirm) && !prevKs.IsKeyDown(InputMap.Confirm))
            {
                IsVisible    = false;
                IsComplete   = true;
                WasCancelled = false;
            }
        }

        public void Draw(SpriteBatch sb, Texture2D pixel, SpriteFont? font)
        {
            if (!IsVisible) return;

            if (_phase == EvoPhase.Flashing)
            {
                bool bright = (int)(_flashTimer / FlashInterval) % 2 == 0;
                var bg = bright ? Color.White : new Color(15, 15, 15);
                var fg = bright ? Color.Black : Color.White;
                sb.Draw(pixel, new Rectangle(0, 179, 480, 91), bg);
                if (font != null)
                    sb.DrawString(font, $"What? {_oldName} is evolving!",
                        new Vector2(8f, 193f), fg, 0f, Vector2.Zero, 0.55f, SpriteEffects.None, 0f);
            }
            else
            {
                sb.Draw(pixel, new Rectangle(0, 179, 480, 91), new Color(15, 15, 15));
                if (font != null)
                {
                    sb.DrawString(font, "Congratulations!",
                        new Vector2(8f, 185f), Color.Yellow, 0f, Vector2.Zero, 0.55f, SpriteEffects.None, 0f);
                    sb.DrawString(font, $"{_oldName} evolved into {_newName}!",
                        new Vector2(8f, 203f), Color.White, 0f, Vector2.Zero, 0.55f, SpriteEffects.None, 0f);
                    sb.DrawString(font, "Space", new Vector2(432f, 255f), Color.DimGray,
                        0f, Vector2.Zero, 0.45f, SpriteEffects.None, 0f);
                }
            }
        }
    }
    ```

    **B — Modifier `BattleScene.cs`:**

    **B1 — Enum:** ajouter `ShowEvolution` entre `ShowMoveLearn` et `BattleEnd`:
    ```csharp
    private enum BattlePhase { Init, SelectAction, SelectMove, ShowLog, ShowLevelUp, ShowMoveLearn, ShowEvolution, BattleEnd }
    ```

    **B2 — Champ:** ajouter après `_moveLearnOverlay`:
    ```csharp
    private readonly EvolutionOverlay _evolutionOverlay = new();
    ```

    **B3 — Update() — ShowLog case:** intercepter avant `NextPhaseAfterBattle()`.
    Remplacer la branche `else`:
    ```csharp
    // AVANT:
    else
    {
        _phase = NextPhaseAfterBattle();
    }

    // APRÈS:
    else if (_state!.PendingEvolution != null)
        TriggerEvolution();
    else
        _phase = NextPhaseAfterBattle();
    ```

    **B4 — Update() — ShowLevelUp case:** même interception après `!_levelUpOverlay.IsVisible`.
    Remplacer:
    ```csharp
    // AVANT:
    if (_pendingMoveQueue?.Count > 0)
        TriggerNextMoveLearn();
    else
        _phase = NextPhaseAfterBattle();

    // APRÈS:
    if (_pendingMoveQueue?.Count > 0)
        TriggerNextMoveLearn();
    else if (_state!.PendingEvolution != null)
        TriggerEvolution();
    else
        _phase = NextPhaseAfterBattle();
    ```

    **B5 — Update() — ShowMoveLearn case:** le code actuel repart en ShowLog, qui gère ensuite l'évolution via B3 — aucun changement requis ici.

    **B6 — Ajouter case ShowEvolution dans Update():** après le case ShowMoveLearn:
    ```csharp
    case BattlePhase.ShowEvolution:
    {
        var ksEvo = Keyboard.GetState();
        _evolutionOverlay.Update(ksEvo, _prevKs, gameTime);
        _prevKs = ksEvo;
        if (_evolutionOverlay.IsComplete)
        {
            var evo = _state!.PendingEvolution!;
            if (!_evolutionOverlay.WasCancelled)
            {
                _state = _state with
                {
                    Player = _state.Player with
                    {
                        Nickname  = evo.NewName,
                        SpeciesId = evo.NewSpeciesId
                    },
                    PendingEvolution = null
                };
                Serilog.Log.Information("[BATTLE] Evolution: {Old} → {New} (SpeciesId {Id})",
                    evo.OldName, evo.NewName, evo.NewSpeciesId);
            }
            else
            {
                _state = _state with { PendingEvolution = null };
                Serilog.Log.Information("[BATTLE] Evolution cancelled: {Old} stays as-is", evo.OldName);
            }
            _phase = NextPhaseAfterBattle();
        }
        break;
    }
    ```

    **B7 — Ajouter méthode helper `TriggerEvolution()`:** après `TriggerNextMoveLearn()`:
    ```csharp
    private void TriggerEvolution()
    {
        var evo = _state!.PendingEvolution!;
        _evolutionOverlay.Trigger(evo.OldName, evo.NewName);
        _phase = BattlePhase.ShowEvolution;
        _prevKs = Keyboard.GetState();
    }
    ```

    **B8 — Draw():** ajouter après le bloc ShowMoveLearn, avant ShowBattleEnd:
    ```csharp
    if (_phase == BattlePhase.ShowEvolution)
    {
        DrawRect(sb, new Rectangle(0, 178, 480, 1), new Color(60, 60, 60));
        _evolutionOverlay.Draw(sb, _pixel!, _font);
    }
    ```

    **C — Modifier `WorldScene.cs`:** ajouter F7 après F6:
    ```csharp
    // F7 — Evolution test : BULBASAUR Lv5 → Lv6 avec évolution vers IVYSAUR
    if (ks.IsKeyDown(Keys.F7) && !_prevKs.IsKeyDown(Keys.F7))
        StartBattle(ScenarioEvolution());
    ```

    Et ajouter la méthode `ScenarioEvolution()` dans le bloc `#if DEBUG`:
    ```csharp
    // F7 : Evolution — BULBASAUR Lv5, 210 EXP (seuil Lv6=216).
    // CHARMANDER yield=64, Lv5 → gains=45 → total=255 ≥ 216 → Lv6 déclenché.
    // EvolvesAtLevel=6 → PendingEvolution → EvolutionOverlay flash 2s → "evolved into IVYSAUR!".
    // X pendant flash annule (ROADMAP SC5). Space en phase Done confirme.
    private static BattleState ScenarioEvolution()
    {
        var tackle = new BattleMove(1, "tackle", 1, MoveCategory.Physical, 40, 100, 35, 35);

        var player = new BattlePokemon(
            SpeciesId: 1, Nickname: "BULBASAUR", Level: 5,
            CurrentHp: 45, MaxHp: 45,
            Attack: 49, Defense: 49, SpecialAttack: 65, SpecialDefense: 65, Speed: 45,
            Type1Id: 12, Type2Id: 4,
            Moves: new[] { tackle },
            CurrentExp: 210,
            GrowthRate: GrowthRate.MediumFast,
            EvolvesAtLevel: 6,
            EvolvesToSpeciesId: 2,
            EvolvesToName: "IVYSAUR");

        var opponent = new BattlePokemon(
            SpeciesId: 4, Nickname: "CHARMANDER", Level: 5,
            CurrentHp: 1, MaxHp: 39,
            Attack: 52, Defense: 43, SpecialAttack: 60, SpecialDefense: 50, Speed: 65,
            Type1Id: 10, Type2Id: null,
            Moves: new[] { tackle },
            BaseExpYield: 64);

        return new BattleState(player, opponent, Turn: 0, WeatherType.None,
            new BattleConfig(), Log: Array.Empty<string>());
    }
    ```

    Éviter: modifier les UI existants (LevelUpOverlay, MoveLearnOverlay, HpBar), modifier BattleEngine.RunBattle().
  </action>
  <verify>dotnet build PokemonSDK.slnx — 0 erreurs, 0 warnings. En game F7 : flash 2s, "BULBASAUR evolved into IVYSAUR!", Space → retour FIGHT/RUN avec "IVYSAUR" affiché dans la HpBar. Re-tester F7 avec X pendant flash → retour FIGHT/RUN avec "BULBASAUR" inchangé.</verify>
  <done>AC-2, AC-3 satisfaits : EvolutionOverlay opérationnel avec cancel, BattleScene orchestre ShowEvolution</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - EvolutionData record SDK.Core
    - BattlePokemon avec EvolvesAtLevel / EvolvesToSpeciesId / EvolvesToName (nullable, defaults null)
    - BattleState.PendingEvolution
    - BattleEngine.AwardExp() détecte évolution post-level-up
    - EvolutionOverlay : flash blanc/noir 2s "What? X is evolving!" — X annule pendant flash
    - Phase Done : "Congratulations! X evolved into Y!" + Space pour confirmer
    - BattleScene phase ShowEvolution entre ShowMoveLearn et BattleEnd
    - Évolution appliquée sur _state.Player (Nickname + SpeciesId) seulement si non annulée
    - Debug F7 : BULBASAUR Lv5 → Lv6 → IVYSAUR (annulable par X)
  </what-built>
  <how-to-verify>
    1. `dotnet run --project src/SDK.MonoGame`
    2. **Test confirmation :** F7 → FIGHT → Tackle → Space (log) → Space (LevelUp) → flash 2s visible ✓
    3. Attendre fin flash → "Congratulations!" + "BULBASAUR evolved into IVYSAUR!" + hint "Space" ✓
    4. Space → retour SelectAction — HpBar affiche "IVYSAUR" (nickname mis à jour) ✓
    5. **Test cancel :** F7 → FIGHT → Tackle → Space (log) → Space (LevelUp) → **X pendant le flash** ✓
    6. Retour SelectAction immédiat — HpBar affiche toujours "BULBASAUR" (aucun changement species) ✓
    7. F1→F6 : combats normaux inchangés, pas d'overlay évolution parasite ✓
    8. `dotnet test tests/` — 293+ tests verts ✓
  </how-to-verify>
  <resume-signal>Type "approved" pour continuer, ou décris les problèmes à corriger</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Battle/BattleEngine.cs — `RunBattle()` et `ApplyMove()` (seul `AwardExp()` modifié)
- src/SDK.MonoGame/UI/LevelUpOverlay.cs
- src/SDK.MonoGame/UI/MoveLearnOverlay.cs
- src/SDK.MonoGame/UI/HpBar.cs
- src/SDK.MonoGame/UI/MoveMenu.cs
- tests/ — aucun test existant modifié (les nouveaux champs ont des defaults null)

## SCOPE LIMITS
- Pas de formule d'évolution par pierre ou échange (level-up uniquement — MVP)
- Pas de sprite de l'espèce évoluée (placeholder gris existant suffit)
- Pas de son (Phase 18)
- EvolvesAtLevel/EvolvesToSpeciesId/EvolvesToName = MVP — Phase 17 (PokeAPI) câblera les vraies données

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx` — 0 erreurs, 0 warnings
- [ ] `dotnet test tests/` — 293+ tests passent (0 régressions)
- [ ] F7 confirm : flash 2s → "evolved into IVYSAUR!" → Space → HpBar "IVYSAUR"
- [ ] F7 cancel : X pendant flash → HpBar "BULBASAUR" inchangé
- [ ] F1→F6 : pas de régression (pas d'overlay évolution non sollicité)
- [ ] checkpoint:human-verify passé
</verification>

<success_criteria>
- Tasks 1 et 2 terminées
- 293+ tests verts
- EvolutionOverlay visible et fonctionnel en game F7 (confirm + cancel)
- ROADMAP SC5 fermé : évolution annulable par X pendant flash
- Phase 13 (BTLUI-02) fermée à 100%
</success_criteria>

<output>
Après completion, créer `.paul/phases/13-exp-levelup-evolution/13-03-SUMMARY.md`
</output>

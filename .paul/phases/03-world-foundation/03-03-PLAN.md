---
phase: 03-world-foundation
plan: 03
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - PokemonSDK.slnx
  - src/SDK.Core/Interfaces/IInputProvider.cs
  - src/SDK.MonoGame/SDK.MonoGame.csproj
  - src/SDK.MonoGame/Program.cs
  - src/SDK.MonoGame/HeadlessRunner.cs
  - src/SDK.MonoGame/Game1.cs
  - src/SDK.MonoGame/Rendering/RenderPipeline.cs
  - src/SDK.MonoGame/Rendering/Camera2D.cs
  - src/SDK.MonoGame/Rendering/TilemapRenderer.cs
  - src/SDK.MonoGame/Rendering/Shaders/xBR.fx
  - src/SDK.MonoGame/Rendering/Shaders/DayNight.fx
  - src/SDK.MonoGame/World/WorldSystem.cs
  - src/SDK.MonoGame/World/PlayerSystem.cs
  - src/SDK.MonoGame/Input/KeyboardInputProvider.cs
  - src/SDK.MonoGame/Content/Content.mgcb
autonomous: true
---

<objective>
## Goal
Créer le projet `SDK.MonoGame` avec Game1, composition root DI, RenderPipeline (480×270 → xBR → 1920×1080), WorldSystem, PlayerSystem et `HeadlessRunner` — le tout compile et les 74 tests existants restent verts.

## Purpose
Phase 3 goal : joueur sur tilemap → rencontre → battle engine. Ce plan pose le scaffolding SDK.MonoGame complet. Sans ce plan, le CI headless (Plan 03-04) et le World en jeu n'existent pas. `HeadlessRunner` permet à Plan 03-04 d'écrire des tests xUnit sans contexte GL.

## Output
- `src/SDK.MonoGame/` complet (14 fichiers)
- `SDK.Core/Interfaces/IInputProvider.cs` (1 interface)
- `PokemonSDK.slnx` mis à jour
- `dotnet build PokemonSDK.slnx -q` → 0 erreur
- `dotnet test PokemonSDK.slnx -q` → 74/74 verts (pas de régression)

Note : tests runtime (Game1.Run()) impossibles sans display. HeadlessRunner est la seule
vérification locale. Les tests visuels sont Plan 03-04 (CI ubuntu/windows).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/03-world-foundation/03-02-SUMMARY.md

## Source Files
@src/SDK.Core/Interfaces/IGameClock.cs
@src/SDK.Core/Interfaces/IEncounterSystem.cs
@src/SDK.Core/Interfaces/IWeatherSystem.cs
@src/SDK.Core/Services/GameTimeClock.cs
@src/SDK.Core/Services/WeatherSystem.cs
@src/SDK.Data/Services/EncounterSystem.cs
@PokemonSDK.slnx
</context>

<!-- No SPECIAL-FLOWS.md in this project — skills section omitted -->

<acceptance_criteria>

## AC-1: SDK.MonoGame projet compilé, D-02 respecté, ajouté à la solution
```gherkin
Given SDK.MonoGame.csproj avec MonoGame.Framework.DesktopGL (jamais WindowsDX)
  And ProjectReference vers SDK.Core, SDK.Data, SDK.Battle
  And PokemonSDK.slnx inclut le nouveau projet
When dotnet build PokemonSDK.slnx -q
Then 0 erreurs — D-01 (net10.0), D-02 (DesktopGL), D-06 (pas de ref SDK.Scripting)
```

## AC-2: HeadlessRunner exécute N frames sans GraphicsDevice ni crash
```gherkin
Given HeadlessRunner.Run(frames: 10) appelé directement (pas via Game1)
  And DI wire GameTimeClock + WeatherSystem + EncounterSystem + WorldSystem + PlayerSystem
When 10 frames simulées à 16.67ms chacune
Then IGameClock.Update() appelé 10×, WorldSystem.Update() appelé 10×
  And PlayerSystem.Position inchangé (aucun input), aucune exception
```

## AC-3: RenderPipeline structure respecte D-14 + D-15 (no-op si headless)
```gherkin
Given RenderPipeline construit avec isHeadless=true
When RenderPipeline.Draw() appelé
Then méthode no-op complète — 0 appel GL, 0 exception
Given RenderPipeline construit avec isHeadless=false (runtime MonoGame)
Then 3 passes définies : internalRT 480×270 → xBR Effect → DayNight tint
  And _internalTarget = RenderTarget2D(GraphicsDevice, 480, 270)
  And _xbrEffect chargé depuis Content (null-safe si shader non compilé)
```

## AC-4: WorldSystem + PlayerSystem branchés sur IGameClock + IEncounterSystem + IWeatherSystem
```gherkin
Given WorldSystem(IGameClock clock, IEncounterSystem enc, IWeatherSystem wx)
  And PlayerSystem(WorldSystem ws, IInputProvider input)
When WorldSystem.Update(delta) appelé
Then clock.Update(delta) appelé
  And WeatherType courant mis à jour via IWeatherSystem.GetWeather(biome, clock.GetTimeOfDay())
When PlayerSystem.TryMove(direction) appelé sur stub zone "test-zone"
Then IEncounterSystem.GetZonesByIdentifier("test-zone", 1) appelé (stub identifiant)
```

## AC-5: Solution complète — build 0 erreur + 74 tests verts
```gherkin
Given PokemonSDK.slnx incluant SDK.MonoGame
When dotnet build PokemonSDK.slnx -q && dotnet test PokemonSDK.slnx -q
Then 0 erreurs de build
  And 74/74 tests verts — zéro régression SDK.Core/SDK.Data/SDK.Battle
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: IInputProvider (SDK.Core) + SDK.MonoGame.csproj + Program.cs + HeadlessRunner</name>
  <files>
    src/SDK.Core/Interfaces/IInputProvider.cs,
    src/SDK.MonoGame/SDK.MonoGame.csproj,
    src/SDK.MonoGame/Program.cs,
    src/SDK.MonoGame/HeadlessRunner.cs,
    src/SDK.MonoGame/Content/Content.mgcb,
    PokemonSDK.slnx
  </files>
  <action>
    1. Créer src/SDK.Core/Interfaces/IInputProvider.cs :
       namespace SDK.Core.Interfaces;
       public interface IInputProvider
       {
           bool IsActionPressed(string action);   // "Up","Down","Left","Right","A","B","Start"
           bool IsActionJustPressed(string action);
       }
       Note : pure interface sans dépendance NuGet — SDK.Core reste zéro external.

    2. Créer src/SDK.MonoGame/SDK.MonoGame.csproj :
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <OutputType>Exe</OutputType>
           <TargetFramework>net10.0</TargetFramework>
           <Nullable>enable</Nullable>
           <ImplicitUsings>enable</ImplicitUsings>
           <RootNamespace>SDK.MonoGame</RootNamespace>
         </PropertyGroup>
         <ItemGroup>
           <PackageReference Include="MonoGame.Framework.DesktopGL" Version="3.8.4.1" />
           <PackageReference Include="MonoGame.Extended" Version="4.0.2" />
           <PackageReference Include="MonoGame.Extended.Tiled" Version="4.0.2" />
           <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
           <PackageReference Include="Serilog" Version="4.2.0" />
           <PackageReference Include="Serilog.Sinks.File" Version="6.0.0" />
           <PackageReference Include="Serilog.Sinks.Console" Version="6.0.0" />
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="..\SDK.Core\SDK.Core.csproj" />
           <ProjectReference Include="..\SDK.Data\SDK.Data.csproj" />
           <ProjectReference Include="..\SDK.Battle\SDK.Battle.csproj" />
         </ItemGroup>
       </Project>
       ```
       Règles : jamais MonoGame.WindowsDX (D-02), jamais SDK.Scripting ref directe (D-06).
       Si MonoGame.Extended 4.0.2 n'existe pas sur NuGet, chercher la version disponible
       la plus proche ≥3.8.x compatible MonoGame 3.8.4.1.

    3. Créer src/SDK.MonoGame/Content/Content.mgcb :
       ```
       #----------------------------- Global Properties ----------------------------#
       /outputDir:bin/$(Platform)/$(Configuration)/Content
       /intermediateDir:obj/$(Platform)/$(Configuration)/Content
       /platform:DesktopGL
       /config:
       /profile:Reach
       /compress:False

       #-------------------------------- References --------------------------------#

       #---------------------------------- Content ---------------------------------#
       ```
       Note : vide pour l'instant. Shaders et assets ajoutés en Plans suivants.

    4. Créer src/SDK.MonoGame/Program.cs :
       ```csharp
       using Microsoft.Extensions.DependencyInjection;
       using SDK.Core.Interfaces;
       using SDK.Core.Services;
       using SDK.Data;
       using SDK.Data.Services;
       using SDK.MonoGame.World;

       var isHeadless = args.Contains("--headless");
       int headlessFrames = 60;
       var framesArg = Array.Find(args, a => a.StartsWith("--max-frames="));
       if (framesArg != null && int.TryParse(framesArg.Split('=')[1], out var n))
           headlessFrames = n;

       var services = new ServiceCollection();
       services.AddDbContext<PokemonDbContext>(opt =>
           opt.UseSqlite($"Data Source=src/SDK.Data/data/PokemonSDK.db"));
       services.AddSingleton<IGameClock, GameTimeClock>();
       services.AddSingleton<IWeatherSystem, WeatherSystem>();
       services.AddScoped<IEncounterSystem, EncounterSystem>();
       services.AddSingleton<WorldSystem>();
       services.AddSingleton<PlayerSystem>();

       var sp = services.BuildServiceProvider();

       if (isHeadless)
       {
           SDK.MonoGame.HeadlessRunner.Run(sp, headlessFrames);
       }
       else
       {
           using var game = new SDK.MonoGame.Game1(sp);
           game.Run();
       }
       ```
       Note : chemin DB relatif depuis repo root — lancer avec `dotnet run --project src/SDK.MonoGame`.
       IScriptEngine injecté via Func<IScriptEngine> en Phase 4 (D-06).

    5. Créer src/SDK.MonoGame/HeadlessRunner.cs :
       ```csharp
       namespace SDK.MonoGame;

       using Microsoft.Extensions.DependencyInjection;
       using SDK.MonoGame.World;
       using SDK.Core.Interfaces;

       public static class HeadlessRunner
       {
           public static void Run(IServiceProvider sp, int frames = 60)
           {
               var clock   = sp.GetRequiredService<IGameClock>();
               var world   = sp.GetRequiredService<WorldSystem>();
               var player  = sp.GetRequiredService<PlayerSystem>();
               var delta   = TimeSpan.FromMilliseconds(1000.0 / 60.0);

               for (int i = 0; i < frames; i++)
               {
                   clock.Update(delta);
                   world.Update(delta);
                   player.Update();
               }
               Console.WriteLine($"[HeadlessRunner] {frames} frames OK — no exceptions");
           }
       }
       ```

    6. Modifier PokemonSDK.slnx — ajouter dans la section <Projects> :
       <Project Path="src/SDK.MonoGame/SDK.MonoGame.csproj" />
  </action>
  <verify>dotnet build PokemonSDK.slnx -q 2>&1 | tail -3</verify>
  <done>AC-1 + AC-5 partiels : solution compile incluant SDK.MonoGame, IInputProvider dans SDK.Core</done>
</task>

<task type="auto">
  <name>Task 2: Game1 + RenderPipeline (480×270 → xBR → 1080p) + Camera2D + shaders stubs</name>
  <files>
    src/SDK.MonoGame/Game1.cs,
    src/SDK.MonoGame/Rendering/RenderPipeline.cs,
    src/SDK.MonoGame/Rendering/Camera2D.cs,
    src/SDK.MonoGame/Rendering/TilemapRenderer.cs,
    src/SDK.MonoGame/Rendering/Shaders/xBR.fx,
    src/SDK.MonoGame/Rendering/Shaders/DayNight.fx
  </files>
  <action>
    1. Créer src/SDK.MonoGame/Game1.cs :
       ```csharp
       namespace SDK.MonoGame;

       using Microsoft.Extensions.DependencyInjection;
       using Microsoft.Xna.Framework;
       using Microsoft.Xna.Framework.Graphics;
       using SDK.Core.Interfaces;
       using SDK.MonoGame.Rendering;
       using SDK.MonoGame.World;

       public class Game1 : Game
       {
           private readonly IServiceProvider _services;
           private GraphicsDeviceManager _graphics;
           private SpriteBatch _spriteBatch = null!;
           private RenderPipeline _renderPipeline = null!;
           private WorldSystem _world = null!;
           private PlayerSystem _player = null!;
           private IGameClock _clock = null!;

           public Game1(IServiceProvider services)
           {
               _services = services;
               _graphics = new GraphicsDeviceManager(this)
               {
                   PreferredBackBufferWidth  = 1920,
                   PreferredBackBufferHeight = 1080,
                   IsFullScreen = false,
               };
               Content.RootDirectory = "Content";
               IsMouseVisible = true;
           }

           protected override void Initialize()
           {
               _clock   = _services.GetRequiredService<IGameClock>();
               _world   = _services.GetRequiredService<WorldSystem>();
               _player  = _services.GetRequiredService<PlayerSystem>();
               base.Initialize();
           }

           protected override void LoadContent()
           {
               _spriteBatch   = new SpriteBatch(GraphicsDevice);
               _renderPipeline = new RenderPipeline(GraphicsDevice, Content, isHeadless: false);
           }

           protected override void Update(GameTime gameTime)
           {
               _clock.Update(gameTime.ElapsedGameTime);
               _world.Update(gameTime.ElapsedGameTime);
               _player.Update();
               base.Update(gameTime);
           }

           protected override void Draw(GameTime gameTime)
           {
               _renderPipeline.BeginScene();
               // TODO Plan 03-04+ : tilemap + player sprite
               _renderPipeline.EndScene(_spriteBatch, _clock.GetTimeOfDay());
               base.Draw(gameTime);
           }
       }
       ```

    2. Créer src/SDK.MonoGame/Rendering/RenderPipeline.cs :
       ```csharp
       namespace SDK.MonoGame.Rendering;

       using Microsoft.Xna.Framework;
       using Microsoft.Xna.Framework.Content;
       using Microsoft.Xna.Framework.Graphics;
       using SDK.Core.Enums;

       public class RenderPipeline
       {
           private readonly bool _headless;
           private RenderTarget2D? _internalTarget;   // 480×270 (D-14)
           private Effect?         _xbrEffect;         // xBR ×4 upscale (D-15)
           private Effect?         _dayNightEffect;    // tint par TimeOfDay
           private Rectangle       _fullscreenRect;    // 1920×1080

           // Tints par TimeOfDay (D-14 conforme)
           private static readonly Dictionary<TimeOfDay, Color> DayNightTints = new()
           {
               [TimeOfDay.Morning] = new Color(255, 220, 180),
               [TimeOfDay.Day]     = Color.White,
               [TimeOfDay.Evening] = new Color(255, 180, 100),
               [TimeOfDay.Night]   = new Color(80,  100, 180),
           };

           public RenderPipeline(GraphicsDevice gd, ContentManager content, bool isHeadless)
           {
               _headless = isHeadless;
               if (isHeadless) return;

               _internalTarget = new RenderTarget2D(gd, 480, 270);
               _fullscreenRect = new Rectangle(0, 0, 1920, 1080);

               // Null-safe : shader compilé en Plan 03-04+ (CI avec MGCB)
               try { _xbrEffect      = content.Load<Effect>("Shaders/xBR"); }
               catch { /* shader pas encore compilé — upscale PointClamp fallback */ }

               try { _dayNightEffect = content.Load<Effect>("Shaders/DayNight"); }
               catch { /* fallback : tint via SpriteBatch Color */ }
           }

           public void BeginScene()
           {
               if (_headless || _internalTarget is null) return;
               // Game1 appelle GraphicsDevice.SetRenderTarget(_internalTarget) avant de dessiner
           }

           public void EndScene(SpriteBatch sb, TimeOfDay timeOfDay)
           {
               if (_headless || _internalTarget is null) return;

               var gd = sb.GraphicsDevice;
               gd.SetRenderTarget(null);
               gd.Clear(Color.Black);

               var tint = DayNightTints.GetValueOrDefault(timeOfDay, Color.White);

               // Passe 2 : xBR upscale (ou PointClamp fallback)
               sb.Begin(
                   effect:       _xbrEffect,
                   samplerState: _xbrEffect != null ? SamplerState.LinearClamp : SamplerState.PointClamp);
               sb.Draw(_internalTarget, _fullscreenRect, Color.White);
               sb.End();

               // Passe 3 : DayNight tint (shader ou Color multiplication)
               if (_dayNightEffect != null)
               {
                   sb.Begin(effect: _dayNightEffect, samplerState: SamplerState.PointClamp);
                   sb.Draw(_internalTarget, _fullscreenRect, tint);
                   sb.End();
               }
           }

           public RenderTarget2D? InternalTarget => _internalTarget;
       }
       ```

    3. Créer src/SDK.MonoGame/Rendering/Camera2D.cs :
       ```csharp
       namespace SDK.MonoGame.Rendering;

       using Microsoft.Xna.Framework;

       public class Camera2D
       {
           public Vector2 Position { get; private set; }
           private const int InternalWidth  = 480;
           private const int InternalHeight = 270;

           public Matrix GetTransform()
               => Matrix.CreateTranslation(
                   -Position.X + InternalWidth  / 2f,
                   -Position.Y + InternalHeight / 2f,
                   0f);

           public void Follow(Vector2 target)
               => Position = Vector2.Lerp(Position, target, 0.12f);  // pixel-perfect lerp

           public Rectangle GetVisibleBounds()
               => new Rectangle(
                   (int)(Position.X - InternalWidth  / 2f) - 32,
                   (int)(Position.Y - InternalHeight / 2f) - 32,
                   InternalWidth  + 64,
                   InternalHeight + 64);
       }
       ```

    4. Créer src/SDK.MonoGame/Rendering/TilemapRenderer.cs (stub) :
       ```csharp
       namespace SDK.MonoGame.Rendering;

       using Microsoft.Xna.Framework.Graphics;

       public class TilemapRenderer
       {
           // Plan 03-04+ : chargement .tmx via MonoGame.Extended.Tiled
           // Stub pour l'instant — compile mais ne dessine rien
           public void Draw(SpriteBatch sb, Camera2D camera) { }
       }
       ```

    5. Créer src/SDK.MonoGame/Rendering/Shaders/xBR.fx (stub HLSL) :
       ```hlsl
       // xBR Upscaling Shader — stub pour Plan 03-04
       // Référence implémentation : https://github.com/libretro/glsl-shaders/tree/master/xbr
       sampler2D Texture : register(s0);

       float4 MainPS(float2 texCoord : TEXCOORD0) : COLOR0
       {
           return tex2D(Texture, texCoord);  // passthrough — xBR réel en Plan 03-04
       }

       technique Technique1
       {
           pass Pass1
           {
               PixelShader = compile ps_3_0 MainPS();
           }
       }
       ```
       Note : pas ajouté à Content.mgcb — compilation shader en Plan 03-04 (CI MGCB).

    6. Créer src/SDK.MonoGame/Rendering/Shaders/DayNight.fx (stub HLSL) :
       ```hlsl
       // DayNight Shader — stub pour Plan 03-04
       sampler2D Texture : register(s0);
       float4 Tint;

       float4 MainPS(float2 texCoord : TEXCOORD0) : COLOR0
       {
           return tex2D(Texture, texCoord) * Tint;
       }

       technique Technique1
       {
           pass Pass1
           {
               PixelShader = compile ps_3_0 MainPS();
           }
       }
       ```
  </action>
  <verify>dotnet build PokemonSDK.slnx -q 2>&1 | tail -3</verify>
  <done>AC-3 satisfait : RenderPipeline complet, Camera2D, shaders stubs — build 0 erreur</done>
</task>

<task type="auto">
  <name>Task 3: WorldSystem + PlayerSystem + KeyboardInputProvider + IGameClock/IEncounterSystem/IWeatherSystem wiring</name>
  <files>
    src/SDK.MonoGame/World/WorldSystem.cs,
    src/SDK.MonoGame/World/PlayerSystem.cs,
    src/SDK.MonoGame/Input/KeyboardInputProvider.cs
  </files>
  <action>
    1. Créer src/SDK.MonoGame/World/WorldSystem.cs :
       ```csharp
       namespace SDK.MonoGame.World;

       using SDK.Core.Enums;
       using SDK.Core.Interfaces;

       public class WorldSystem
       {
           private readonly IGameClock        _clock;
           private readonly IEncounterSystem  _encounters;
           private readonly IWeatherSystem    _weather;

           public WeatherType CurrentWeather { get; private set; } = WeatherType.None;
           public string      CurrentZoneId  { get; set; }         = "test-zone";
           public int         CurrentGen     { get; set; }         = 1;

           public WorldSystem(IGameClock clock, IEncounterSystem encounters, IWeatherSystem weather)
           {
               _clock      = clock;
               _encounters = encounters;
               _weather    = weather;
           }

           public void Update(TimeSpan delta)
           {
               _clock.Update(delta);
               CurrentWeather = _weather.GetWeather(BiomeType.Grass, _clock.GetTimeOfDay());
           }

           public bool CheckWildEncounter()
           {
               var zones = _encounters.GetZonesByIdentifier(CurrentZoneId, CurrentGen);
               foreach (var zone in zones)
               {
                   if ((float)zone.SpawnRate > Random.Shared.NextSingle() * 0.5f)
                       return true;
               }
               return false;
           }
       }
       ```
       Note : `Random.Shared` thread-safe (.NET 6+). Stub zone "test-zone" — remplacé Plan 03-04+.

    2. Créer src/SDK.MonoGame/World/PlayerSystem.cs :
       ```csharp
       namespace SDK.MonoGame.World;

       using Microsoft.Xna.Framework;
       using SDK.Core.Interfaces;

       public class PlayerSystem
       {
           private readonly WorldSystem   _world;
           private readonly IInputProvider _input;

           public Vector2 Position { get; private set; } = new Vector2(240, 135);  // centre 480×270
           public const int TileSize = 16;  // D-14 : tiles 16×16

           public PlayerSystem(WorldSystem world, IInputProvider input)
           {
               _world = world;
               _input = input;
           }

           public void Update()
           {
               var moved = false;
               if (_input.IsActionJustPressed("Up"))    { Position -= new Vector2(0, TileSize); moved = true; }
               if (_input.IsActionJustPressed("Down"))  { Position += new Vector2(0, TileSize); moved = true; }
               if (_input.IsActionJustPressed("Left"))  { Position -= new Vector2(TileSize, 0); moved = true; }
               if (_input.IsActionJustPressed("Right")) { Position += new Vector2(TileSize, 0); moved = true; }

               if (moved) _ = _world.CheckWildEncounter();  // fire-and-forget (Plan 03-04 intègre battle)
           }
       }
       ```
       Note : TileSize 16 conforme D-14. Collision layer ajoutée en Plan 03-04.

    3. Créer src/SDK.MonoGame/Input/KeyboardInputProvider.cs :
       ```csharp
       namespace SDK.MonoGame.Input;

       using Microsoft.Xna.Framework.Input;
       using SDK.Core.Interfaces;

       public class KeyboardInputProvider : IInputProvider
       {
           private KeyboardState _current;
           private KeyboardState _previous;

           public void Update()
           {
               _previous = _current;
               _current  = Keyboard.GetState();
           }

           public bool IsActionPressed(string action) => action switch
           {
               "Up"    => _current.IsKeyDown(Keys.Up)    || _current.IsKeyDown(Keys.W),
               "Down"  => _current.IsKeyDown(Keys.Down)  || _current.IsKeyDown(Keys.S),
               "Left"  => _current.IsKeyDown(Keys.Left)  || _current.IsKeyDown(Keys.A),
               "Right" => _current.IsKeyDown(Keys.Right) || _current.IsKeyDown(Keys.D),
               "A"     => _current.IsKeyDown(Keys.Z)     || _current.IsKeyDown(Keys.Enter),
               "B"     => _current.IsKeyDown(Keys.X)     || _current.IsKeyDown(Keys.Back),
               "Start" => _current.IsKeyDown(Keys.Escape),
               _       => false,
           };

           public bool IsActionJustPressed(string action)
           {
               bool nowPressed  = IsActionPressed(action);
               var prevProvider = new KeyboardInputProvider();
               // compare avec état précédent — impl simplifiée
               return nowPressed && _current != _previous && IsKeyJustPressed(action);
           }

           private bool IsKeyJustPressed(string action) => action switch
           {
               "Up"    => WasJust(Keys.Up)    || WasJust(Keys.W),
               "Down"  => WasJust(Keys.Down)  || WasJust(Keys.S),
               "Left"  => WasJust(Keys.Left)  || WasJust(Keys.A),
               "Right" => WasJust(Keys.Right) || WasJust(Keys.D),
               "A"     => WasJust(Keys.Z)     || WasJust(Keys.Enter),
               "B"     => WasJust(Keys.X)     || WasJust(Keys.Back),
               "Start" => WasJust(Keys.Escape),
               _       => false,
           };

           private bool WasJust(Keys key)
               => _current.IsKeyDown(key) && !_previous.IsKeyDown(key);
       }
       ```
       Note : HeadlessRunner utilise NullInputProvider (pas KeyboardInputProvider) — voir Task 1.

    4. Ajouter NullInputProvider pour HeadlessRunner dans src/SDK.MonoGame/Input/NullInputProvider.cs :
       ```csharp
       namespace SDK.MonoGame.Input;
       using SDK.Core.Interfaces;

       public class NullInputProvider : IInputProvider
       {
           public bool IsActionPressed(string action)     => false;
           public bool IsActionJustPressed(string action) => false;
       }
       ```

    5. Mettre à jour Program.cs Task 1 — enregistrer NullInputProvider si headless, KeyboardInputProvider sinon :
       ```csharp
       if (isHeadless)
           services.AddSingleton<IInputProvider, NullInputProvider>();
       else
           services.AddSingleton<IInputProvider, KeyboardInputProvider>();
       ```
       Ajouter ces deux lignes AVANT le `services.AddSingleton<WorldSystem>()` dans Program.cs.

    Vérification finale :
      dotnet build PokemonSDK.slnx -q → 0 erreurs
      dotnet test PokemonSDK.slnx -q → 74/74 verts (aucune régression)
  </action>
  <verify>dotnet build PokemonSDK.slnx -q && dotnet test PokemonSDK.slnx -q 2>&1 | tail -5</verify>
  <done>AC-2 + AC-4 + AC-5 satisfaits : WorldSystem/PlayerSystem wirés, HeadlessRunner exécutable, 74 tests verts</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Core/Entities/ (entités figées Plans 01-02)
- src/SDK.Core/Services/ (RealTimeClock, GameTimeClock, WeatherSystem — Plan 03-02)
- src/SDK.Data/ (EncounterSystem, WorldDataSeeder, migrations — Plans 03-01/03-02)
- src/SDK.Battle/ (hors scope Phase 3)
- tests/ (74 tests existants ne doivent pas régresser)
- PokemonSDK.slnx (modifier uniquement pour ajouter SDK.MonoGame — pas supprimer/renommer)

## SCOPE LIMITS
- Pas de .tmx tilemap réel — WorldSystem stub zone "test-zone" (Plan 03-04+)
- Pas de sprites joueur/NPC — PlayerSystem position seulement (Plan 03-04+)
- Pas d'UI (DialogueBox, BattleUI, HUD) — Phase 4
- Pas d'audio (AudioManager) — Phase 7
- Pas de compilation MGCB des shaders .fx — Content.mgcb reste vide (Plan 03-04 CI)
- SDK.MonoGame ne référence pas SDK.Scripting (D-06) — IScriptEngine via Func en Phase 4
- xBR shader = passthrough stub — implémentation complète quand CI MGCB disponible
- Pas de test xUnit SDK.MonoGame.Tests dans ce plan — Plan 03-04 le crée

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] dotnet build PokemonSDK.slnx -q → 0 erreurs, 0 warnings critiques
- [ ] dotnet test PokemonSDK.slnx -q → 74/74 verts (pas de régression)
- [ ] dotnet list src/SDK.MonoGame/SDK.MonoGame.csproj package → MonoGame.Framework.DesktopGL présent (D-02)
- [ ] grep -r "MonoGame.WindowsDX" src/SDK.MonoGame/ → 0 résultats (D-02)
- [ ] grep -r "SDK.Scripting" src/SDK.MonoGame/ → 0 résultats (D-06)
- [ ] src/SDK.MonoGame/Rendering/RenderPipeline.cs → _internalTarget = RenderTarget2D(gd, 480, 270) (D-14)
- [ ] HeadlessRunner.Run() sans SDL_Init/GraphicsDevice (pure C# loop — Plan 03-04 écrit les tests)
</verification>

<success_criteria>
- Tous les tasks complétés
- 0 erreurs de build sur PokemonSDK.slnx
- AC-1 → AC-5 tous satisfaits
- 74 tests existants verts (zéro régression)
- SDK.MonoGame zéro ref SDK.Scripting (D-06)
- RenderPipeline 480×270 → 3 passes (D-14 + D-15 code)
- HeadlessRunner exécutable sans GL context (prêt pour Plan 03-04 tests)
</success_criteria>

<output>
Après complétion, créer `.paul/phases/03-world-foundation/03-03-SUMMARY.md`
</output>

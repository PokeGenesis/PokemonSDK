---
phase: 03-world-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SDK.Core/Entities/EncounterZone.cs
  - src/SDK.Core/Enums/BiomeType.cs
  - src/SDK.Core/Interfaces/IEncounterSystem.cs
  - src/SDK.Data/PokemonDbContext.cs
  - src/SDK.Data/Configurations/EncounterZoneConfiguration.cs
  - src/SDK.Data/Migrations/[generated]
  - src/SDK.Data/Seeding/BattleDataSeeder.cs
  - tests/SDK.Data.Tests/BattleTranslationsD22Tests.cs
autonomous: true
---

<objective>
## Goal
Poser les fondations data de Phase 3 : entité EncounterZone + interface IEncounterSystem dans SDK.Core, Migration 003 (encounter_zones), et combler le gap D-22 avec SeedMoveTranslations + SeedAbilityTranslations (15 moves × 6 locales + 6 abilities × 6 locales).

## Purpose
Plan 03-02 (EncounterSystem) et Plan 03-03 (Game1/WorldSystem) ont besoin de ces fondations. Les translations Move/Ability sont un bug de conformité D-22 issu de Phase 2 — elles bloquent toute feature multilingue sur les capacités et talents.

## Output
- `EncounterZone` entity + `BiomeType` enum + `IEncounterSystem` interface dans SDK.Core
- Migration 003 `AddWorldData` avec table `encounter_zones`
- `BattleDataSeeder.SeedMoveTranslations` + `SeedAbilityTranslations` (90 + 36 = 126 rows)
- Test `BattleTranslationsD22Tests` vérifiant les counts D-22
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/02-battle-engine-core/02-04-SUMMARY.md

## Source Files
@src/SDK.Core/Entities/PokemonSpecies.cs
@src/SDK.Core/Entities/Translation.cs
@src/SDK.Data/PokemonDbContext.cs
@src/SDK.Data/Configurations/PokemonTypeConfiguration.cs
@src/SDK.Data/Seeding/BattleDataSeeder.cs
@src/SDK.Data/Seeding/DataSeeder.cs
@tests/SDK.Data.Tests/
</context>

<acceptance_criteria>

## AC-1: EncounterZone world primitives dans SDK.Core sans dépendance externe
```gherkin
Given SDK.Core doit rester à zéro NuGet externe (règle dépendances)
When EncounterZone, BiomeType et IEncounterSystem sont ajoutés
Then `dotnet build PokemonSDK.slnx -q` passe sans erreur
  AND `dotnet list src/SDK.Core/SDK.Core.csproj package` retourne zéro PackageReference
  AND CoreBattleDependencyTests 4/4 verts
```

## AC-2: Table encounter_zones présente après Migration 003
```gherkin
Given PokemonDbContext est mis à jour avec DbSet<EncounterZone>
When la migration AddWorldData est générée et appliquée
Then `sqlite3 src/SDK.Data/data/PokemonSDK.db ".tables"` inclut encounter_zones
  AND les 47 tests existants restent verts (0 régression)
```

## AC-3: Translations D-22 — 15 moves × 6 locales + 6 abilities × 6 locales
```gherkin
Given BattleDataSeeder.SeedAll() est appelé sur une DB in-memory vide
When SeedMoveTranslations et SeedAbilityTranslations s'exécutent
Then ctx.Translations.Count(t => t.EntityType == "Move") == 90
  AND ctx.Translations.Count(t => t.EntityType == "Ability") == 36
  AND BattleTranslationsD22Tests passe (2 assertions)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: SDK.Core world primitives (EncounterZone, BiomeType, IEncounterSystem)</name>
  <files>
    src/SDK.Core/Entities/EncounterZone.cs,
    src/SDK.Core/Enums/BiomeType.cs,
    src/SDK.Core/Interfaces/IEncounterSystem.cs
  </files>
  <action>
    Créer `src/SDK.Core/Entities/EncounterZone.cs` :
    - namespace SDK.Core.Entities
    - Propriétés : Id (int PK), ZoneIdentifier (string), Generation (int, D-09), BiomeType (BiomeType),
      SpeciesId (int FK vers PokemonSpecies), MinLevel (int), MaxLevel (int), SpawnRate (decimal 0.0-1.0)
    - Initialiser ZoneIdentifier = string.Empty (pattern existant)

    Créer `src/SDK.Core/Enums/BiomeType.cs` :
    - namespace SDK.Core.Enums
    - Valeurs : Grass, Cave, Water, Building, Route

    Créer `src/SDK.Core/Interfaces/IEncounterSystem.cs` :
    - namespace SDK.Core.Interfaces
    - Interface avec une seule méthode stub pour Plan 03-02 :
      `IEnumerable<EncounterZone> GetZones(int generation);`
    - using SDK.Core.Entities

    Éviter : toute référence NuGet, toute logique d'implémentation (SDK.Core = domaine pur).
  </action>
  <verify>
    dotnet build PokemonSDK.slnx -q
    dotnet list src/SDK.Core/SDK.Core.csproj package  # doit afficher zéro résultat
    dotnet test PokemonSDK.slnx -q --filter "SDK.Core.Tests"  # 4/4 verts
  </verify>
  <done>AC-1 satisfait : SDK.Core build propre, 0 NuGet, 4/4 CoreDependencyTests verts</done>
</task>

<task type="auto">
  <name>Task 2: Migration 003 AddWorldData + PokemonDbContext update</name>
  <files>
    src/SDK.Data/PokemonDbContext.cs,
    src/SDK.Data/Configurations/EncounterZoneConfiguration.cs,
    src/SDK.Data/Migrations/[generé par EF]
  </files>
  <action>
    Modifier `src/SDK.Data/PokemonDbContext.cs` :
    - Ajouter `public DbSet<EncounterZone> EncounterZones => Set<EncounterZone>();`
    - Ajouter `using SDK.Core.Entities;` si absent

    Créer `src/SDK.Data/Configurations/EncounterZoneConfiguration.cs` :
    - namespace SDK.Data.Configurations
    - Implémenter IEntityTypeConfiguration<EncounterZone>
    - HasKey(e => e.Id)
    - Property ZoneIdentifier : IsRequired(), HasMaxLength(100)
    - Property Generation : IsRequired() (D-09)
    - Property SpawnRate : HasColumnType("REAL")
    - Property BiomeType : HasConversion<int>() (enum → int SQLite)
    - HasOne + WithMany pour FK SpeciesId vers PokemonSpecies (optional FK — pas de cascade delete forcée)

    Générer la migration :
    `dotnet ef migrations add AddWorldData --project src/SDK.Data --startup-project src/SDK.MonoGame`

    Appliquer :
    `dotnet ef database update --project src/SDK.Data --startup-project src/SDK.MonoGame`

    Éviter : data seeds via migrations (uniquement schema DDL).
  </action>
  <verify>
    sqlite3 src/SDK.Data/data/PokemonSDK.db ".tables"  # doit inclure encounter_zones
    dotnet test PokemonSDK.slnx -q  # 47/47 verts (0 régression)
  </verify>
  <done>AC-2 satisfait : encounter_zones présente en DB, 47 tests verts</done>
</task>

<task type="auto">
  <name>Task 3: SeedMoveTranslations + SeedAbilityTranslations + test D-22</name>
  <files>
    src/SDK.Data/Seeding/BattleDataSeeder.cs,
    tests/SDK.Data.Tests/BattleTranslationsD22Tests.cs
  </files>
  <action>
    Dans `BattleDataSeeder.cs`, ajouter deux méthodes publiques statiques :

    `SeedMoveTranslations(PokemonDbContext ctx)` :
    - Guard: if (ctx.Translations.Any(t => t.EntityType == "Move")) return;
    - Utiliser exactement le même pattern tuple que SeedTypeTranslations dans DataSeeder.cs
    - 15 moves (IDs 1-15) avec noms officiels en 6 locales (en/es/fr/de/it/ja) :
      1=Tackle, 2=Growl, 3=Scratch, 4=Tail Whip, 5=Thunder Shock, 6=Ember,
      7=Water Gun, 8=Vine Whip, 9=Flamethrower, 10=Thunderbolt, 11=Surf,
      12=Solar Beam, 13=Hyper Beam, 14=Swords Dance, 15=Quick Attack
      (EntityType = "Move", Field = "name")
    - Appeler ctx.SaveChanges() après

    `SeedAbilityTranslations(PokemonDbContext ctx)` :
    - Guard: if (ctx.Translations.Any(t => t.EntityType == "Ability")) return;
    - 6 abilities (IDs 1-6) avec noms officiels en 6 locales :
      1=Overgrow, 2=Blaze, 3=Torrent, 4=Static, 5=Intimidate, 6=Keen Eye
      (EntityType = "Ability", Field = "name")
    - Appeler ctx.SaveChanges() après

    Dans `BattleDataSeeder.SeedAll()` :
    - Ajouter SeedMoveTranslations(ctx) et SeedAbilityTranslations(ctx) après SeedAbilities(ctx)

    Créer `tests/SDK.Data.Tests/BattleTranslationsD22Tests.cs` :
    - Classe BattleTranslationsD22Tests avec SqliteTestFixture (pattern Plan 01-02)
    - Test `MoveTranslations_AllSixLocales_Seeded()` :
      assert ctx.Translations.Count(t => t.EntityType == "Move") == 90
    - Test `AbilityTranslations_AllSixLocales_Seeded()` :
      assert ctx.Translations.Count(t => t.EntityType == "Ability") == 36
    - Appeler BattleDataSeeder.SeedAll(ctx) + DataSeeder.SeedAll(ctx) dans setup

    Éviter : EntityType = "move" en minuscules (utiliser "Move" comme pattern PokemonType/PokemonSpecies).
  </action>
  <verify>
    dotnet test PokemonSDK.slnx -q  # tous les tests verts + 2 nouveaux BattleTranslationsD22Tests
    sqlite3 src/SDK.Data/data/PokemonSDK.db "SELECT COUNT(*) FROM Translations WHERE EntityType='Move';"
    # doit retourner 90 après re-seed
  </verify>
  <done>AC-3 satisfait : 90 Move translations + 36 Ability translations vérifiées, deferred issues D-22 résolus</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/SDK.Battle/` — moteur de combat verrouillé Phase 2
- `src/SDK.Data/Migrations/20260602165634_InitialCreate.*` — migration 001 verrouillée
- `src/SDK.Data/Migrations/20260603200241_AddBattleData.*` — migration 002 verrouillée
- `tests/SDK.Battle.Tests/` — 30 tests Phase 2, ne pas modifier
- `tests/SDK.Core.Tests/` — 4 tests Platform Phase 1, ne pas modifier

## SCOPE LIMITS
- Pas de seed de données EncounterZone dans ce plan (table vide = normal, Plan 03-02 seed)
- Pas de WorldConfig value object (Plan 03-03, context MonoGame)
- Pas de IEncounterZoneRepository ni implémentation IEncounterSystem (Plan 03-02)
- Pas de modifications à Game1.cs ni SDK.MonoGame (Plan 03-03)

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] `dotnet build PokemonSDK.slnx -q` — 0 erreurs, 0 warnings
- [ ] `dotnet list src/SDK.Core/SDK.Core.csproj package` — 0 résultats (AC-1)
- [ ] `sqlite3 src/SDK.Data/data/PokemonSDK.db ".tables"` — inclut encounter_zones (AC-2)
- [ ] `dotnet test PokemonSDK.slnx -q` — tous les tests verts, 2 nouveaux D-22 tests inclus (AC-3)
- [ ] `dotnet test PokemonSDK.slnx -q --filter "BattleTranslationsD22Tests"` — 2/2 verts
</verification>

<success_criteria>
- Toutes les tâches complétées
- Tous les checks de vérification passent
- 0 régression sur les 47 tests existants
- 2 nouveaux tests BattleTranslationsD22Tests verts
- Deferred issues "Translations Move manquantes" et "Translations Ability manquantes" résolus
</success_criteria>

<output>
Après completion, créer `.paul/phases/03-world-foundation/03-01-SUMMARY.md`
</output>

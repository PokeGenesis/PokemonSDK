---
phase: 03-world-foundation
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/SDK.Core/Interfaces/IRealTimeClock.cs
  - src/SDK.Core/Interfaces/IWeatherSystem.cs
  - src/SDK.Core/Interfaces/IEncounterSystem.cs
  - src/SDK.Core/Services/RealTimeClock.cs
  - src/SDK.Core/Services/WeatherSystem.cs
  - src/SDK.Data/Services/EncounterSystem.cs
  - src/SDK.Data/Seeding/WorldDataSeeder.cs
  - tests/SDK.Core.Tests/WorldServicesTests.cs
  - tests/SDK.Data.Tests/EncounterSystemTests.cs
autonomous: true
---

<objective>
## Goal
Implémenter les trois services world layer dont Game1 + WorldSystem (Plan 03-03) ont besoin : RealTimeClock (heure → TimeOfDay), WeatherSystem (biome + heure → WeatherType), et EncounterSystem (requête DB des zones de rencontre).

## Purpose
Phase 3 goal : joueur sur tilemap → rencontre sauvage → battle engine. Ce plan crée les services qui déterminent QUAND et QUOI rencontrer, et quelle météo s'applique. Sans ces services, Plan 03-03 (Game1/WorldSystem) n'a rien à brancher en DI.

## Output
- 5 nouveaux fichiers SDK.Core (2 interfaces + 2 services + 1 interface étendue)
- EncounterSystem dans SDK.Data/Services/
- WorldDataSeeder dans SDK.Data/Seeding/ (5 zones Gen 1, SpeciesId 1/25/175)
- 2 fichiers de tests couvrant les 3 services — cible ≥10 nouveaux tests verts

Note : TimeOfDay et WeatherType existent déjà dans SDK.Core/Enums/ — ne pas recréer.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/03-world-foundation/03-01-SUMMARY.md

## Source Files
@src/SDK.Core/Interfaces/IEncounterSystem.cs
@src/SDK.Core/Entities/EncounterZone.cs
@src/SDK.Core/Enums/BiomeType.cs
@src/SDK.Core/Enums/TimeOfDay.cs
@src/SDK.Core/Enums/WeatherType.cs
@src/SDK.Data/Seeding/DataSeeder.cs
@tests/SDK.Data.Tests/BattleTranslationsD22Tests.cs
</context>

<!-- No SPECIAL-FLOWS.md in this project — skills section omitted -->

<acceptance_criteria>

## AC-1: SDK.Core world services sans dépendance NuGet externe
```gherkin
Given SDK.Core/Services/ contient RealTimeClock et WeatherSystem
When dotnet list src/SDK.Core/SDK.Core.csproj package
Then 0 packages listés — SDK.Core reste pur (invariant D-01)
```

## AC-2: RealTimeClock mappe l'heure UTC → TimeOfDay correctement
```gherkin
Given RealTimeClock instancié avec Func<DateTime> injectable (heure contrôlée)
When GetTimeOfDay() appelé pour heures 0, 6, 11, 17, 21
Then Night / Morning / Day / Evening / Night retournés — 8+ InlineData verts
```

## AC-3: WeatherSystem retourne la météo correcte par biome et heure
```gherkin
Given WeatherSystem instancié (aucune dépendance)
When GetWeather(BiomeType.Cave, _) et GetWeather(BiomeType.Water, TimeOfDay.Night)
Then Cave → WeatherType.None (abri), Water/Night → WeatherType.Rain — 6+ assertions vertes
```

## AC-4: EncounterSystem lit les zones depuis la DB in-memory
```gherkin
Given EncounterSystemTests utilise SqliteTestFixture + DataSeeder.SeedAll + WorldDataSeeder.SeedAll
When GetZones(1) et GetZonesByIdentifier("pallet-route-1", 1) et GetZones(99)
Then zones Gen 1 retournées, GetZonesByIdentifier filtre par identifiant, génération inconnue → vide
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: SDK.Core — IRealTimeClock + RealTimeClock + IWeatherSystem + WeatherSystem</name>
  <files>
    src/SDK.Core/Interfaces/IRealTimeClock.cs,
    src/SDK.Core/Interfaces/IWeatherSystem.cs,
    src/SDK.Core/Services/RealTimeClock.cs,
    src/SDK.Core/Services/WeatherSystem.cs
  </files>
  <action>
    Créer src/SDK.Core/Interfaces/IRealTimeClock.cs :
      namespace SDK.Core.Interfaces;
      public interface IRealTimeClock
      {
          TimeOfDay GetTimeOfDay();
      }

    Créer src/SDK.Core/Interfaces/IWeatherSystem.cs :
      namespace SDK.Core.Interfaces;
      using SDK.Core.Enums;
      public interface IWeatherSystem
      {
          WeatherType GetWeather(BiomeType biome, TimeOfDay time);
      }

    Créer src/SDK.Core/Services/RealTimeClock.cs :
      namespace SDK.Core.Services;
      using SDK.Core.Enums;
      using SDK.Core.Interfaces;
      public class RealTimeClock(Func<DateTime>? clock = null) : IRealTimeClock
      {
          private readonly Func<DateTime> _clock = clock ?? (() => DateTime.UtcNow);
          public TimeOfDay GetTimeOfDay()
          {
              int hour = _clock().Hour;
              return hour switch
              {
                  >= 6 and < 11  => TimeOfDay.Morning,
                  >= 11 and < 17 => TimeOfDay.Day,
                  >= 17 and < 21 => TimeOfDay.Evening,
                  _              => TimeOfDay.Night,
              };
          }
      }
      Note : Func<DateTime> injectable → testable sans InternalsVisibleTo.
      Plages : Morning=[6,11), Day=[11,17), Evening=[17,21), Night=reste (21-23 + 0-5).

    Créer src/SDK.Core/Services/WeatherSystem.cs :
      namespace SDK.Core.Services;
      using SDK.Core.Enums;
      using SDK.Core.Interfaces;
      public class WeatherSystem : IWeatherSystem
      {
          public WeatherType GetWeather(BiomeType biome, TimeOfDay time) =>
              (biome, time) switch
              {
                  (BiomeType.Cave, _)                  => WeatherType.None,
                  (BiomeType.Water, TimeOfDay.Night)   => WeatherType.Rain,
                  (BiomeType.Water, TimeOfDay.Evening) => WeatherType.Rain,
                  (BiomeType.Building, _)              => WeatherType.None,
                  _                                    => WeatherType.None,
              };
      }
      Note : Pure function, pas d'état mutable. WeatherType.None = pas d'effet météo
      (ciel dégagé). La gestion de la météo dynamique (Sun/Sand/Hail) viendra
      des moves en combat — hors scope ici.
      Éviter : dépendances injectées, état interne, logs.
  </action>
  <verify>dotnet build src/SDK.Core/SDK.Core.csproj -q && dotnet list src/SDK.Core/SDK.Core.csproj package</verify>
  <done>AC-1 satisfait : SDK.Core compile sans erreur, 0 packages NuGet listés</done>
</task>

<task type="auto">
  <name>Task 2: IEncounterSystem étendu + EncounterSystem (SDK.Data) + WorldDataSeeder</name>
  <files>
    src/SDK.Core/Interfaces/IEncounterSystem.cs,
    src/SDK.Data/Services/EncounterSystem.cs,
    src/SDK.Data/Seeding/WorldDataSeeder.cs
  </files>
  <action>
    Modifier src/SDK.Core/Interfaces/IEncounterSystem.cs :
      Ajouter la méthode : IEnumerable<EncounterZone> GetZonesByIdentifier(string zoneIdentifier, int generation);
      Garder : IEnumerable<EncounterZone> GetZones(int generation);
      Résultat :
        public interface IEncounterSystem
        {
            IEnumerable<EncounterZone> GetZones(int generation);
            IEnumerable<EncounterZone> GetZonesByIdentifier(string zoneIdentifier, int generation);
        }

    Créer src/SDK.Data/Services/EncounterSystem.cs :
      namespace SDK.Data.Services;
      using SDK.Core.Entities;
      using SDK.Core.Interfaces;
      public class EncounterSystem(PokemonDbContext ctx) : IEncounterSystem
      {
          public IEnumerable<EncounterZone> GetZones(int generation)
              => ctx.EncounterZones.Where(e => e.Generation == generation).ToList();

          public IEnumerable<EncounterZone> GetZonesByIdentifier(string zoneIdentifier, int generation)
              => ctx.EncounterZones
                    .Where(e => e.ZoneIdentifier == zoneIdentifier && e.Generation == generation)
                    .ToList();
      }
      Note : Primary constructor pattern (.NET 8+). ToList() matérialise — pas de
      tracking EF requis pour lectures pures.

    Créer src/SDK.Data/Seeding/WorldDataSeeder.cs :
      namespace SDK.Data.Seeding;
      using SDK.Core.Entities;
      using SDK.Core.Enums;
      public static class WorldDataSeeder
      {
          public static void SeedAll(PokemonDbContext ctx) => SeedEncounterZones(ctx);

          public static void SeedEncounterZones(PokemonDbContext ctx)
          {
              if (ctx.EncounterZones.Any()) return;
              ctx.EncounterZones.AddRange(
                  new EncounterZone { Id=1, ZoneIdentifier="pallet-route-1", Generation=1,
                      BiomeType=BiomeType.Grass, SpeciesId=1,  MinLevel=2, MaxLevel=5,  SpawnRate=0.10m },
                  new EncounterZone { Id=2, ZoneIdentifier="pallet-route-1", Generation=1,
                      BiomeType=BiomeType.Grass, SpeciesId=25, MinLevel=3, MaxLevel=6,  SpawnRate=0.15m },
                  new EncounterZone { Id=3, ZoneIdentifier="pallet-route-1", Generation=1,
                      BiomeType=BiomeType.Grass, SpeciesId=175,MinLevel=2, MaxLevel=4,  SpawnRate=0.05m },
                  new EncounterZone { Id=4, ZoneIdentifier="viridian-forest", Generation=1,
                      BiomeType=BiomeType.Cave,  SpeciesId=25, MinLevel=4, MaxLevel=7,  SpawnRate=0.20m },
                  new EncounterZone { Id=5, ZoneIdentifier="viridian-forest", Generation=1,
                      BiomeType=BiomeType.Grass, SpeciesId=1,  MinLevel=3, MaxLevel=6,  SpawnRate=0.12m }
              );
              ctx.SaveChanges();
          }
      }
      Note : SpeciesId 1/25/175 correspondent aux espèces seedées par DataSeeder.SeedSpecies.
      FK Restrict — DataSeeder.SeedAll() doit être appelé avant WorldDataSeeder.SeedAll().
  </action>
  <verify>dotnet build PokemonSDK.slnx -q</verify>
  <done>AC-4 préparé : EncounterSystem + WorldDataSeeder compilent, FKs valides</done>
</task>

<task type="auto">
  <name>Task 3: Tests — WorldServicesTests (SDK.Core.Tests) + EncounterSystemTests (SDK.Data.Tests)</name>
  <files>
    tests/SDK.Core.Tests/WorldServicesTests.cs,
    tests/SDK.Data.Tests/EncounterSystemTests.cs
  </files>
  <action>
    Créer tests/SDK.Core.Tests/WorldServicesTests.cs :
      namespace SDK.Core.Tests;
      using FluentAssertions;
      using SDK.Core.Enums;
      using SDK.Core.Services;

      public class RealTimeClockTests
      {
          [Theory]
          [InlineData(0,  TimeOfDay.Night)]
          [InlineData(5,  TimeOfDay.Night)]
          [InlineData(6,  TimeOfDay.Morning)]
          [InlineData(10, TimeOfDay.Morning)]
          [InlineData(11, TimeOfDay.Day)]
          [InlineData(16, TimeOfDay.Day)]
          [InlineData(17, TimeOfDay.Evening)]
          [InlineData(20, TimeOfDay.Evening)]
          [InlineData(21, TimeOfDay.Night)]
          [InlineData(23, TimeOfDay.Night)]
          public void GetTimeOfDay_MapsHourCorrectly(int hour, TimeOfDay expected)
          {
              var clock = new RealTimeClock(() => new DateTime(2026, 6, 4, hour, 0, 0, DateTimeKind.Utc));
              clock.GetTimeOfDay().Should().Be(expected);
          }
      }

      public class WeatherSystemTests
      {
          private readonly WeatherSystem _sut = new();

          [Fact] public void Cave_AlwaysFog()
          {
              _sut.GetWeather(BiomeType.Cave, TimeOfDay.Morning).Should().Be(WeatherType.None);
              _sut.GetWeather(BiomeType.Cave, TimeOfDay.Day).Should().Be(WeatherType.None);
              _sut.GetWeather(BiomeType.Cave, TimeOfDay.Night).Should().Be(WeatherType.None);
          }
          [Fact] public void Water_NightAndEvening_Rain()
          {
              _sut.GetWeather(BiomeType.Water, TimeOfDay.Night).Should().Be(WeatherType.Rain);
              _sut.GetWeather(BiomeType.Water, TimeOfDay.Evening).Should().Be(WeatherType.Rain);
          }
          [Fact] public void Grass_Day_Clear()
              => _sut.GetWeather(BiomeType.Grass, TimeOfDay.Day).Should().Be(WeatherType.None);
          [Fact] public void Route_Morning_Clear()
              => _sut.GetWeather(BiomeType.Route, TimeOfDay.Morning).Should().Be(WeatherType.None);
          [Fact] public void Building_AlwaysClear()
          {
              _sut.GetWeather(BiomeType.Building, TimeOfDay.Night).Should().Be(WeatherType.None);
              _sut.GetWeather(BiomeType.Building, TimeOfDay.Day).Should().Be(WeatherType.None);
          }
      }
      Note : 10 InlineData RealTimeClock + 7 assertions WeatherSystem = 17 vérifications.

    Créer tests/SDK.Data.Tests/EncounterSystemTests.cs :
      namespace SDK.Data.Tests;
      using FluentAssertions;
      using SDK.Data.Seeding;
      using SDK.Data.Services;

      public class EncounterSystemTests
      {
          private static EncounterSystem CreateSeeded(out SqliteTestFixture fixture)
          {
              fixture = new SqliteTestFixture();
              using var ctx = fixture.CreateContext();
              DataSeeder.SeedAll(ctx);
              WorldDataSeeder.SeedAll(ctx);
              return new EncounterSystem(fixture.CreateContext());
          }

          [Fact]
          public void GetZones_ReturnsZonesForKnownGeneration()
          {
              var sut = CreateSeeded(out var fixture);
              using var _ = fixture;
              sut.GetZones(1).Should().NotBeEmpty();
          }

          [Fact]
          public void GetZones_ReturnsEmptyForUnknownGeneration()
          {
              var sut = CreateSeeded(out var fixture);
              using var _ = fixture;
              sut.GetZones(99).Should().BeEmpty();
          }

          [Fact]
          public void GetZonesByIdentifier_ReturnsMatchingZones()
          {
              var sut = CreateSeeded(out var fixture);
              using var _ = fixture;
              sut.GetZonesByIdentifier("pallet-route-1", 1).Should().HaveCount(3);
          }

          [Fact]
          public void GetZonesByIdentifier_ReturnsEmptyForUnknownZone()
          {
              var sut = CreateSeeded(out var fixture);
              using var _ = fixture;
              sut.GetZonesByIdentifier("unknown-zone", 1).Should().BeEmpty();
          }

          [Fact]
          public void GetZonesByIdentifier_AllSpawnRatesPositive()
          {
              var sut = CreateSeeded(out var fixture);
              using var _ = fixture;
              sut.GetZonesByIdentifier("pallet-route-1", 1)
                 .Should().AllSatisfy(z => z.SpawnRate.Should().BePositive());
          }
      }
      Note : CreateSeeded() helper crée fixture + seed dans un ctx, retourne EncounterSystem
      sur un ctx distinct (pattern établi BattleTranslationsD22Tests). 5 tests.

    Vérification finale :
      dotnet test PokemonSDK.slnx -q → cible 49 existants + ≥15 nouveaux = 64+ total, tous verts.
  </action>
  <verify>dotnet test PokemonSDK.slnx -q 2>&1 | tail -5</verify>
  <done>AC-2 + AC-3 + AC-4 satisfaits : tous tests verts, RealTimeClock/WeatherSystem/EncounterSystem couverts</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/SDK.Core/Entities/EncounterZone.cs (entité figée Plan 03-01)
- src/SDK.Core/Enums/BiomeType.cs (figé Plan 03-01)
- src/SDK.Core/Enums/TimeOfDay.cs (existe déjà — ne pas modifier)
- src/SDK.Core/Enums/WeatherType.cs (existe déjà — ne pas modifier)
- src/SDK.Data/Migrations/ (pas de nouvelle migration — schéma inchangé)
- src/SDK.Data/Seeding/BattleDataSeeder.cs (D-22 résolu, ne pas toucher)
- src/SDK.Data/Seeding/DataSeeder.cs (stable)
- src/SDK.Battle/ (hors scope Phase 3 Plan 02)

## SCOPE LIMITS
- Pas de MonoGame (SDK.MonoGame n'existe pas encore — Plan 03-03)
- Pas de migration EF — table encounter_zones créée en Plan 03-01
- WeatherSystem = pure function sans état — l'état météo courant est du ressort de WorldSystem (Plan 03-03)
- RealTimeClock wrap DateTime.UtcNow — la simulation de temps accéléré est Plan 03-03
- Pas de seed PokemonSpecies/Moves supplémentaires — DataSeeder.SeedAll couvre déjà les FKs requises

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] dotnet list src/SDK.Core/SDK.Core.csproj package → 0 résultats (AC-1)
- [ ] dotnet build PokemonSDK.slnx -q → 0 erreurs, 0 warnings pertinents
- [ ] dotnet test PokemonSDK.slnx -q → tous verts (≥64 tests)
- [ ] RealTimeClockTests : 10 InlineData → verts (AC-2)
- [ ] WeatherSystemTests : Cave/Water/Grass/Route/Building → verts (AC-3)
- [ ] EncounterSystemTests : 5 tests GetZones + GetZonesByIdentifier → verts (AC-4)
- [ ] 49 tests existants toujours verts (aucune régression)
</verification>

<success_criteria>
- Tous les tasks complétés
- 0 erreurs de build sur PokemonSDK.slnx
- AC-1 → AC-4 tous satisfaits
- 49 tests existants toujours verts + ≥15 nouveaux tests verts
- SDK.Core zéro dépendance NuGet externe (invariant D-01 maintenu)
- IEncounterSystem étendu avec GetZonesByIdentifier prêt pour Plan 03-03
</success_criteria>

<output>
Après complétion, créer `.paul/phases/03-world-foundation/03-02-SUMMARY.md`
</output>

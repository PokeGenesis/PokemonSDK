---
phase: 03-world-foundation
plan: 04
type: tdd
wave: 1
depends_on: ["03-03"]
files_modified:
  - tests/SDK.MonoGame.Tests/SDK.MonoGame.Tests.csproj
  - tests/SDK.MonoGame.Tests/HeadlessSmokeTests.cs
  - PokemonSDK.slnx
  - .github/workflows/ci.yml
autonomous: true
---

<objective>
## Goal
Créer `tests/SDK.MonoGame.Tests/` avec 3 HeadlessSmokeTests couvrant HeadlessRunner.Run() sans GL context, puis activer le step headless commenté dans `.github/workflows/ci.yml` — résultat : 77/77 tests verts sur ubuntu-latest ET windows-latest.

## Purpose
PLAT-02 (Builds Windows + Linux CI) exige un CI vert sur les deux plateformes.
HeadlessRunner (Plan 03-03) ne peut être prouvé sans tests qui l'appellent directement.
Ce plan ferme Phase 3 : dernière vérification avant de passer à la Phase 4 (Scripting).

## Output
- `tests/SDK.MonoGame.Tests/` — 3 tests HeadlessSmokeTests (Moq IEncounterSystem, pas de DB)
- `PokemonSDK.slnx` — test project enregistré
- `.github/workflows/ci.yml` — TODO Phase 3 remplacé par step headless actif (ubuntu only)
- `dotnet test PokemonSDK.slnx` → 77/77 verts (74 existants + 3 nouveaux)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/03-world-foundation/03-03-SUMMARY.md

## Source Files — HeadlessRunner + interfaces
@src/SDK.MonoGame/HeadlessRunner.cs
@src/SDK.MonoGame/World/WorldSystem.cs
@src/SDK.MonoGame/World/PlayerSystem.cs
@src/SDK.MonoGame/Input/NullInputProvider.cs
@src/SDK.Core/Interfaces/IGameClock.cs
@src/SDK.Core/Interfaces/IEncounterSystem.cs
@src/SDK.Core/Interfaces/IWeatherSystem.cs
@src/SDK.Core/Interfaces/IInputProvider.cs
@src/SDK.Core/Services/GameTimeClock.cs
@src/SDK.Core/Services/WeatherSystem.cs

## Pattern de référence — test project existant
@tests/SDK.Battle.Tests/SDK.Battle.Tests.csproj

## CI existant
@.github/workflows/ci.yml
</context>

<!-- No SPECIAL-FLOWS.md in this project — skills section omitted -->

<acceptance_criteria>

## AC-1: HeadlessRunner.Run(60 frames) — zéro exception, Moq IEncounterSystem
```gherkin
Given DI container avec GameTimeClock + WeatherSystem + Moq<IEncounterSystem> (retourne []) + NullInputProvider
  And WorldSystem + PlayerSystem enregistrés comme singletons
When HeadlessRunner.Run(sp, frames: 60) appelé
Then 0 exception levée
  And Console affiche "[HeadlessRunner] 60 frames OK — no exceptions"
```

## AC-2: PlayerSystem.Position inchangée après 10 frames avec NullInputProvider
```gherkin
Given DI headless configuré (NullInputProvider — toujours false)
  And PlayerSystem.Position initiale = Vector2(240, 135)
When HeadlessRunner.Run(sp, frames: 10) appelé
Then PlayerSystem.Position == Vector2(240, 135) — zéro mouvement
```

## AC-3: IGameClock.GetTimeOfDay() retourne une valeur TimeOfDay valide après 1 frame
```gherkin
Given DI headless configuré avec GameTimeClock
When HeadlessRunner.Run(sp, frames: 1) appelé
Then IGameClock.GetTimeOfDay() retourne Morning | Day | Evening | Night (pas default(TimeOfDay))
```

## AC-4: 77/77 tests verts — CI inclut SDK.MonoGame.Tests sur ubuntu + windows
```gherkin
Given PokemonSDK.slnx incluant tests/SDK.MonoGame.Tests/
  And .github/workflows/ci.yml avec matrix [ubuntu-latest, windows-latest]
When dotnet test PokemonSDK.slnx --configuration Release
Then 77/77 tests verts (74 existants + 3 HeadlessSmokeTests)
  And 0 régression dans SDK.Core.Tests / SDK.Battle.Tests / SDK.Data.Tests
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: SDK.MonoGame.Tests project + HeadlessSmokeTests (3 tests)</name>
  <files>
    tests/SDK.MonoGame.Tests/SDK.MonoGame.Tests.csproj,
    tests/SDK.MonoGame.Tests/HeadlessSmokeTests.cs,
    PokemonSDK.slnx
  </files>
  <action>
    1. Créer tests/SDK.MonoGame.Tests/SDK.MonoGame.Tests.csproj :
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <TargetFramework>net10.0</TargetFramework>
           <ImplicitUsings>enable</ImplicitUsings>
           <Nullable>enable</Nullable>
           <IsPackable>false</IsPackable>
         </PropertyGroup>
         <ItemGroup>
           <PackageReference Include="coverlet.collector" Version="10.0.1">
             <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
             <PrivateAssets>all</PrivateAssets>
           </PackageReference>
           <PackageReference Include="FluentAssertions" Version="8.10.0" />
           <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
           <PackageReference Include="Moq" Version="4.20.72" />
           <PackageReference Include="xunit" Version="2.9.3" />
           <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="../../src/SDK.MonoGame/SDK.MonoGame.csproj" />
           <ProjectReference Include="../../src/SDK.Core/SDK.Core.csproj" />
           <ProjectReference Include="../../src/SDK.Data/SDK.Data.csproj" />
         </ItemGroup>
       </Project>
       ```
       Versions copiées exactement depuis SDK.Battle.Tests.csproj (cohérence — ne pas changer).

    2. Créer tests/SDK.MonoGame.Tests/HeadlessSmokeTests.cs :
       ```csharp
       namespace SDK.MonoGame.Tests;

       using FluentAssertions;
       using Microsoft.Extensions.DependencyInjection;
       using Microsoft.Xna.Framework;
       using Moq;
       using SDK.Core.Enums;
       using SDK.Core.Interfaces;
       using SDK.Core.Services;
       using SDK.Data.Models;
       using SDK.MonoGame.Input;
       using SDK.MonoGame.World;

       public class HeadlessSmokeTests
       {
           private static IServiceProvider BuildHeadlessServices()
           {
               var mockEncounters = new Mock<IEncounterSystem>();
               mockEncounters
                   .Setup(e => e.GetZonesByIdentifier(It.IsAny<string>(), It.IsAny<int>()))
                   .Returns(new List<EncounterZone>());

               var services = new ServiceCollection();
               services.AddSingleton<IGameClock, GameTimeClock>();
               services.AddSingleton<IWeatherSystem, WeatherSystem>();
               services.AddSingleton<IEncounterSystem>(_ => mockEncounters.Object);
               services.AddSingleton<WorldSystem>();
               services.AddSingleton<PlayerSystem>();
               services.AddSingleton<IInputProvider, NullInputProvider>();

               return services.BuildServiceProvider();
           }

           [Fact]
           public void Run_60Frames_NoException()
           {
               var sp = BuildHeadlessServices();
               var act = () => HeadlessRunner.Run(sp, 60);
               act.Should().NotThrow();
           }

           [Fact]
           public void Run_10Frames_PlayerPositionUnchanged_WithNullInput()
           {
               var sp = BuildHeadlessServices();
               var player = sp.GetRequiredService<PlayerSystem>();
               var before = player.Position;
               HeadlessRunner.Run(sp, 10);
               player.Position.Should().Be(before);
           }

           [Fact]
           public void Run_1Frame_ClockReturnsValidTimeOfDay()
           {
               var sp = BuildHeadlessServices();
               HeadlessRunner.Run(sp, 1);
               var clock = sp.GetRequiredService<IGameClock>();
               var validValues = new[] { TimeOfDay.Morning, TimeOfDay.Day, TimeOfDay.Evening, TimeOfDay.Night };
               validValues.Should().Contain(clock.GetTimeOfDay());
           }
       }
       ```
       Notes :
       - Moq<IEncounterSystem> évite toute dépendance DB dans les tests MonoGame
       - NullInputProvider (toujours false) garantit PlayerSystem ne bouge pas
       - GameTimeClock pur C# — update delta = 16.67ms/frame via HeadlessRunner interne
       - `List<EncounterZone>()` requiert using SDK.Data.Models (ref SDK.Data dans .csproj)

    3. Modifier PokemonSDK.slnx — ajouter dans la section <Projects> :
       <Project Path="tests/SDK.MonoGame.Tests/SDK.MonoGame.Tests.csproj" />
  </action>
  <verify>dotnet build PokemonSDK.slnx && dotnet test PokemonSDK.slnx 2>&1 | tail -10</verify>
  <done>AC-1 + AC-2 + AC-3 + AC-4 partiels : 77/77 tests verts sur machine locale</done>
</task>

<task type="auto">
  <name>Task 2: Activer step headless dans ci.yml + cleanup TODOs Phase 3</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    Modifier .github/workflows/ci.yml :

    1. Remplacer le bloc commenté Phase 3 :
       ```yaml
       # TODO: Phase 3 — activer quand SDK.MonoGame créé
       # - name: Headless smoke test
       #   if: matrix.os == 'ubuntu-latest'
       #   run: dotnet run --project src/SDK.MonoGame -- --headless
       ```
       Par le step actif :
       ```yaml
       - name: Headless smoke test (SDK.MonoGame --headless)
         if: matrix.os == 'ubuntu-latest'
         run: dotnet run --project src/SDK.MonoGame --configuration Release -- --headless --max-frames=10
         working-directory: ${{ github.workspace }}
         continue-on-error: true
       ```
       Note : `continue-on-error: true` — le step headless teste Program.cs + DI complet
       (inclut DB path). DB absente en CI → exit propre mais non bloquant.
       Les tests xUnit (SDK.MonoGame.Tests) sont la vraie porte de validation.

    2. Ajouter `fail-fast: false` à la stratégie matrix (évite qu'un OS blocking bloque l'autre) :
       Remplacer :
       ```yaml
       strategy:
         matrix:
           os: [ubuntu-latest, windows-latest]
       ```
       Par :
       ```yaml
       strategy:
         fail-fast: false
         matrix:
           os: [ubuntu-latest, windows-latest]
       ```

    3. Garder le step Phase 7 commenté tel quel (pas encore implémenté).

    Vérification : le fichier ci.yml doit être YAML valide — vérifier avec :
    `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML OK"`
  </action>
  <verify>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML OK"</verify>
  <done>AC-4 complet : CI workflow valide, matrix ubuntu+windows, step headless actif, fail-fast: false</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/ (tous les projets source — 03-01→03-03 finalisés)
- tests/SDK.Core.Tests/ (24 tests — zéro toucher)
- tests/SDK.Battle.Tests/ (30 tests — zéro toucher)
- tests/SDK.Data.Tests/ (20 tests — zéro toucher)
- .github/workflows/deploy-*.yml (hors scope)
- .github/workflows/publish-nuget.yml (hors scope)

## SCOPE LIMITS
- Pas de MonoGame.Extended / TilemapRenderer réel — restera stub (Phase 4+)
- Pas de compilation shaders MGCB — Content.mgcb reste vide (Phase 7 DX)
- Pas de tests visuels / GL context — HeadlessRunner only
- Pas de nouveaux tests pour SDK.Core / SDK.Data / SDK.Battle — hors scope
- Pas de secrets CI / deploy configuration — ci.yml build+test uniquement

</boundaries>

<verification>
Avant de déclarer le plan complet :
- [ ] dotnet build PokemonSDK.slnx → 0 erreurs, 0 warnings critiques
- [ ] dotnet test PokemonSDK.slnx → 77/77 verts (74 + 3 HeadlessSmokeTests)
- [ ] python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" → YAML OK
- [ ] grep -r "MonoGame.WindowsDX" tests/SDK.MonoGame.Tests/ → 0 résultats (D-02)
- [ ] grep "fail-fast: false" .github/workflows/ci.yml → présent
- [ ] grep "Headless smoke test" .github/workflows/ci.yml → step actif (pas commenté)
- [ ] grep "SDK.MonoGame.Tests" PokemonSDK.slnx → présent
</verification>

<success_criteria>
- Tous les tasks complétés
- 77/77 tests verts (0 régression + 3 nouveaux HeadlessSmokeTests)
- ci.yml YAML valide, matrix ubuntu+windows, step headless actif
- PLAT-02 satisfait : CI couvre linux + windows
- Phase 3 complète : 4/4 plans exécutés
</success_criteria>

<output>
Après complétion, créer `.paul/phases/03-world-foundation/03-04-SUMMARY.md`
</output>
